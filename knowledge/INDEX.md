# Knowledge Index

One line per entry. The manager reads this file at every session start; keep entries short and pointer-like.

- `lessons.md#2026-07-31-w35-green-and-wrong` — **A re-pin went GREEN AND WRONG: a self-citation auto-resolved to the FIRST line matching its anchor text (`:762`, an unrelated docstring) instead of the occurrence inside the function it names (`:8586`, in `_sweep_husks`), and 3579 tests passed on it** — the anchor assertion checks only *does the cited line contain this string*, so **the substring was the whole gate** (2026-07-28's magic-substring lesson, arriving on the citation side). *A re-pin script that takes the first grep hit is a citation generator, not a citation fixer.* **Conflict count did not predict re-pin count, 3rd wave running — and the mechanism is now named: assertion ordering MASKS later citation rot.** Two merges, 19 conflict hunks (ALL pure line-number citations, zero semantic), **20 distinct numbers**, **six iterative pin runs**; only 3 of merge 1's 7 were visible on the first run. **A single red/green read of the pin file is a lower bound, never a census — run it to fixpoint.** **A stale branch can be CORRUPT where main is merely stale** (`cmd_respawn:6308-6256`, a backward range, vs HEAD's `6302-6304`) — resolving toward HEAD was right on merit, so read the sides before trusting "take either". Both merges **predicted exactly and derived, not inherited** (baseline re-measured 3499/14/1; merge 1 → 3518 via `--collect-only` proving "+19" by construction; merge 2 → 3579 from 3594 = 3533+61), identical on 3.13 and 3.10, receipts 68/68 both times; `main` 0726914→467b72d, origin byte-identical each step, both parents of both merges proved arithmetically. **The respawn defect looks like DOCTRINE, not code, and may be a 3-day-old regression of an already-root-caused finding**: `cmd_respawn`'s OWN docstring says "truncated per the registry schema", the cap is `task[:200]`, and `dispatch_bg` then CLOBBERS the full task file with the short recomposition — while 2026-07-23 already root-caused "respawn/send truncation" to that same cap, so **a root cause recorded in `knowledge/` is not a fix and nothing re-executes it**. Spawn re-confirmed sound live (both briefs survived at 116/129 lines). **A prediction that came true on schedule beats an observation**: the identity-witness leak returned at exactly the boot the predecessor said it would. **A FAIL row can be correct in its finding and DEAD in its remedy** — doctor still says ND4(c) is "BLOCKED on an operator ruling" that has since been made (R2, 3rd instance, this time by expiry). **`git worktree remove` has a third failure mode**: `w34-rulings` reads "is not a working tree" **while its directory still exists** — deregistered, not deleted, and invisible to `git worktree list`, so the 27-list-only census undercounts.
- `lessons.md#2026-07-31-w34-respawn-truncation` — **`fleet respawn` silently ATE the brief it exists to reuse.** Two respawned lanes' task files cut to 784/783 bytes mid-sentence; the lane spawned fresh in the same wave intact at 8927 — clean discriminator, and the supervisor had read both at full length minutes earlier, so the destruction happened AT RESPAWN. Workers got a header and one sentence: no fences, no mission, no WHAT-THIS-BRIEF-GOT-WRONG. **Both recovered independently from the operator record, which is what makes it dangerous** — the defect is invisible in every outcome the fleet records. Root cause NOT established (capped registry task snapshot is the hypothesis); **until fixed, dispatch by `spawn` with a fresh task file**. Also: **a brief can be stale by ordering work that already shipped** (§7's re-grounding and the `:2174` row landed 2026-07-28, R2 already fixed — what was owed was discharging 3 PROVISIONAL markers). **The spec that forbids the work it authorises** — multi-fleet v8 ratified in the docket while its own `Status:` line still read AWAITING OPERATOR RULING and Sequencing item 2 still blocked build dispatch; 2026-07-22's lesson inverted, so **a ratification lands in as many documents as name it**. **An owed follow-up from a ratification can be IMPOSSIBLE**: ND4(c)'s ordered re-grounding is definitionally `IDENTITY_UNRESOLVED`, which ratified ND4(b) governs with the opposite verdict — one input, two bindings, **no discriminator exists**; worker priced 3 candidates, recommended A, took none. **The hole is LIVE and common** (claim-holder on an unstamped cold daemon is exempt from a HARD ceiling; 4 of 4 bodies). **b6 gated RED, so 'merge on green' did not fire** — council-killed 4–0, obsolete, and **replaying it would REGRESS shipped code**, undoing wave 33's P1-4 fold from two commits earlier: *a stale branch's merge cost is not its diff, it is what main has since fixed that the branch would undo.* Salvage was the value — main had **no supervisor-claim wedge doctor row at all** (doctor exited 0 on a fleet that could not boot a supervisor); porting it found 2 defects in the original, incl. the **R2 named-remedy-that-always-fails defect, 2nd instance**. **The identity-witness LEAK vanished for one boot and that is not a fix** — the daemon died with the login expiry and this body was its first dispatch, so it donated its own env; day-4 wedge-2 confirmed from the clean side, and the next dispatch re-seeds it. `git worktree remove` is DENIED while the idle worker still holds the dir (two-step order). Floors 3499/14/1 predicted and hit exactly on both interpreters; receipts 68/68 rc=0; ruling-B bypass event fired on every push as ratified (7th measurement).
- `lessons.md#2026-07-30-operator-docket` — **Docket pass 2026-07-30: six rulings in one sitting — multi-fleet v8 RATIFIED ready-for-build (slice 0 first, round-7 pins as slice conditions); identity clause REPLACED with the substitution model (sid trustworthy, all other env observations are about the daemon's cold-starter; ND4c re-ground + two ritual amendments owed); §7 council ruling ratified WITH `:2174` repair as a condition; §7 grounds re-scoped to effect (subsumed); b6 branch → gate lane, merge on green; branch protection → B, drop/scope the GitHub rule (operator's own settings action; doctrine unchanged).** THE lesson: the docket cleared in minutes once put as structured questions with a recommendation each — the queue was never the operator's latency, it was nobody asking.
- `lessons.md#2026-07-30-w33-p14` — **The conflict count did not predict the damage count.** `w30/p14` merged (main `1466f38`; floors 3498/14/1, predicted before running and hit exactly on both interpreters). Wave 32 priced the CONFLICT perfectly — 2 hunks, pure prose — and named ONE pin as the mechanism; the merge needed **7 sites re-pinned across 5 numbers**, and **5 of the 7 were found by a DIFFERENT pin**. A conflict is where both sides edited a line; a rot is wherever *either* side MOVED a line someone cites — the second set is strictly larger and no merge report shows it. **Price a merge by what MOVED, not by what collided.** Zero conflict markers still not evidence of a correct resolution (2nd wave running, restated by experiment: markers gone, 6 red in 0.70s). Every stale number verified CORRECT AT ITS OWN BASE before being touched; all corrections came from the pins' own derivations, **none hand-picked**. Whole 11-defect census enumerated in ONE pass by walking the pin modules' data structures (3 fix-run-fix cycles → 1). Both parents proved arithmetically, not inferred from "the merge succeeded". Wave 30's "merge cites first and every later lane's rot is caught free" landed as written — p14 is the first customer. **Worktree purge 89→28**: the removable set was RE-DERIVED rather than inherited from the census worker (63/24/2 vs 61/27/1; **every disagreement resolved toward KEEPING**). **`git cherry` beats ancestry in both directions** — 4 trees ahead-by-ancestry with no ref containing them were patch-equivalent and landed — **but patch-equivalence proves the CONTENT landed, not that the REF is disposable** (removing the worktree removes the only handle on a detached HEAD). A scratch-named dir (`_g3_merge`) held 1371 staged lines and live conflict markers. `M .claude/settings.json` on 3 trees was a CRLF/stat-cache artifact (empty content diff; git's only output a warning on *stderr*). **The brief's guards pointed at empty space** — `fix/b6-interface-release`, `fix/outcome-usage-provenance` and `resid-probe*` have no worktree at all, so *a protection aimed at nothing reads exactly like a protection that worked*. CRLF in a Python-written path list made all 61 removes refuse with `'path?' is not a working tree` — **failed safe**, but the message names the path, not the encoding. 5th consecutive bypass event; operator's slot still untouched (10th wave).
- `lessons.md#2026-07-30-pin220` — **Pin tier re-run at claude 2.1.220: 6/6 green on `py -3.13` AND `py -3.10`, zero skips; nothing RED, nothing weakened** (2.1.212 native contract holds at 2.1.220 — closed 9-key roster schema undrifted, `claude rm` clears `--all`, rm taxonomy still rc=1 `no job matching`, G2b/G10/Stop-hook record intact). **CURRENT PIN STATE: verified + stamped at claude 2.1.220** (`state/pin-pass.json`, first stamped 2026-07-26T17:03:45Z, re-verified 2026-07-30) — read this line, not the 2.1.218 in the dated 2026-07-23 entry below. **THE lesson: a dated historical stamp is not a current-state stamp, and this project's only authoritative pin stamp is gitignored** (`state/pin-pass.json`), so **no reader of the repo can answer "what version is the native contract verified at" from the repo** — every git-tracked mention is historical by construction, and this lane was commissioned on a brief that read the 2026-07-23 entry's "stamped at 2.1.218" as present tense when the tier had already run green at 2.1.220 four days earlier. **The remedy is an append, never an edit to the dated line** — rewriting a record of an act to fix a lookup problem falsifies the lesson to save the index. **The vendor-bump gate itself WORKED** (doctor FAILed, a supervisor ran the tier, stamped only after): *a mechanism can be working while the surface describing it is what generates false work.* A green tier still does not clear the doctor row (step 6 stamps a temp FLEET_HOME — the real stamp stays a deliberate act, correctly). **A `--bg` worker still cannot observe the dead-daemon path** (4th restatement): zero skips is positive evidence for the live-daemon branch only, and M4's "background service may be restarting" string is now three waves uncaptured — needs an interactive quiet-machine vantage, not a forced daemon stop. `# volatile` version receipt re-confirmed WARN-never-FAIL (2.1.207 vs 2.1.220, exit 0); floors 2970/14 = 2984 collected, identical both interpreters. **CORRECTED 2026-07-30 — that floor figure DOES NOT REPRODUCE AT ITS OWN COMMIT: `ae94e07` collects 2996, not 2984. Measured since: 2982/14 = 2996 at `981c64a`, and 2996/14 = 3010 at `747402d` (predicted exactly before running, both interpreters). Nothing under `knowledge/` is re-executed by `tools/verify_receipts.py` (it covers `docs/specs/**` only), so this is the surface where an unexecuted number sits dated and authoritative — PREDICT against it before you trust it. Correction table appended to the entry; the dated line itself is deliberately unedited.**
- `lessons.md#2026-07-28-transitive-clearance` — **The §7 exemption that stopped at a frame boundary: `cmd_autoclean` really did not call the gate, and tier 1 delegates to `cmd_archive`, which does.** THE lesson: **a clearance is transitive only if you walked the graph** — *"X does not call G"* is about ONE FRAME, *"nothing reachable from X calls G"* is about a CALL GRAPH, and the first shipped as the second for 24h. Exemption now carried as a **parameter at every frame** (`as_autoclean_tier`), ratified 4/4; the competing `_archive_pass` extraction was **cancelled** because it re-encodes the exemption as an absence-of-a-call, the shape that just failed. **Ground an exemption on EFFECT, never CONFIGURATION** — *"the scheduled task has no session id"* was void one day later when the timer was retired. **A ratified doc can contradict itself and re-grounding one paragraph does not fix it**: §7 said *exempt* in one line and listed `autoclean` in the **binding** Mutating-lifecycle GATED row; fixing only the paragraph ships the same contradiction with better prose attached. **A behavioural pin is only as general as the tiers it RUNS** — the deliberate "no tier of the sweep" test executed 2 of 3 (tier 3 flag-gated, fixture never passed the flag), so a plant under tier 3 left the file green; **a test that advertises totality while iterating a subset is worse than a narrow one, because its name is what the next reader believes.** **4th instance of *a pin written against the mechanism you fixed misses the one you introduced*** — the new AST pin's walk was fault-injectable and its allowlist/guard check was not, i.e. the unprovable half was the half that admits the next delegate. **R2's audience is the ENTRY POINT, not the frame**: the refusal named `--nonce` while `fleet autoclean` has no such flag. **A citation is unverifiable from a tree lacking its target** (the council doc lives on `main`, not the branch citing it). Re-measured: 38/2 **exact** (both gate errors predate the fix); 4/44 **decayed to 0/1**. **A journal is a claim** — predecessor's said *"orientation only"* over ~360 lines of uncommitted finished work; read `git diff --stat`, not the handoff. **An author cannot discharge the operator gate its own work raised** (arrived staged, deleting the open §7 request while its sibling edit said the request was filed).
- `lessons.md#2026-07-28-claims-vs-code` — **The gate found the fix right and every sentence around it wrong.** Stamp: stillborn-handoff defects A/B/C **fixed at `87cbf9a`**. Two adversarial lenses re-verified all three fixes and both still ESCALATEd — **not one finding asked for a code change**; every one was a claim the commit made about itself, or a pin that did not pin. THE lesson: **a pin gated behind a magic substring pins the substring, not the property** — defect C's pin read `if "in flight" in err: assert ...`, and a line-count-identical, single-cause, *reassuring* reword that just omitted `in flight` left the full suite green at 2667 (reproduced: old pin 10 passed, new pin red). **Any `if <literal> in output:` wrapping an assertion is an opt-out the next author gets free.** **Trust the convergent finding**: both lenses independently found `cmd_sup_handoff_begin` stamping `turns = 1` with no `turn_started` — the only surviving MAJOR. **Fixing the site that was wrong reproduces the miss at the next site** → the pin is a source scan over *every* `["turns"] = 1` (7 sites), scoped to the sibling statement list not the enclosing function, watched red at a site it was not written for. **A coincidence that discriminates is not a discriminator**: 7 `turn_started` vs 10 stillborn looked like a boot signal; all 7 timestamps match a `HANDOFF-COMPLETE` **to the second** — `sup-handoff-complete` wrote them, and the correct fix *destroys* that separation. *Find the line of code that writes a correlation before citing it as a detector.* **"rc=0 and no error log" measures that nothing crashed, not that anything was readable** — SPEC §6.1 documented defect B as a working feature via exactly that check. **Half a mechanism invites a fix to the half that already works** (`read_outcomes(name, sid=sid)` *does* open the orphan path; it was starved by the same null). **Grep enumerations before shipping them** (`send`'s "fresh-session path" does not exist). **A range you cannot reproduce from the file you cite** ("75–122 min alive"; actual 43…2254) **is the same defect, quieter.** And: **a file loaded by a process on a schedule is code with worse tooling** — `skills/fleet/SKILL.md` told every supervisor at boot "eight stillbirths across two days".
- `lessons.md#2026-07-30-interface-stress` — **The interface stress test: 17 supervisor waves, 73 commits, the 7-round multi-fleet gate marathon.** THE verdict: one overhead session clears the queue ONLY because the harness compacts — its fleet-meter read 471k against the 200k ceiling, so **the three-tier design holds because the top tier cheats**. Binding constraint = supervisor boot cost (~85–110k window at boot, ~40–65k real runway; `sup-context` reads the WHOLE window — inherit nobody's number); throughput 4x'd on "dispatch in first 30k, merge before reading" (bin/fleet.py merge ≈60–65k, docs ≈35k). **A gate loop does not terminate itself** — 7 rounds all GATING while the kill surface shrank from architecture to mechanics; the terminus is the operator docket. Effect-grounded beat configuration-grounded every round; every ambient channel died, every explicit one survived; the mid-marathon oracle consult was the highest-leverage step. Fence lenses by denying READS+PUSH never writes, re-derive fence lists from disk (15 named, 19 found); `out=` is WRONG evidence (9.5–100×); "WHAT THIS BRIEF GOT WRONG" paid ~every wave. Multi-fleet v8 + identity clause + branch-protection all parked on operator gates, nothing narrowed.
- `lessons.md#2026-07-27-evening-outage` — **The fleet stopped for 12h53m and nobody knew.** THE lesson: **a clean shutdown with no reader is indistinguishable from a healthy fleet** — the release was clean, the journal excellent, `sup-status` said `RELEASED` the whole time, and the statusline rendered it in the same calm white as `sup held`. The outage was on screen and read as a resting state; 29 commits sat unpushed. **Measure an outage before drawing the lesson from it**: a power cut covered 9h14m, so the genuinely uncovered part is **3h38m** in two windows (2h09m release→shutdown, 1h28m reboot→restart) — worse for the design, not better. **Fleet has no post-reboot restart path** (durable-sessions-survive-reboots was built for WORKERS only) — and the operator refused a fleet-side mechanism, correctly: a watcher would fire in a session nobody asked to be fleet-aware (D7) or dispatch with no operator in the loop. **The revival trigger is the operator relaunching the interface session**; the restart path is a ritual step. **`claude-fleet-autoclean` had `StartWhenAvailable: False`** so it dropped the missed sweep — and the ruling was to **delete the timer**, not configure it: `fleet autoclean` moves onto the supervisor's beat and the interface ritual. **A timer sweeps when the clock says so; a beat sweeps when the fleet is alive.** Both: *when the honest mechanism would have to be an injection or an autonomous actor, put the trigger on the human action that was going to happen anyway.* **A signal nobody is obliged to read is not a signal.** **A hook was the obvious answer and the wrong one, 2nd time** — the interface is an ordinary session, so a hook reaching it fires in EVERY session (the D7 leak again): buildable version is pull-only (machine-readable succession-needed fact WITH its cause — ceiling AND usage-limit park; loud on statusline/`sup-status`/`doctor`; one verb for the 3-step maneuver **including the middle step only the interface can do**, since B6 means a supervisor can never complete its own stand-down). **Auto-spawn refused**: a body dispatching its own replacement is how two live supervisors happen. **A permanently-red `doctor` is a disabled `doctor`** (3 FAIL → 1; two were stale). **Measure before deleting even when the record says it's safe** — files documented as carrying "spent plaintext tokens" carried a hash *reference*. **Answer parked decisions through the channel that parked them** (`sup-decision --answer`, not prose in a `send`). **Inherited numbers are not measurements**: re-ran 2563/11 before pushing — right, and that is the point. **POSTSCRIPT — I "falsified" a TRUE hypothesis with a confounded experiment and a worker caught me.** The stillborn-handoff `dontask` theory is **CORRECT**; `handoff-autopsy` refuted my probes. My command (`py -3.13 --version`) was character-for-character in the repo's allow-list, and its `Write` denial in the same turn was the real signal — **a control that isn't controlled is worse than no experiment**, and the fleet's own dogfood repo is the most allow-listed dir on the machine, i.e. the worst place to probe permissions. My second argument ("0 turns") was a **registry accounting defect**, not a fact: 82KB transcripts exist. *When the evidence for a defect comes from the component suspected of being broken, that is not evidence.* Root cause `SUCCESSOR_DEFAULT_MODE="dontask"` (right about prompt-hangs, wrong about denials); **the two defects nobody was looking for cost more than the one everybody was** — `sup-handoff-begin` files the record with `session_id=None` while the sid is in hand, so the successor's own diagnosis went to an orphaned outcome file, and `peek` says "no transcript yet" whenever `sid is None`. **Buy the measurement from someone who is not you, and write briefs that invite the reader to refute the brief.**
- `lessons.md#2026-07-27-day5-surface` — **Day-5 interface tier: the surface lied about the shape of the system.** THE lesson: **a doc describing a CLI must be re-derived from `--help`, never from memory** — `skills/fleet/SKILL.md` was missing FOUR shipped verbs (`sup-spawn`, `sup-context`, `sup-decision`, `knowledge`) and told an interface session to *become* the supervisor (the day-4 claim wedge); `supervisor.md` carried **two `[UNBUILT]` tags on BUILT features**. Six drifts, one `--help` + two greps. **A flat view of a tiered system is a lying view**: the statusline read 14 retired `sup|…|boot` husks as workers and omitted the only field that says whether anything can dispatch — fixed by a PROJECTION not a filter (`workers[].tier` = the BODY, `snap["supervisor"]` = the CLAIM; conflating them reports a husk as command). First live render: `sup held 14m  8 bodies  work 1  idle 24  +9 dead`. **`unknown` must be a rendered word, never silence** (day-4's absence-is-not-evidence applied to a view); a `released` claim never renders an age (§6.3 strips the heartbeat — an age there invents staleness out of a correct stand-down). Cost field removed (Max-20x doctrine: it summed rows that report nothing) and PINNED, because removing a field is a doctrine change. **Line-number citations are a recurring tax** — inserting above `bin/fleet.py:~4600` re-reds the `retired_sids` pins, 2nd time in 2 days; budget the re-pin and warn the supervisor so it re-pins FORWARD. **Process miss: the interface answers a parked decision through `fleet sup-decision --answer`, not prose in a steer** — else routing state and truth diverge, visible only from `sup-status`. Open: a worker measured that `status`/`peek`/`result` DO quarantine, so D4 + the CLAUDE.md "views never quarantine" rule are false of shipped code.
- `lessons.md#2026-07-26-day4` — **Day-4: three-tier switch-over, TWO STACKED WEDGES, and the merge filed as housekeeping.** THE lesson: **a guard postcondition one legitimate caller class can never satisfy is a liveness defect** (B6 keys on a proxy — releaser liveness — for "the prior holder may still act"; the interface never exits, so nobody could boot, not even the releaser). Corollary: **a verb that clears a state must not be gated on that state** (`kill` refused BY the wedge it clears → the no-sid bypass is load-bearing, §7.2 UNRATIFIED). Two repairs killed on evidence: post-hoc attestation is forgeable (`--sid` + published expected answer); provenance-by-absence fails OPEN (`clean` deletes, autoclean archives, `sup-spawn` mints `session_id: None`) — **absence is not evidence here**. Wedge 2 underneath: **the daemon donates the FIRST dispatch's env to every later session**, so a supervisor inherits a worker's `FLEET_WORKER`, takes a claim and can never release it — clearing wedge 1 alone would have produced wedge 2. **Handover length WAS the failure mode**: 5 incarnations burned full contexts reading long handovers and merged nothing while `fix/handoff-seams` sat as "item 5, housekeeping" and its hole ate 6 supervisors; a one-page "merge first, read second" brief merged it on turn 1. Fix-waves-mint-defects **7/7**, twice from a SUPERVISOR'S RULING; **a pin written against the mechanism you fixed misses the one you introduced** (3x); **an allowlist entry is a claim** — a detector shipped blessing the 7th instance of its own class. Merged: §10.4 tombstone, handoff-seams, gate-arm.
- `lessons.md#2026-07-24-day3` — **Operator morning queue cleared + day-3 autonomous run opened**: G-1..G-4 ratified (G-1 + freeze-message harden order), M1 deferred-then-reframed → **full M1+M2 `fleet q` build ordered** (value = long-term multi-session codebase work, not read-dup), both GOALS proposals applied (`3ccb2d5`, tier-policy block live, §7.2 holder-alone), cc-oracle v0.2.0 pushed public, sup-spawn rulings ratified (gen-0 pipe-name + logical "supervisor"; bypass-ack = warn). **Nonce refusal class #4**: never pipe sup-boot/checkpoint through head/tail — NONCE is the last line; §5.7 lever exercised. `send @file` full at 2.1KB via fork-steer. Standing: 4-councilor+synthesis for operator-gated questions. **`core.autocrlf=true` makes `git show HEAD:f | sha256sum` != the worktree file's on a CLEAN tree — a byte-restore proof comparing those two reads clean as dirty; use `git status --porcelain`** (and commit before fault-injecting: `git checkout --` reverts the whole uncommitted file). Template → **v1.10**. **Handoff seams** (`fix/handoff-seams`): begin recorded the successor nowhere abort could read → a stillborn handoff was unabortable, and the drill test had been *fabricating* the missing evidence to stay green; + plaintext-token GC + `--nonce` missing from the abort recipe.
- `lessons.md#2026-07-24-day2` — **Day-2 of the autonomous run: NEXT-SESSION queue DRAINED** — claim-nonce slice (`2d58eba`, §7 gate live), three-tier slice (`c6fde34`, resolver/band/ceiling), native-substrate receipts, C/soak retired, residuals fixed; 1897/13 both floors. THE lesson: **the shipped gate gated its own manager within the hour (3 refusal classes, all correct, all recovered) — merging machinery that governs the merging session is the sharpest dogfood**; legacy→nonce live upgrade per §5.3. Band doctrine: 4 lossless BAND-HANDOFFs (first live exercises), two stopping BEFORE design-open work = doctrine working. Fix-waves-mint-defects 5/5 (marketplace-name SPURIOUS-FIX whose own test encoded the wrong name). Fences in worker vocabulary ("no push of ANY ref"). 10h host outage absorbed losslessly (same-sid heartbeat wart path, 4th exercise). Survived: council-ruling extension precedent (E(2) family widening cites grounding, doesn't re-litigate).
- `lessons.md#2026-07-23-overnight-dogfood` — **First fully-autonomous overnight campaign (M-F external #2: cc-oracle; council-governed, operator asleep).** THE lesson: **the freeze verdict scored a live catch — the "dead" holder was still checkpointing 24 min into the new session; a 2–1 council seize-now ruling was built on a stale evidence snapshot (re-verify load-bearing facts at ACT time, not ruling time; "roster-gone" ≠ dead)**. `dontask` = auto-deny-everything → per-worktree `settings.local.json` allowlists with deny-rules-as-hard-fences (`git push`, `claude plugin`) became doctrine; registry task snapshot is 200 chars (respawn/send truncation root-caused). Adversarial review: 1 CRIT + false-block MAJ past 118/0 green; fix-waves-mint-defects 5/5 lifetime incl. a SPURIOUS-FIX whose own test encoded the wrong marketplace name (stale-plan-doc value). Shipped: cc-oracle mf/integration 147/0 sound (unpushed, operator merges), doc-sync 1d878cf pushed, M1 evidence doc, `docs/OVERNIGHT-2026-07-23.md` full gated-decision ledger. pmbot knowledge corrected (trunk=master; overnight hazards). New: `projects/claude-oracle.md`.
- `lessons.md#2026-07-23-three-tier-ratified` — **M-F re-draft campaign: three-tier-command.md RATIFIED spec-of-record** (5 waves, 3 dual-lens gate rounds + 2 confirmations, ~30 findings, 0 spurious; supervisor TOP tier + UL fallback chain, 150–200k band binds supervisors AND workers, tier-based provider-agnostic resolver, cost caps flag-gated off; 3 claim-nonce prerequisites gate the BUILD). H1 shipped: verify_receipts errors on unclassifiable receipt shapes (3rd silent-drop recurrence; founding artifact held 16 evasions vs the probe's 4; 20-mutation review found 6 survivors). Process → template **v1.9**: RULING-LANDS-FIRST (a steered ruling without its commit on the target branch = manager-owned CRITICAL), mutate-the-detector, operator-amendment waves don't burn the escalation budget. Pin tier 6/6 green + stamped at claude 2.1.218.
- `lessons.md#2026-07-22-hook-removal` — **D7: the SessionStart hook briefed EVERY session on the machine — including unrelated projects.** Operator dumped context in another repo and found this fleet's open gates + all 19 workers + INDEX.md sitting in it (~7k chars). Hook + plugin `hooks` key deleted; fleet is **pull-only**, manifest declares no hooks, test pins it. Tests 1403→1393, green on 3.13 and 3.10. **THE lesson: a guard written against the case you noticed does not cover the case you didn't** — D5's own docstring said "fires in EVERY session", then guarded only workers; the other half went unguarded 13 days. **The leak is invisible from inside the repo that leaks** (every test ran where the briefing looks correct; only a session elsewhere shows the defect) — for anything installed machine-wide, the test that matters runs outside the project. **Filtering was the obvious fix and the wrong one**: the question is not "make this injection smaller" but "is this injection the plugin's to make". Ritual + ask-first gates moved to `skills/fleet/SKILL.md`. **Deleting a consumer strands its data**: `~/.claude/fleet-home` had exactly one reader (the hook). Same-day follow-up **deleted it** rather than documenting it — stamping it was fleet's only *unconditional* write to global machine state, and the obvious rescue (give it a reader) would let a stale marker silently redirect `fleet clean`/`kill` at another fleet's registry. Doctor 23→22. **A dead mechanism kept "for the future" still pays its costs while its benefit is hypothetical.** **Editing the repo does not fix an installed plugin** — it runs from `~/.claude/plugins/cache/`; bumped 0.1.0→0.2.0. Final: 1383 passed / 8 skipped on 3.13 and 3.10, doctor 22 PASS / 0 FAIL.
- `lessons.md#2026-07-22-operator-decisions` — Operator decisions + doc reconciliation pass. **SDD R1–R4 RATIFIED** (Phase-1 `spec verify` behind `sdd.enabled`, default off; scope = slice ∩ whole-spec; advisory judges; specs git-tracked from birth). **PROCESS DEFECT: a ratification is not landed until the dated `lessons.md` line exists** — it lived only in a commit message and a `Status:` line, so for two days the spec said yes while OPERATOR-GATES / NEXT-SESSION / PLAN-PROGRESS said no and lessons.md said nothing; the rule existed and was skipped, which is what made the contradiction invisible from any single document. **A fresh doc can be born stale**: a design written that day pasted a 7832-line figure in five places (actual 8706) — `verify_receipts.py` covers `docs/specs/**` only, so nothing re-runs a number in design prose. One pass, zero code defects, all drift doc-side.
- `lessons.md#2026-07-23-doc-pass-2` — Doc pass 2 (full claim-vs-reality audit, one day after the last one; measured 1403 passed / 8 skipped identical on 3.13 AND 3.10, doctor 23 PASS, fleet.py 9091 lines, `main` the only branch). **Docs drift in one direction: under-claim what shipped, over-claim what works.** `fleet attach` was advertised as working in README x3 + getting-started x2 while `cmd_attach` raises unconditionally — fenced off at M-B, user docs never swept. **When a verb is fenced off, grep the user-facing docs for its name in the same commit; the spec is not the surface that lies.** `21 checks` was wrong in 3 files incl. a pasted grep receipt in `SPEC.md` — **the spec of record is the one doc `tools/verify_receipts.py` does not cover**. SPEC's `c63d7dd` pin is 4 milestones back so every @NNNN anchor is wrong (banner added; re-pinning is a campaign, not an edit). ROADMAP/PLAN still gate on a `SOAK GATE 1 SIGNED` line that has **never been written**; doctrine alive, sequencing dead — operator gate opened.
- `lessons.md#2026-07-22-reconcile` — Reconcile (posix/unix port + provider docs merged to `main`; 4 workers, 1 break-lens gate; Windows 1302→1376, **Linux 200 failures → 0**). **A stub platform backend is a coverage blindfold** — `_PosixPlatform` raised on every call, so `main` failed 200 tests on Ubuntu and no alarm existed. **Duplicated work merges worse than divergent work**: all 14 conflict hunks were M-E landed twice by different routes; the posix port itself conflicted with NOTHING because it lives behind the adapter. Split a merge by **disjoint file sets** (2 resolvers + manager splice + `commit --amend` = one merge commit, both parents). A **"closed set" that consults the environment is not closed** (fixed-UTC allowlist via `zoneinfo`; 7/8 members absent on stock Ubuntu; safe degradation hid it). **`.lower()` on a path is a false MATCH on posix**, not a false MISS — and that predicate decides which scheduled job may be deleted. **The floor you declare is the floor you must run** (`add_note()` 3.11+ broke Ctrl-C at the 3.10 floor; 2nd time a 3.11+ API beat a grep). Fault injection found 2 behaviours with **no pin** that left 1376 tests green on both OSes. Make the reviewer **attack the manager's attribution**: the `comm -23` failure-set census proved zero merge-unique failures. macOS remains developed-on but **unreceipted**.
- `lessons.md#2026-07-21-me` — M-E (daemon-wedge detection + 2 shipped-code defects + UL follow-ups + claim-nonce spec; 4 workers + 6 reviewers, 5 dual-lens gates, 12 fix waves, tests 1136->1302). **9th live catch = the SUBSTRATE, not our code and not the vendor contract**: a stale `~/.claude/daemon.lock` naming a Windows-RECYCLED pid (given to a service whose start time is unreadable) killed every `--bg` dispatch machine-wide for ~16h while doctor read all-PASS; `daemon stop --any` does not clear it. **A detector must detect its founding incident** — the shipped wedge check PASSED on the exact outage it was built from, because its test fabricated two evidence lines that never happened; when the real artifact exists, the fixture IS the artifact. **Demand-driven evidence never carries a count/span threshold** (adding evidence made the verdict weaker: 1 refusal detected, 2 at 29s missed — it measured the operator's retry cadence). **A reviewer-ordered remedy is a fix wave** (6/6 waves minted a defect; the `me/ul` one was the reviewer's own `skipif`, which could silently disable 17 tests green). **VANTAGE gate**: probe-context is necessary and not sufficient — the manager filed a phantom regression measured from inside a worktree. **Receipts are executable claims**: `tools/verify_receipts.py` + `tests/test_receipts.py`, resolved at each `# at <sha>` pin, seed-tested 3x — a tool that only DETECTS a class does not prevent it. Design: **a bearer secret cannot authorize on a substrate with no privilege separation** (claim-nonce reframed to detection; `Status: drafting`, operator-gated). Template -> **v1.8**.
- `lessons.md#2026-07-20-nfc` — NFC ship arc (5 PRs to main in one day + prod deploy; 3 workers + manager slices). **Deploy-script triple**: never pipe a gating build (masked exit code killed a healthy server after a failed build); prod builds MUST SQLX_OFFLINE=true; **binary+DB-backup is the rollback unit** (old binary VersionMissing()s on a migrated DB). **Classifier = ops actor**: intermittently blocks merges/sup-checkpoint, hard-blocks self-permission-widening + prod-dir mutations (rightly) — package prod mutations as operator-run scripts. Adversarial verify earned again at 21-string scale (KK word-order bug). **Claim wart bit a 3rd time** (fork rotated sid under live claim) — nonce slice unblocked+overdue. Guardrail pattern: gate the transition not the tenure; 422 re-renders carry submitted state.
- `lessons.md#2026-07-18-md` — M-D (portability directive + three-tier design gate + 2.1.212 rehome + UL parser; 4 workers + 2 branch reviewers, 2 code branches merged, tests 1054→1136). **8th live-tier catch = a VENDOR change**: claude 2.1.212 made the daemon transient (idle-exit 5s, no service install; rm/stop rc=1 3-way ambiguous; agents --json serves stale roster) — run the pin tier on every `claude` bump, not just merges. **Probe-context is uncontrolled**: a `--bg` worker CANNOT observe the dead-daemon path (it's a live daemon client) — the manager's interactive post-merge FLEET_LIVE run is the standing verification. Fix waves minted a defect in 4/4 waves again; `ESCALATE-beats-3rd-wave` held (both branches: 2 waves + final gate). **Three-tier design gate = RESTRUCTURE, both lenses independent, same root cause**: supervisor claim keyed on session_id, fork-steer rotates it → first beat breaks the claim → per-body nonce is a hard prerequisite slice (adjudication `docs/reviews/THREE-TIER-ADJUDICATION-2026-07-17.md`); gate also grep-surfaced 2 shipped defects (handoff dispatch bypasses choke point → successor supervisors have no hooks; autoclean ownership predicate path-only). **Claim wart**: `sup-boot` refuses its own holder on a stale heartbeat (no self-recognition) + can't tell operator-authorized `claude stop` from a daemon restart (no `sup-release` verb) — both feed the nonce spec. Template → **v1.7** (probe-context gate, vendor-bump gate). Portability directive: SPEC.md now binds multi-platform Win/macOS/Linux.

- `docs/PRIOR-ART.md#local-prior-art` — exPardus knowledge librarian (2026-07-17): automated knowledge-staleness loop, adopted into phase-5 spec + template v1.5 — evidence-gated retirement (verdict alone never retires; grep receipt required), destruction circuit breaker, mass-ref-miss = config-error abort, randomized fence tokens for untrusted content in judge prompts, `last_verified`+source-anchor metadata; code at `C:\proga\exPardus\expardus_knowledge\tools\knowledge\librarian\`.
- `projects/pmbot.md` — polymarket_experimenting (Rust latency-arb + Python): per-crate cargo (workspace clippy fails on PyO3), frozen kernel/parity, never-read-collector.db rule, git-worktree recipe for parallel collision-prone tasks, cold-build cost ~$1.7/worker.
- `lessons.md#2026-07-09-c3` — Campaign 3 (pmbot Plan-3 parallel Rust TDD via worktrees): worktree-per-task → spawn --dir → wait --all → merge → cleanup; workers' own subagents confirm Python→Rust port fidelity; bypass mode + $6 budget fit; worker correctly overrides "don't touch X" for exhaustive-match compile necessity; concurrent managers coexist via name-prefix retirement discipline.
- `lessons.md#2026-07-09-c4` — Campaign 4 (pmbot Plan 3 complete, safety-critical Rust, 5 workers ~$22): **builders build, adversaries find** — 5 real bugs shipped past green tests AND a prior opus review, all caught by adversarial reviewers with repro authority; **fault-inject your tests or they're theater** (break production N ways, any test that stays green is CRITICAL); hostile reviewer framing beats generic "review this" (which returned a bare "No issues" on a diff with a proven bug); name the trap in the worker brief; "the plan's SDK snippets are indicative, not authoritative"; `fleet result` truncates → read the worker's commits; API-529 death after commits is invisible from `result`, `git log` is the only truth.

- `playbooks/spawn-etiquette.md` — doctrine for spawning and running workers (one task per worker, respawn over marathons, wait-in-background, permission modes, budget caps, batch spawns).
- `lessons.md#2026-07-07` — Campaign 0 (building fleet): dual-lens review doctrine, live-smoke-beats-unit-tests, Stop-block race is by-design (check idle+mail), sticky dead + respawn recovery, cost model.
- `lessons.md#2026-07-08` — Campaign 1 (haiku demo): full lifecycle green; fleet.cmd needs full path (not on PATH); mid-turn steering proven; respawn re-executes done tasks (not idempotent); "Final message = X" output contract works.
- `lessons.md#2026-07-08-c1` — Campaign 1 (SPEC v2.1 amendment pass, ~15 workers): PROCESS CHANGE #1 — tag folded findings `[UNBUILT — owned by <kernel>]` + split regressions passes-today vs pins-unbuilt (prevents the 1D fix wave); amend campaign-template.md.
- `lessons.md#2026-07-08-c1` — doc-adapted truth gate: anchor+witness grep per finding (not anchor-only) + manager spot-checks spec-vs-code claims against bin/fleet.py before ordering a fix; amend campaign-template.md.
- `lessons.md#2026-07-08-c1` — ops: mid-campaign feature-fold via `*-INTENT-*.md` binding note through the same gate (UL1/UL2); 7-wide disjoint commits = zero index.lock; `fleet result` cp1252 unicode crash → `PYTHONIOENCODING=utf-8` (C2 fix candidate); UL2 subagents default-on, doctor-safe.
- `lessons.md#2026-07-09-c2` — Campaign 2 (harness + 11 hardening kernels, first code campaign, 506 tests): **the fleet safely modifying itself is EARNED** — revert-on-red exercised end-to-end (merge→red→`revert -m 1`→worktree fix→re-merge→green). 2 real breaks caught by adversarial lens (double-launch HIGH, UL1 false-park MED).
- `lessons.md#2026-07-09-c2` — C2 PROCESS CHANGES → campaign-template v1.2: (1) hook-source `worktree` demo tests must `pytest.skip` under default `main`, never hard-assert (§2 + §5); (2) `git checkout -- tests/fixtures/streams/` before every git-state check — live harness non-idempotent on corpus (§5); (3) `git log` is the only truth a turn landed — 529 freezes cost/journal (§3 g); (4) re-merge after a reverted merge = `git revert <the-revert>` then `git merge` (§5).
- `lessons.md#2026-07-09-c2` — C2 ops: `fleet result`/`peek` cp1252 crash STILL live (C2 out-of-scope); new standing post-merge checks (FLEET_LIVE tier, template re-render, new doctor checks, hook-smoke); new CLI — `resume-limited`, `--token-ceiling`, `over_budget`/`over_ceiling`/`limited` statuses.
- `lessons.md#2026-07-09-dogfood-stupidbox` — External dogfood #1 (`stupidbox`, first non-fleet campaign, soak day 1: 7 launches, doctor clean). PROCESS CHANGE: `fleet respawn` ignores task-file edits — re-pass `--task @file` to change scope (also avoids the truncated-task-snapshot loss). Reusable: dispatcher-first lazy-import scaffold = clean N-wide disjoint parallelism in a fresh repo; single fleet install surfaces foreign concurrent campaigns in your status/doctor — retire only names you spawned. See `projects/stupidbox.md`.
- lessons.md — 2026-07-09: `Bash(fleet:*)` in a read-only slash command let a probe run `fleet clean`; grant subcommands, not the CLI. Recover swept workers by sid from `events.jsonl`.
- lessons.md — 2026-07-09: four silent failures (tests clobbering ~/.claude, /fleet shadowed by the skill, inline `!` isn't bash, cp1252). All found by running, none by tests.
- `lessons.md#2026-07-10-c4-spec-portability` — C4 spec wave (docs-only, 4 workers, ~$36, ESCALATED at loop 2). **An author may not promote its own spec** (caught self-promoting; manager reverted). **A fix wave's failure mode is a new defect one call site away** — both waves fixed their target and broke an adjacent thing, each specifying against an inspection-built call-site list instead of a grep. `DISPUTED: none` across 19 findings → add `SPURIOUS-FIX` to re-review verdict vocabulary (none found; all 19 real). Technical: a probe ctime representation is a correctness surface — REALTIME/monotonic mixing → false-`gone` → double-launch; boot-relative ticks → false-`alive` → `killpg` on an unrelated process group (boot spans <2.3s, one ±2s window; ROADMAP Phase-2 logon manager spawns inside the boot burst); `btime` is REALTIME-derived too; the boot_id gate's null-rule fires at `launch_turn` → every Linux worker born dead. WSL = real repro authority, grant it in the task file. Over-ceiling worker refuses `send` → `respawn --task @file --max-budget-usd`. OPEN FOR ALTAI: ~~2 PLAN.md defects + the `turn_pid_boot_id` schema/ownership question~~ [RESOLVED 2026-07-20 sweep: PLAN amendments #1/#2 landed 2026-07-10 (PLAN-PROGRESS.md:49,51); F33 turn_pid_boot_id cancelled by the native pivot (§6)].
- `lessons.md#2026-07-10-c4-spec-portability` (see the CLOSED sub-entry) — **C4 spec wave, closed: portability spec `ready-for-build`, SPEC.md v2.2 + F33 ratified, 7 workers ~$80, zero code touched.** THE lesson → campaign-template **v1.4 GREP-RECEIPT GATE**: an enumeration produced by inspection was wrong FIVE times (fix wave 1; fix wave 2 → every Linux worker born dead; the ratified PLAN.md contract, 3 edits vs 11; a LOW too small to grep, which recreated F20's drift; the manager's own sweep, which left 7 false `[UNBUILT]` tags from C2 standing — a builder would have rebuilt 4 working features). All invisible to two adversarial reviewers reading carefully; each took one grep. Corollaries: audit prose not just tags; MOVE stale pins, never delete; no author promotes its own spec (bound the manager too); `SPURIOUS-FIX` a required re-review verdict; ESCALATE beats a 3rd fix wave. Probe-ctime is a correctness surface (NTP step → double-launch; boot-relative ticks → `killpg` on an unrelated process group; boot_id gate, fresh-read discriminator, hoist above `Popen`). A transient 403 kills a turn exactly like a 529 — `git log` is the only truth.
- `lessons.md#2026-07-17-mc` — **M-C COMPLETE, pivot DONE** (§6 legacy deleted −7130 lines; autoclean shipped + schtasks live; SPEC v3): roster field-presence rules need a TIME AXIS (startup state-only transient → newborn dead-suspected; 7th live-tier catch); **ZOMBIE-MANAGER class** — host restart can fork-session your own manager into a second live body sharing claim+sid, paraphrase-steering your workers (detect: event-timeline + fork-session cmdline; sup-claim blind, per-body nonce candidate; adjudicate its work through the normal gate, don't discard); fix waves minted defects 3/5 — pinfix reopened double-launch, caught pre-merge ("make cleanup fail, suite stays green" = canonical theater probe); 529-storm: re-brief lean beats resuming 100k transcripts (3m/10m/20m backoff); UL park+resume battle-proven in prod ("resets 12am (TZ)" horizon unparsed → --force-now gap); autoclean: quarantined-registry fail-open was THE critical class (guards live IN the sweep, key on artifact presence); schtasks must embed --fleet-home + guard BEFORE marker write; path-interpolated `pytest.raises(match=)` is vacuous.
- `lessons.md#2026-07-16` — **M-B native dispatch COMPLETE** (12 tasks + final wave, main ff'd; hybrid: fleet workers implement in worktrees, subagent review pairs + fix waves gate; ~15 pre-merge Criticals). THE lesson (3rd confirmation, strongest): **FLEET_LIVE pin suite caught 3 prod Criticals invisible to 1,162 unit tests + 11 review pairs** — non-bypass spawn hang (fix `--add-dir`), `claude stop/rm` want SHORT id (silent no-op on real sessions), FORCE_COLOR ANSI corrupts short-id parse → false DOA. Also: plan snippets marked verbatim-binding transfer author bugs (demote to indicative mid-campaign, defect class vanished); fix waves mint new Criticals — "new-defect hunt on the fix" in every re-review (fired 3×); Windows CRT O_APPEND NOT atomic (CreateFileW FILE_APPEND_DATA is); reviewer-adjudicates-from-reality catches dispatcher error. M-C inputs in NEXT-SESSION + final-review.md.
- `lessons.md#2026-07-14` — M-0 native-substrate spike: contract at `docs/specs/native-substrate.md` (hooks-in-bg CONFIRMED, fork-steer RATIFIED, silent limit walls, stop-no-hook tombstone duty, taskkill-respawn zombie, rm archival, task-file bootstrap); evidence-discipline reviews caught overclaims in 5/8 experiment tasks; research reports must land in repo before citation.
- `lessons.md#2026-07-14-mA` — M-A supervisor shipped (plan+7 tasks, 764 tests, READY-TO-MERGE): verbatim-code plans run on haiku; reviewer mutation-repro overrides controller instinct; shared-fixture edits silently kill branch coverage; whole-branch review catches cross-task classes (header injection, untested NORMAL seize path); timeout hierarchies are design — document the ordering; 4 M-B fast-follows in NEXT-SESSION.
