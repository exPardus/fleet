# Knowledge Index

One line per entry. The manager reads this file at every session start; keep entries short and pointer-like.

- `lessons.md#2026-07-22-operator-decisions` — Operator decisions + doc reconciliation pass. **SDD R1–R4 RATIFIED** (Phase-1 `spec verify` behind `sdd.enabled`, default off; scope = slice ∩ whole-spec; advisory judges; specs git-tracked from birth). **PROCESS DEFECT: a ratification is not landed until the dated `lessons.md` line exists** — it lived only in a commit message and a `Status:` line, so for two days the spec said yes while OPERATOR-GATES / NEXT-SESSION / PLAN-PROGRESS said no and lessons.md said nothing; the rule existed and was skipped, which is what made the contradiction invisible from any single document. **A fresh doc can be born stale**: a design written that day pasted a 7832-line figure in five places (actual 8706) — `verify_receipts.py` covers `docs/specs/**` only, so nothing re-runs a number in design prose. One pass, zero code defects, all drift doc-side.
- `lessons.md#2026-07-22-reconcile` — Reconcile (posix/unix port + provider docs merged to `main`; 4 workers, 1 break-lens gate; Windows 1302→1376, **Linux 200 failures → 0**). **A stub platform backend is a coverage blindfold** — `_PosixPlatform` raised on every call, so `main` failed 200 tests on Ubuntu and no alarm existed. **Duplicated work merges worse than divergent work**: all 14 conflict hunks were M-E landed twice by different routes; the posix port itself conflicted with NOTHING because it lives behind the adapter. Split a merge by **disjoint file sets** (2 resolvers + manager splice + `commit --amend` = one merge commit, both parents). A **"closed set" that consults the environment is not closed** (fixed-UTC allowlist via `zoneinfo`; 7/8 members absent on stock Ubuntu; safe degradation hid it). **`.lower()` on a path is a false MATCH on posix**, not a false MISS — and that predicate decides which scheduled job may be deleted. **The floor you declare is the floor you must run** (`add_note()` 3.11+ broke Ctrl-C at the 3.10 floor; 2nd time a 3.11+ API beat a grep). Fault injection found 2 behaviours with **no pin** that left 1376 tests green on both OSes. Make the reviewer **attack the manager's attribution**: the `comm -23` failure-set census proved zero merge-unique failures. macOS remains developed-on but **unreceipted**.
- `lessons.md#2026-07-21-me` — M-E (daemon-wedge detection + 2 shipped-code defects + UL follow-ups + claim-nonce spec; 4 workers + 6 reviewers, 5 dual-lens gates, 12 fix waves, tests 1136->1302). **9th live catch = the SUBSTRATE, not our code and not the vendor contract**: a stale `~/.claude/daemon.lock` naming a Windows-RECYCLED pid (given to a service whose start time is unreadable) killed every `--bg` dispatch machine-wide for ~16h while doctor read all-PASS; `daemon stop --any` does not clear it. **A detector must detect its founding incident** — the shipped wedge check PASSED on the exact outage it was built from, because its test fabricated two evidence lines that never happened; when the real artifact exists, the fixture IS the artifact. **Demand-driven evidence never carries a count/span threshold** (adding evidence made the verdict weaker: 1 refusal detected, 2 at 29s missed — it measured the operator's retry cadence). **A reviewer-ordered remedy is a fix wave** (6/6 waves minted a defect; the `me/ul` one was the reviewer's own `skipif`, which could silently disable 17 tests green). **VANTAGE gate**: probe-context is necessary and not sufficient — the manager filed a phantom regression measured from inside a worktree. **Receipts are executable claims**: `tools/verify_receipts.py` + `tests/test_receipts.py`, resolved at each `# at <sha>` pin, seed-tested 3x — a tool that only DETECTS a class does not prevent it. Design: **a bearer secret cannot authorize on a substrate with no privilege separation** (claim-nonce reframed to detection; `Status: drafting`, operator-gated). Template -> **v1.8**.
- `lessons.md#2026-07-20-nfc` — NFC ship arc (5 PRs to main in one day + prod deploy; 3 workers + manager slices). **Deploy-script triple**: never pipe a gating build (masked exit code killed a healthy server after a failed build); prod builds MUST SQLX_OFFLINE=true; **binary+DB-backup is the rollback unit** (old binary VersionMissing()s on a migrated DB). **Classifier = ops actor**: intermittently blocks merges/sup-checkpoint, hard-blocks self-permission-widening + prod-dir mutations (rightly) — package prod mutations as operator-run scripts. Adversarial verify earned again at 21-string scale (KK word-order bug). **Claim wart bit a 3rd time** (fork rotated sid under live claim) — nonce slice unblocked+overdue. Guardrail pattern: gate the transition not the tenure; 422 re-renders carry submitted state.
- `lessons.md#2026-07-18-md` — M-D (portability directive + three-tier design gate + 2.1.212 rehome + UL parser; 4 workers + 2 branch reviewers, 2 code branches merged, tests 1054→1136). **8th live-tier catch = a VENDOR change**: claude 2.1.212 made the daemon transient (idle-exit 5s, no service install; rm/stop rc=1 3-way ambiguous; agents --json serves stale roster) — run the pin tier on every `claude` bump, not just merges. **Probe-context is uncontrolled**: a `--bg` worker CANNOT observe the dead-daemon path (it's a live daemon client) — the manager's interactive post-merge FLEET_LIVE run is the standing verification. Fix waves minted a defect in 4/4 waves again; `ESCALATE-beats-3rd-wave` held (both branches: 2 waves + final gate). **Three-tier design gate = RESTRUCTURE, both lenses independent, same root cause**: supervisor claim keyed on session_id, fork-steer rotates it → first beat breaks the claim → per-body nonce is a hard prerequisite slice (adjudication `docs/reviews/THREE-TIER-ADJUDICATION-2026-07-17.md`); gate also grep-surfaced 2 shipped defects (handoff dispatch bypasses choke point → successor supervisors have no hooks; autoclean ownership predicate path-only). **Claim wart**: `sup-boot` refuses its own holder on a stale heartbeat (no self-recognition) + can't tell operator-authorized `claude stop` from a daemon restart (no `sup-release` verb) — both feed the nonce spec. Template → **v1.7** (probe-context gate, vendor-bump gate). Portability directive: SPEC.md now binds multi-platform Win/macOS/Linux.

- `docs/PRIOR-ART.md#local-prior-art` — exPardus knowledge librarian (2026-07-17): automated knowledge-staleness loop, adopted into phase-5 spec + template v1.5 — evidence-gated retirement (verdict alone never retires; grep receipt required), destruction circuit breaker, mass-ref-miss = config-error abort, randomized fence tokens for untrusted content in judge prompts, `last_verified`+source-anchor metadata; code at `C:\proga\exPardus\expardus_knowledge\tools\knowledge\librarian\`.
- `projects/pmbot.md` — polymarket_experimenting (Rust latency-arb + Python): per-crate cargo (workspace clippy fails on PyO3), frozen kernel/parity, never-read-collector.db rule, git-worktree recipe for parallel collision-prone tasks, cold-build cost ~$1.7/worker.
- `lessons.md#2026-07-09-c3` — Campaign 3 (pmbot Plan-3 parallel Rust TDD via worktrees): worktree-per-task → spawn --dir → wait --all → merge → cleanup; workers' own subagents confirm Python→Rust port fidelity; bypass mode + $6 budget fit; worker correctly overrides "don't touch X" for exhaustive-match compile necessity; concurrent managers coexist via name-prefix retirement discipline.
- `lessons.md#2026-07-09-c4` — Campaign 4 (pmbot Plan 3 complete, safety-critical Rust, 5 workers ~$22): **builders build, adversaries find** — 5 real bugs shipped past green tests AND a prior opus review, all caught by adversarial reviewers with repro authority; **fault-inject your tests or they're theater** (break production N ways, any test that stays green is CRITICAL); hostile reviewer framing beats generic "review this" (which returned a bare "No issues" on a diff with a proven bug); name the trap in the worker brief; "the plan's SDK snippets are indicative, not authoritative"; `fleet result` truncates → read the worker's commits; API-529 death after commits is invisible from `result`, `git log` is the only truth.

- `playbooks/spawn-etiquette.md` — doctrine for spawning and running workers (one task per worker, respawn over marathons, wait-in-background, permission modes, budget caps, batch spawns).
- `lessons.md#2026-07-07` — Campaign 0 (building fleet): dual-lens review doctrine, live-smoke-beats-unit-tests, Stop-block race is by-design (check idle+mail), sticky dead + respawn recovery, cost model.
- `lessons.md#2026-07-08` — Campaign 1 (haiku demo): full lifecycle green; fleet.cmd needs full path (not on PATH); mid-turn steering proven; respawn re-executes done tasks (not idempotent); "Final message = X" output contract works.
- `lessons.md#2026-07-08-c1` — Campaign 1 (SPEC v2.1 amendment pass, ~15 workers): PROCESS CHANGE #1 — tag folded findings `[UNBUILT — owned by <kernel>]` + split regressions passes-today vs pins-unbuilt (prevents the 1D fix wave); amend campaign-template.md.
- `lessons.md#2026-07-08-c1` — doc-adapted truth gate: anchor+witness grep per finding (not anchor-only) + manager spot-checks spec-vs-code claims against bin/fleet.py before ordering a fix; amend campaign-template.md.
- `lessons.md#2026-07-08-c1` — ops: mid-campaign feature-fold via `*-INTENT-*.md` binding note through the same gate (UL1/UL2); 7-wide disjoint commits = zero index.lock; `fleet result` cp1252 unicode crash → `PYTHONIOENCODING=utf-8` (C2 fix candidate); UL2 subagents default-on, doctor-safe.
- `lessons.md#2026-07-09-c2` — Campaign 2 (harness + 11 hardening kernels, first code campaign, 506 tests): **the fleet safely modifying itself is EARNED** — revert-on-red exercised end-to-end (merge→red→`revert -m 1`→worktree fix→re-merge→green). 2 real breaks caught by adversarial lens (double-launch HIGH, UL1 false-park MED).
- `lessons.md#2026-07-09-c2` — C2 PROCESS CHANGES → campaign-template v1.2: (1) hook-source `worktree` demo tests must `pytest.skip` under default `main`, never hard-assert (§2 + §5); (2) `git checkout -- tests/fixtures/streams/` before every git-state check — live harness non-idempotent on corpus (§5); (3) `git log` is the only truth a turn landed — 529 freezes cost/journal (§3 g); (4) re-merge after a reverted merge = `git revert <the-revert>` then `git merge` (§5).
- `lessons.md#2026-07-09-c2` — C2 ops: `fleet result`/`peek` cp1252 crash STILL live (C2 out-of-scope); new standing post-merge checks (FLEET_LIVE tier, template re-render, new doctor checks, hook-smoke); new CLI — `resume-limited`, `--token-ceiling`, `over_budget`/`over_ceiling`/`limited` statuses.
- `lessons.md#2026-07-09-dogfood-stupidbox` — External dogfood #1 (`stupidbox`, first non-fleet campaign, soak day 1: 7 launches, doctor clean). PROCESS CHANGE: `fleet respawn` ignores task-file edits — re-pass `--task @file` to change scope (also avoids the truncated-task-snapshot loss). Reusable: dispatcher-first lazy-import scaffold = clean N-wide disjoint parallelism in a fresh repo; single fleet install surfaces foreign concurrent campaigns in your status/doctor — retire only names you spawned. See `projects/stupidbox.md`.
- lessons.md — 2026-07-09: `Bash(fleet:*)` in a read-only slash command let a probe run `fleet clean`; grant subcommands, not the CLI. Recover swept workers by sid from `events.jsonl`.
- lessons.md — 2026-07-09: four silent failures (tests clobbering ~/.claude, /fleet shadowed by the skill, inline `!` isn't bash, cp1252). All found by running, none by tests.
- `lessons.md#2026-07-10-c4-spec-portability` — C4 spec wave (docs-only, 4 workers, ~$36, ESCALATED at loop 2). **An author may not promote its own spec** (caught self-promoting; manager reverted). **A fix wave's failure mode is a new defect one call site away** — both waves fixed their target and broke an adjacent thing, each specifying against an inspection-built call-site list instead of a grep. `DISPUTED: none` across 19 findings → add `SPURIOUS-FIX` to re-review verdict vocabulary (none found; all 19 real). Technical: a probe ctime representation is a correctness surface — REALTIME/monotonic mixing → false-`gone` → double-launch; boot-relative ticks → false-`alive` → `killpg` on an unrelated process group (boot spans <2.3s, one ±2s window; ROADMAP Phase-2 logon manager spawns inside the boot burst); `btime` is REALTIME-derived too; the boot_id gate's null-rule fires at `launch_turn` → every Linux worker born dead. WSL = real repro authority, grant it in the task file. Over-ceiling worker refuses `send` → `respawn --task @file --max-budget-usd`. OPEN FOR ALTAI: ~~2 PLAN.md defects + the `turn_pid_boot_id` schema/ownership question~~ [RESOLVED 2026-07-20 sweep: PLAN amendments #1/#2 landed 2026-07-10 (PLAN-PROGRESS.md:49,51); F33 turn_pid_boot_id cancelled by the native pivot (§6)].
- `lessons.md#2026-07-10-c4-spec-portability` (see the CLOSED sub-entry) — **C4 spec wave, closed: portability spec `ready-for-build`, SPEC.md v2.2 + F33 ratified, 7 workers ~$80, zero code touched.** THE lesson → campaign-template **v1.4 GREP-RECEIPT GATE**: an enumeration produced by inspection was wrong FIVE times (fix wave 1; fix wave 2 → every Linux worker born dead; the ratified PLAN.md contract, 3 edits vs 11; a LOW too small to grep, which recreated F20's drift; the manager's own sweep, which left 7 false `[UNBUILT]` tags from C2 standing — a builder would have rebuilt 4 working features). All invisible to two adversarial reviewers reading carefully; each took one grep. Corollaries: audit prose not just tags; MOVE stale pins, never delete; no author promotes its own spec (bound the manager too); `SPURIOUS-FIX` a required re-review verdict; ESCALATE beats a 3rd fix wave. Probe-ctime is a correctness surface (NTP step → double-launch; boot-relative ticks → `killpg` on an unrelated process group; boot_id gate, fresh-read discriminator, hoist above `Popen`). A transient 403 kills a turn exactly like a 529 — `git log` is the only truth.
- `lessons.md#2026-07-17-mc` — **M-C COMPLETE, pivot DONE** (§6 legacy deleted −7130 lines; autoclean shipped + schtasks live; SPEC v3): roster field-presence rules need a TIME AXIS (startup state-only transient → newborn dead-suspected; 7th live-tier catch); **ZOMBIE-MANAGER class** — host restart can fork-session your own manager into a second live body sharing claim+sid, paraphrase-steering your workers (detect: event-timeline + fork-session cmdline; sup-claim blind, per-body nonce candidate; adjudicate its work through the normal gate, don't discard); fix waves minted defects 3/5 — pinfix reopened double-launch, caught pre-merge ("make cleanup fail, suite stays green" = canonical theater probe); 529-storm: re-brief lean beats resuming 100k transcripts (3m/10m/20m backoff); UL park+resume battle-proven in prod ("resets 12am (TZ)" horizon unparsed → --force-now gap); autoclean: quarantined-registry fail-open was THE critical class (guards live IN the sweep, key on artifact presence); schtasks must embed --fleet-home + guard BEFORE marker write; path-interpolated `pytest.raises(match=)` is vacuous.
- `lessons.md#2026-07-16` — **M-B native dispatch COMPLETE** (12 tasks + final wave, main ff'd; hybrid: fleet workers implement in worktrees, subagent review pairs + fix waves gate; ~15 pre-merge Criticals). THE lesson (3rd confirmation, strongest): **FLEET_LIVE pin suite caught 3 prod Criticals invisible to 1,162 unit tests + 11 review pairs** — non-bypass spawn hang (fix `--add-dir`), `claude stop/rm` want SHORT id (silent no-op on real sessions), FORCE_COLOR ANSI corrupts short-id parse → false DOA. Also: plan snippets marked verbatim-binding transfer author bugs (demote to indicative mid-campaign, defect class vanished); fix waves mint new Criticals — "new-defect hunt on the fix" in every re-review (fired 3×); Windows CRT O_APPEND NOT atomic (CreateFileW FILE_APPEND_DATA is); reviewer-adjudicates-from-reality catches dispatcher error. M-C inputs in NEXT-SESSION + final-review.md.
- `lessons.md#2026-07-14` — M-0 native-substrate spike: contract at `docs/specs/native-substrate.md` (hooks-in-bg CONFIRMED, fork-steer RATIFIED, silent limit walls, stop-no-hook tombstone duty, taskkill-respawn zombie, rm archival, task-file bootstrap); evidence-discipline reviews caught overclaims in 5/8 experiment tasks; research reports must land in repo before citation.
- `lessons.md#2026-07-14-mA` — M-A supervisor shipped (plan+7 tasks, 764 tests, READY-TO-MERGE): verbatim-code plans run on haiku; reviewer mutation-repro overrides controller instinct; shared-fixture edits silently kill branch coverage; whole-branch review catches cross-task classes (header injection, untested NORMAL seize path); timeout hierarchies are design — document the ordering; 4 M-B fast-follows in NEXT-SESSION.
