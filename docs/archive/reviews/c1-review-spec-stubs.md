# Adversarial review — the 7 injected spec stubs (Wave 1C, `c1-review-spec-stubs`)

Date: 2026-07-08 · Reviewer: fleet worker `c1-review-spec-stubs` (read-only; no spec edited)
Targets: `docs/specs/{phase-2-watchtower, phase-3-telegram, providers, portability, phase-5-intelligence, phase1-hardening-kernels, phase-4-webui}.md`
Cross-checked against: `docs/SPEC.md` (numbered invariants §2 + §4/§5 v2.1 + appendix F31), `docs/reviews/SPEC-REVIEW-2026-07-08.md`, `docs/PLAN.md` (Wave-1B table L108–116 + Findings-traceability L264–300), `docs/reviews/USAGE-LIMIT-RESILIENCE-INTENT-2026-07-08.md`.

## 1. Verdict

The injections are **substantively faithful and near-complete**: every confirmed finding assigned to a stub in the traceability table is present with real technical content (no hollow anchors over vacuous prose), every witness sentence I spot-checked matches the review doc, all seven stubs carry an `## Invariants touched` section, and — critically — **every invariant citation across all seven stubs matches the canonical numbered list in SPEC §2** (no misnumbering). Cross-stub consistency is **strong**: the env-script contract agrees between providers↔portability; the token-ceiling kernel (item 10) agrees across kernels↔phase-5↔providers; the needs-input dependency agrees across watchtower-OQ1↔telegram↔PLAN L300; UL1/F31 agrees between kernels↔SPEC appendix. The graveyard-adjacency answers (harness≠exit-code-capture, ceiling≠$-table, drain-event≠DLQ, park≠auto-adoption) correctly answer causes-of-death and are NOT findings. The defects that remain are **citation and scope-hygiene slips, not design errors**: one wrong finding-number citation (portability cites the §4 amendment as "F20" = auto-respawn cut, when it is F15/B1), one vestige of the pre-cut auto-respawn design left in watchtower F8 (a live "respawn-loop cap" contradicting F20's notify-only v1), and one dangling done-criterion in the web-UI stub ("CSRF protection verified") that contradicts its own READ-ONLY / CSRF-deferred decision. Net: **3 MEDIUM, 3 LOW; cross-stub consistency = PASS with one traceability fix; no F1–F16 / refuted / graveyard relitigation committed.**

## 2. Findings

**MEDIUM — §4 state-machine amendment mis-cited as "F20" (should be F15/B1) — portability.md, OQ2 "Carry (F15/M4)" block —**
> "Per SPEC §4 (as amended by F20), a query that returns 'exists but unreadable' … resolves to the alive-unknown tier — never demoted to `dead`, never respawn/kill-eligible without `--force`"
Why wrong: the alive-unknown tier / never-demote-to-dead / --force gate is the PID-probe three-way semantics of **F15 (= review M4)**, folded into §4 by `spec-amend-1-state` (B1). SPEC.md confirms the ownership — the amended §11 crash row is tagged "(v2.1, **M4**)" (L246) and `probe_three_way` is the M4 regression (L290). **F20 is the *auto-respawn CUT* decision** (traceability L289; watchtower stub L14–15) and amends nothing in §4. The surrounding sentence is *semantically correct*; only the finding-number is wrong. In a process whose entire value is finding-numbered traceability, a drafter chasing "§4 as amended by F20" lands on the watchtower auto-respawn-cut, not the state machine. Fix: in the F15/M4 carry block, change "(as amended by F20)" → "(as amended by the v2.1 state-machine amendment, F15/B1 — `spec-amend-1-state`)".

**MEDIUM — watchtower F8 asserts a live "respawn-loop cap" and respawn as a current action, contradicting F20's notify-only v1 — phase-2-watchtower.md, "Fixed constraints" F8 block vs the F20 DECISION + Invariants-touched —**
> F8 (L30): "every ACTION (send, respawn) executes only via the same lock-guarded fleet.py command paths … On guard/kill FAILURE … count the attempt toward the **respawn-loop cap**; never retry-spin holding the lock."
> vs F20 (L15): "v1 watchtower is DETECT-AND-NOTIFY only." and Invariant-7 note (L70): "the remaining detect-and-notify loop **launches no turns**."
Why wrong: F8 imports review M9's amendment text (review L81 uses the same "count toward the respawn-loop cap" phrase) verbatim, but M9 predates the M10/F20 CUT that the manager adopted. With auto-respawn cut, v1 performs **no respawn action**, so there is no respawn-loop cap in v1 — F8 references machinery F20 removed. A builder reading F8 as a present-tense fixed constraint can construct a v1 respawn action path, exactly the unbounded autonomous mutator F20 ordered out of Phase 2. Fix: scope F8's respawn / respawn-loop-cap clauses to the post-Phase-3 re-vet (when the action path returns), leaving v1 with the read + send/notify discipline only; or annotate "respawn action path + its cap are DEFERRED with F20; the discipline below applies when auto-respawn is re-vetted." (Send-as-action can stay, gated on the open OQ7 manager-wake.)

**MEDIUM — web-UI done-criterion "CSRF protection verified" contradicts the READ-ONLY / CSRF-deferred decision — phase-4-webui.md, "Done criteria" vs "Decisions" (webui-readonly + webui-csrf) —**
> Done criteria (L42): "… CSRF protection verified. (v1 read-only: 'send/attach round-trip' = the copy-command path + CLI/telegram; write-form round-trip is deferred until friction earns it.)"
> vs Decision (L26): CSRF is "spec'd as the gate now and **built only when friction earns the write forms**"; Decision (L25): "all mutating POST → 405."
Why wrong: v1 has zero write endpoints and CSRF machinery is explicitly deferred, so there is no CSRF-protected mutating path to *verify* in v1 — the parenthetical fixed the "round-trip" clause but left "CSRF protection verified" standing as a v1 acceptance gate. The verifiable v1 origin defense is the **Host-header/DNS-rebinding check** (L21), which is not CSRF-token protection. Fix: in Done criteria replace "CSRF protection verified" with "Host-header/same-origin rejection + mutating-POST→405 verified; CSRF-token machinery specced (not built until write forms are earned)".

**LOW — telegram OQ1 re-lists "(kill?)" as an open confirmation question that B5 already FIXED — phase-3-telegram.md, OQ1 vs B5 —**
> OQ1 (L46): "What's dangerous enough to require confirmation round-trip (**kill?** spawn with bypass)? (See the B5 command-set proposal above …)"
> vs B5 (L31): "(3) /kill and /respawn require the same confirm" — declared a "confirmed constraint … NOT an open question."
Why wrong: B5 hard-fixes /kill and /respawn = confirm-with-nonce and the transitive-bypass rule; OQ1 phrasing invites C6 to re-decide whether kill needs confirmation, softening a FIXED security constraint. The OQ should refine only the *residual* grammar (placement of /send, /mute, /spawn) within B5's fixed baseline. Fix: reword OQ1 to "grammar refinement within the B5-fixed confirm baseline (kill/respawn/transitive-bypass are already confirm-fixed; open: where /send, /spawn, /mute land)".

**LOW — B5 command-set proposal omits `/manager`, the highest-privilege command, from the confirm/no-confirm split — phase-3-telegram.md, B5 "Concrete starting command-set proposal" —**
> B5 (L33): "no-confirm = `/status /peek /result /mute`; confirm-with-nonce = `/send /respawn /kill /spawn`"
Why wrong: `/manager <request>` (OQ1 grammar, L46) launches the manager-on-call turn — "the highest-privilege path in the system" (Fixed constraints L23) that can mint bypass workers — yet it appears in neither list. It is *covered indirectly* by B5's transitive-privilege rule (any inbound path that would create a bypass worker → confirm), but a command-set proposal that silently drops the most dangerous command is an incomplete starting point for the security sign-off it is written for. Fix: add `/manager` to the split with its disposition (start = no-confirm; its *actions* that mint bypass workers hit the transitive-privilege confirm), so the enumeration is exhaustive.

**LOW — telegram OQ1 command grammar and the B5 command-set list diverge — phase-3-telegram.md, OQ1 grammar vs B5 proposal —**
> OQ1 grammar (L46) lists `/status /peek /send /spawn /kill /manager`; B5 proposal (L33) lists `/status /peek /result /mute` + `/send /respawn /kill /spawn`.
Why (minor): `/result` and `/mute` appear only in B5; `/respawn` appears only in B5; `/manager` only in OQ1. Two overlapping-but-unequal command inventories in one stub invite a C6 drafter to reconcile them ad hoc. Fix: state once (in B5, the fixed baseline) the full v1 command inventory and have OQ1 reference it, rather than re-listing a partially-overlapping set. (Editorial; both are explicitly C6-owned, so no semantic loss — but reconcile to avoid two sources of truth.)

*Not-a-finding (recorded for the adjudicator):* telegram's needs-input dependency note (L15) calls the Phase-2 disposition "the F20-style disposition." F20 is the auto-respawn cut, so the phrase reads as a mis-citation, but it is used *analogically* — "an explicit cut-or-defer decision recorded, not silently dropped," which is exactly what PLAN L300 requires for needs-input. Defensible; at most swap "F20-style" for "explicitly-recorded (cut-or-defer) disposition" to avoid a reader mistaking it for a content cite.

## 3. Checked-and-clean coverage (per stub)

**phase-2-watchtower.md** — All 10 assigned anchors present & substantive: F8, B2, F20, F21, F22, F23, F24, F25, OQ1 (six-rule list intact — needs-input-detected + journal-staleness both retained, L47–54), F29. Invariants 1/6/7/9 all cited by correct number. F20 auto-respawn CUT is clean: rationale given, lossless-auto-respawn done-criterion explicitly deleted (L75). F21 static-sink (no plugin framework), F22 service→doc-snippets, F23 logon-triggered interactive schtasks + PS-WinRT toast via adapter + file fallback — all sound; F22 correctly answers graveyard #10 (no bespoke restart wrapper). *Only defect: the F8 respawn-loop-cap vestige (finding 2).*

**phase-3-telegram.md** — All 8 anchors present & substantive: needs-input-dep, B4, B5, F16, F17, F18, F19, F30. B4 + B5 are under **Fixed constraints as constraints, not OQs** (lens 5 PASS); each says "confirmed constraint … NOT an open question." Command-set proposal present. Invariants 5/6/7 cited correctly. Witness sentences match review B4/B5/F17/F16/F18/F19/F30. *Defects: OQ1/B5 hygiene (findings 4–6, all LOW).*

**providers.md** — F10 + F14 present & substantive. **Secrets-storage "or" is GONE** (L21: "the 'or' is resolved … the previously-open alternative is dropped") — lens 5 PASS. Env-script contract (per-attach, 0600, gitignored state/, deleted on release, no secret in argv/logs) agrees verbatim-in-intent with portability F10. Demand-gate = BUILD-only, spec runs regardless (L14) — matches PLAN §0.4 / PR5 (lens 4 PASS). Token-ceiling = kernels item 10 (consistent). Invariants 6/8 cited correctly. Clean.

**portability.md** — B3 (test-suite port added to scope), F15, F10, OQ3-scope decision all present & substantive; done-criteria now achievable as scoped (B3 injection sound). macOS/POSIX-notification DEFERRAL is **recorded, not silently dropped** (L34–36; matches PLAN L300) — lens 4 PASS. Linux-attach-built vs Linux-notify-deferred split is coherent with watchtower F23's file-fallback. Invariants 5/8 cited correctly. *Only defect: the F20 mis-citation (finding 1) — content correct, number wrong.*

**phase-5-intelligence.md** — F12 lever (over-ceiling → Stop hook ALLOWS stop, never block; hard enforcement in fleet.py launch paths) + flag-sizing note present & substantive; matches kernels item 10 exactly. Token-ceiling correctly recorded as shipping early in C2, consumed here as doctrine. Invariants 2/6 cited correctly (exit-0 inversion-avoidance reasoning is sound). Clean.

**phase1-hardening-kernels.md** — F11 (kernel-3 PostCompact: template+§7 registration, sid→name resolution with sid-keyed fallback/skip, BLOCKED-with-evidence event-name verification, kernel-2 lint learns it) + items 6–11 (F27 harness w/ §0.1.6 hook-source, F13 budget/sources persistence, F9 send-lock+mail-events, F15 PID three-way, F12/F14 token-ceiling, UL1 usage-limit) all present & substantive. Graveyard-adjacency answered correctly per item (harness≠exit-code-capture, drain-event≠DLQ, ceiling≠$-table, park≠auto-adoption) — these ANSWER causes-of-death, not reopen graveyard (HARD RULE respected). UL1 cites SPEC appendix **F31** — confirmed present in SPEC (L294, L364) — and the intent doc, which exists. Item numbering consistent with providers' "item 10" references. Invariants 2/6/7 cited correctly. Clean.

**phase-4-webui.md** — B2 reader-handle discipline, READ-ONLY decision (zero write endpoints, mutating POST→405, attach=copy-command), CSRF/same-origin hard-precondition, ThreadingHTTPServer + 127.0.0.1 bind + Host-header rebinding check all present & substantive. READ-ONLY + CSRF-precondition is coherent as a *design* (lens 4 mostly PASS). Invariants 1/6/9 cited correctly. *Only defect: the dangling "CSRF protection verified" done-criterion (finding 3).*

---
RESULT: files=docs/reviews/c1-review-spec-stubs.md tests=n/a-review spec=no-drift(review-only) criteria=6 findings (0 HIGH / 3 MEDIUM [portability F20 mis-cite, watchtower F8 respawn-loop-cap vs F20-cut, webui CSRF done-criterion vs read-only] / 3 LOW [telegram OQ1/B5 command hygiene]); cross-stub consistency=PASS (env-script, token-ceiling, needs-input, invariant-numbering, F31/UL1 all agree — sole cross-doc slip is portability's F20 citation); relitigation avoided (no F1–F16 / §6.1-refuted / graveyard reopened; graveyard-answering injections correctly treated as sound)
