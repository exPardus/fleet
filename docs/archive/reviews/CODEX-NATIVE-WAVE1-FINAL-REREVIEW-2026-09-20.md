# Native Codex wave-one final rereview — GREEN

Reviewed exact head `5ef765d6e280a2b37b5b81f9009e0351b42a85ca` against RED
`c978793616e80a7f8d822613147be6214b7ff92b`. No blocker remains in the
rebriefed scope.

Pre-provider rollback now matches the original claim, restores its brief/task
while `fleet.lock` excludes a same-name successor, then removes the row. Client
and host reject nonfinite/nonpositive operation timeouts; the host caps nested
RPC waits by both the authenticated envelope and its remaining deadline. Stored
role-pressure traces are graded by actor/effect and required order, reject the
baseline side effects and changed Interface worktree, and pin dispatch to the
Supervisor. Fleet and Codex skills give workers end-to-end autonomy for bounded
work while keeping descendant/reviewer dispatch supervisor-owned. Claude and
legacy mcx routes are preserved.

Python 3.10 and 3.12 each: focused native/mcx/skill **274 passed, 2 skipped**;
source/docs gate **186 passed**; Claude CLI/core **194 passed, 2 skipped**.
`git diff --check` and `compileall` passed. No live provider, bridge, enablement,
or external operation ran.

Remaining acceptance gaps: pressure evidence is deterministic stored action
traces, not a live model execution; the live protocol/lifecycle matrix,
supervisor/handoff/interface support, Windows transport, migration, and default
enablement remain unbuilt or unproved. Native remains an opt-in partial worker
spawn with unsupported verbs refusing and mcx the default.
