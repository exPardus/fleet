# Campaign 1 — binding adjudicated fix list (Wave 1D)

Manager adjudication of `c1-review-spec-core.md` (3 major) + `c1-review-spec-stubs.md` (3 MED, 3 LOW), 2026-07-08. **This is the ONE binding list** (PLAN §0.3f) — fixes anchored to section/function roles, routed to the ORIGINAL builder via `fleet send`. Manager verified the two code-ground-truth claims against `bin/fleet.py` before ordering (CM1: `LAUNCH_CLAIM_MAX_AGE_SECONDS=600` @599 + demotion @677 — CONFIRMED; CM2: no `Get-CimInstance`/`alive-unknown`/`elevation` in code — CONFIRMED).

## Adjudication rulings (scope)
- **The unifying remedy for CM2+CM3:** the v2.1 amendment correctly describes the intended end-state (that is what a spec does), but it MUST distinguish **descriptive** amendments (already enforced+tested in shipped `fleet.py`) from **prescriptive** ones (the fix is a C2/later kernel, not yet built). Not "delete the prescriptive text" — **label it**. Every prescriptive amendment gets an explicit `[UNBUILT — owned by <kernel/campaign>]` marker (the honest future-tense F25/§7 "will gain a THIRD entry" and F31/UL1 are the MODEL), and §12's regression list is split into "pins already-shipped behavior (passes today)" vs "pins unbuilt fixes (fails until the kernel lands)."
- **CM1 is a genuine content error** (not mere labeling): the spec now contradicts code in the *wrong direction* and must be corrected.
- **Out of scope / not defects (do not action):** "M5 unfixed in code," "B2 unfixed in code," "M4 unfixed in code" as *code* complaints — correct; those code fixes are C2 kernels (`harden-fleet-b`/`-d`, item 9). The remedy here is spec LABELING, never touching fleet.py (C1 is doc-only). No F1–F16/§6.1-refuted/graveyard reopened by either reviewer (both respected the hard rule).

---
## SPEC.md fixes → owner `spec-amend-1-state` (single coherent pass; it set the v2.1 header + owns §4/§11/§12; edits sibling appendix entries under the SPEC.md single-writer rule)

**FIX-1 (CM1, major) — correct the §4 pre-claim rule to match shipped code.**
- §4 pre-claim bullet + appendix F17 pre-claim face + §11 reboot row currently say working+turn_pid=None is "NEVER demoted" and "cleared by respawn, not recompute." Shipped `recompute_status` (@677) demotes a pre-claim to `dead` once `last_activity` is older than `LAUNCH_CLAIM_MAX_AGE_SECONDS` (600s), making it recoverable via any dead-path lever.
- Correct to: a **fresh** pre-claim (real launchers stamp the pid within seconds) is not demoted by recompute; a **stale** pre-claim (older than the launch-claim max-age) demotes to `dead` and is recoverable via `kill`/`attach --force`/`respawn`/`clean` — not only `respawn`. Update the §11 reboot row identically (a reboot outage > max-age demotes the pre-claim, not "stays working until respawn").
- Reword §12 regression `preclaim_never_demoted` → "a FRESH pre-claim survives recompute; a STALE (> max-age) pre-claim demotes to dead" — matching the regression the code actually pins.

**FIX-2 (CM2, major) — mark the three-way PID probe as UNBUILT.**
- §4 "PID liveness — three-way probe", appendix F20, and §12 `probe_three_way` describe the alive-unknown tier / retry-before-dead / Get-CimInstance fallback / doctor elevation check as if shipped. Shipped probe is a two-way boolean (confirmed: no such code).
- Tag these `[UNBUILT — owned by C2 hardening kernel item 9 (F15/M4)]`; move `probe_three_way` from the "passes today" set into the "pins unbuilt fixes" bucket (see FIX-3).

**FIX-3 (CM3, major) — correct the descriptive/prescriptive framing globally.**
- The v2.1 header line ("Body brings the spec TO the shipped code; no code change proposed") is false: F20, F22, F23, F24, F31 prescribe unbuilt behavior. Reword the header to state the body folds BOTH already-shipped drift AND not-yet-built fixes (each labeled).
- Add `[UNBUILT — owned by <kernel>]` to each prescriptive appendix amendment: **F20** (probe → C2 item 9), **F22** (budget/setting_sources persistence → C2 `harden-fleet-b`/item 7), **F23** (proxy-budget caveat is descriptive-of-a-gap — keep, but note the enforcing token-ceiling is C2 item 10), **F24** (rotation retry/`_unrotate` → note: `_unrotate` EXISTS and is used in respawn launch-failure rollback per the core reviewer's clean-list, but the rotation-retry-on-PermissionError wrapper is UNBUILT → C2 `harden-fleet-d` B2 half). **F31/UL1** already honestly future-tensed — leave as the model.
- Split §12 "required regressions" into two labeled subsections: **(a) pins already-shipped behavior (correct code passes today)** — F17-core sticky-dead/attached/classification, F18 guard ladder, F21 cost model, F25 hook-boundary (as a spec rule), F29 launch-sequence, F30 quarantine; **(b) pins unbuilt fixes (fail until the C2 kernel lands)** — corrected `preclaim_never_demoted` stale-branch, `probe_three_way`, budget-persistence, rotation-retry, UL1 limited/resume. Bucket (b) items each name their owning kernel.

*(spec-amend-1-state stays live at 1 turn — send the fix, no respawn. It edits F21/F22/F24 [link-2 content] and §12 [link-3 content] under the SPEC.md single-writer rule; links 2 & 3 stay idle during this pass — no concurrent SPEC.md writer.)*

---
## Stub fixes → original builders (parallel; disjoint files)

**FIX-4 (SM1, MED) → `stub-inject-portability`.** In the F15/M4 carry block, "(as amended by F20)" is a wrong finding-number cite (F20 = auto-respawn CUT, amends nothing in §4). Change to "(as amended by the v2.1 state-machine amendment, F15/B1 — `spec-amend-1-state`)". Content is correct; only the number is wrong.

**FIX-5 (SM2, MED) → `stub-inject-watchtower`.** F8 asserts a live "respawn-loop cap" and respawn-as-current-action, contradicting the F20 notify-only-v1 cut (F8 imported M9's pre-cut text verbatim). Scope F8's respawn/respawn-loop-cap clauses to the post-Phase-3 re-vet (when the action path returns); v1 keeps the read + send/notify discipline only. Annotate: "respawn action path + its loop cap are DEFERRED with F20; the lock-discipline below applies when auto-respawn is re-vetted."

**FIX-6 (SM3, MED) → `stub-inject-kernels-webui`** (owns phase-4-webui.md). Done-criterion "CSRF protection verified" contradicts the READ-ONLY / CSRF-deferred decision (no mutating path exists in v1 to verify). Replace with: "Host-header/same-origin rejection + mutating-POST→405 verified; CSRF-token machinery specced (not built until write forms are earned)."

**FIX-7 (SL1+SL2+SL3, LOW, batched) → `stub-inject-telegram`.**
- SL1: OQ1 re-lists "(kill?)" as an open confirmation question that B5 already FIXED. Reword OQ1 to "grammar refinement within the B5-fixed confirm baseline (kill/respawn/transitive-bypass already confirm-fixed; open: where /send, /spawn, /mute land)."
- SL2: B5 command-set omits `/manager` (the highest-privilege command). Add `/manager` to the split with disposition (start = no-confirm; its actions that mint bypass workers hit the transitive-privilege confirm).
- SL3: OQ1 grammar and the B5 command inventory diverge. State the full v1 command inventory ONCE in B5 (the fixed baseline) and have OQ1 reference it.

---
## Not sent as fixes (accepted as-is)
- UL1/F31 "limit-shaped stderr undefined pattern" — non-blocking; explicitly deferred to the C2 kernel probe (the kernel MUST pin the pattern). Recorded, not a C1 fix.
- telegram needs-input "F20-style disposition" phrasing — defensible analogical use; optional editorial (folded into FIX-7 if the builder wishes, not required).

## After this wave
1. SPEC.md fix (FIX-1..3) lands → THEN new link `spec-amend-5-subagents` (UL2, sequential SPEC.md) → THEN re-review `c1-review-spec-2` covers fixes + UL2.
2. Stub fixes (FIX-4..7) run parallel to the SPEC.md work.
3. Re-review loop ≤3 (each loop introduces ~1 new issue — re-review after every wave).
