# Wave 64 ritual audit — code owns deterministic work
DONE means: every step in the three named rituals and the discovered ritual surfaces below has a CODE or MODEL row, every CODE row names an executor, every proposed executor specifies inputs and a one-line output, every MODEL row justifies judgement, and every turn-cost cell is measured or explicitly UNMEASURED.

**Model:** gpt-6-astra, high. **Snapshot:** `ef35df139f41ddaa32d72211096b7ea32a07596c`, branch `w64/rituals-audit`, 2026-09-10. **Classification:** CODE by default; only content judgement is MODEL. Proposed names describe future work, not commands to run now.

The first batch is **complete as a named starting batch, incomplete as an inventory of automatable rituals**. All five requests are placed below: `fleet wave-close`, `fleet sup-boot` extensions, `fleet sup-guard`, `fleet interface-register`, and `fleet journal-roll` inside wave-close/checkpoint. Additional gaps include nonce transport, event-driven beats, brief/dispatch validation, DONE evidence assembly, and handoff orchestration. The second batch is also placed: keeper verdict pages, a timer-generated daily gate docket, lesson skeletons, and handoff bundles. Their rows give interfaces, not implementation designs.

**Ownership:** the reap half is **in flight** in `w64-reap`; I read `/home/altai/proga/fleet/state/tasks/lens/w64-reap.md`. Reuse that lane's shared pass in boot, complete, release and wave-close; do not build a competing reaper. Its criterion/protections are that lane's responsibility, including resolving its landed-idle wording against its live-pid protection. Keeper work is owned by the concurrent w64 waker lane; verdict integration belongs with that lane, not a parallel keeper edit. No code or live state was changed here.

**Table reading:** the first group is the three requested files. The clearly marked **FOUND** group, beginning at [F01](#found), is the additional ritual surface. Both groups remain in **one table**. Repeated instructions in distinct source files have distinct rows; substeps with different classification are split. Repeated command names represent components of one executor, not competing designs. Line locators refer to the snapshot. `shipped` means the named primitive exists; `partial` means the primitive exists but orchestration/enforcement is missing; the explicitly requested first/second batch additions are marked `proposed` even where they extend shipped primitives. MODEL rows use `partial` to mean the present ritual relies on human/model judgement, not that a model CLI is shipped. The requested seven-column order is retained, including its penultimate MEASURED column.

**Measurement scope and limits.** “Last three generations” means the last three distinct incarnation IDs recorded chronologically across `supervisor/journal-history/2026-07-to-09.md` then `supervisor/JOURNAL.md`, including the current partial generation: `inc-20260910T041513Z-181d`, `inc-20260910T075355Z-4f99`, `inc-20260910T153011Z-0a56`. This is a snapshot window, not three completed generations. Exact journal-header counts are respectively: 7 CHECKPOINT + 1 SEIZED + 1 HANDOFF-BEGIN + 1 HANDOFF-COMPLETE; 9 CHECKPOINT; 1 CHECKPOINT + 1 SEIZED. Those are **record counts, not Bash/tool-call counts**. One call may batch several operations, a command may retry without an entry, and interface turns are not captured by supervisor checkpoints. Neither these records nor the searched lane reports establish the actual call count per ritual per generation. Consequently every cost is `UNMEASURED`; none is replaced with zero, an invocation estimate, a row count, or a savings claim.

Evidence keys used in the notes:

- **E1:** history lines 11793–12240 (`181d`); history 12244–12574 plus board 1–290 (`4f99`); board 292–330 (`0a56`). Generation header census reproduced in the work log below.
- **E2:** board 9–31 records three completed floor executions (3.12 once, 3.10 twice), including overlap caused by mistaking an empty output/absent pytest pid for a dead job. This measures executions, not the number of Bash calls or polling turns. Board 184–186 records another contaminated live-home run.
- **E3:** board 138–139, 188–190, 240–242, 289–290 contains differently scoped wave-62/63 and wave-63 THROUGHPUT statements; only the 708fa45..fb2a863 segment states its range in the line. Board 246 explicitly records delivery to work:fleet. These are neither compatible totals nor per-step call telemetry.
- **E4:** board 303–321 records the inbox move, campaign append, Claude freeze (all lanes mcx Astra; no resume-limited), and autoclean archived 0/skipped 30. History 12342 records archived 9/skipped 16. Counts of rows reaped are not turn costs.
- **E5:** board contains **6 CHECKPOINT headers**, not the mandated 3. `bin/fleet.py:17746` appends without rolling; the one-time split did not ship continuing enforcement.
- **E6:** `docs/lanes/w63-keeperpane.md` records mocked tmux argv assertions; `w62-keeperc.md` and `w61-keeperblind.md` record roster/incident timings. These are useful behaviour evidence, not supervisor-generation tool-call telemetry. Searching all 60 lane Markdown files for Bash/tool-call/turn-count language found no complete attributable per-step call ledger.

For proposed automation, `home` always means the explicitly selected fleet home; registry/roster joins retain unknown/error states. Proposed mutators retain claim, nonce, ownership and authorization checks. Missing evidence yields UNKNOWN/UNMEASURED or a refusal, never a plausible integer. Code may enforce a recorded decision; it must not invent an operator ruling.

| ritual file | step (verbatim or tight paraphrase) | CODE / MODEL | verb or script | exists? (shipped / proposed / partial) | MEASURED turn cost today | note |
|---|---|---|---|---|---|---|
| `skills/fleet/supervisor.md:10–19` | **S01** Assemble only last-three board, GOALS, named standing directives and current tasks; carry directive names forward. | CODE | `fleet sup-boot` extension: home + board/task paths → `BOOT-INPUTS <manifest>`. | proposed | UNMEASURED | First batch; generated extra sections remain opt-in. |
| `skills/fleet/supervisor.md:10–19` | **S02** Measure actual pre-first-dispatch read tokens once after roll; enforce 40k read cap. | CODE | `fleet boot-budget`: incarnation + usage evidence + read manifest → `BOOT-READ tokens=<n or UNMEASURED> files=<manifest> verdict=<state>`. | proposed | UNMEASURED | Usage evidence required; bytes/tokenizer estimates cannot substitute. |
| `skills/fleet/supervisor.md:16–24` | **S03** Choose extra material only when reconciliation needs its content. | MODEL | — | partial | UNMEASURED | Judgement identifies which unresolved question needs an additional source. |
| `skills/fleet/supervisor.md:21–28;89–103` | **S04** Redirect boot bundle, extract verdict/incarnation/nonce, expose permitted bounded slices, then delete plaintext bundle. | CODE | `fleet sup-run boot`: caller + explicit home + authorized boot args → `BOOT <verdict> <inc> inputs=<safe manifest>`. | proposed | UNMEASURED | Shipped rendered shell sequence; proposed executor must preserve caller-bound generation continuity and secret cleanup, not publish nonce in durable logs. |
| `skills/fleet/supervisor.md:29–43` | **S05** Branch on claim/seize/resume/limit-transfer, refusal, freeze, continuity refusal, or claim-pending successor. | CODE | `fleet sup-boot` + proposed `fleet sup-run`: exit/verdict/claim-pending → `CONTINUE / WAIT-TRANSFER / STOP / PAGE <reason>`. | partial | UNMEASURED | The decision tree is specified; identifying ambiguity and paging it is CODE. |
| `skills/fleet/supervisor.md:39–43` | **S06** Resolve the meaning of an ambiguous claim or requested override after paging. | MODEL | — | partial | UNMEASURED | The operator must judge competing evidence or authorize recovery; an unreadable claim is not evidence of death. |
| `skills/fleet/supervisor.md:44–59` | **S07** Keep the latest NONCE; unchanged preserves it; present it only to verbs accepting the flag. | CODE | `fleet sup-run`: bound caller generation + parser capabilities + requested verb → `VERB <name> rc=<n> generation=<rotated/unchanged>`. | proposed | UNMEASURED | Do not bypass §7 or expose a reusable global nonce store. Parser, not the stale prose list, determines flags. |
| `skills/fleet/supervisor.md:60–62` | **S08** Reconcile worker outcome and silent-limit discriminator from boot status. | CODE | `fleet status` / boot status section. | shipped | UNMEASURED | Status primitive exists; unknown inputs remain explicit. |
| `skills/fleet/supervisor.md:60–62` | **S09** Decide what an anomalous worker needs next after reconciliation. | MODEL | — | partial | UNMEASURED | Judgement on task progress distinguishes a useful continuation, revised scope, and a content failure. |
| `skills/fleet/supervisor.md:63–81` | **S10** Compare FLEET_WORKER witness with registry sid-union identity; record supervisor/worker/absent variant. | CODE | `fleet sup-boot` extension: env witness + registry sid union → `IDENTITY <resolved identity> witness=<variant>`. | proposed | UNMEASURED | Witness never overrides registry identity; absent is benign. |
| `skills/fleet/supervisor.md:85–89;105–110` | **S11** Dispatch gen-0 with fleet-home cwd, launch-id name and tier defaults; resolve incarnation/address through claim. | CODE | `fleet sup-spawn --task @<campaign>`; `fleet sup-status`; `fleet send supervisor @<file>`. | shipped | UNMEASURED | Choosing campaign content is S29; launch id is not incarnation. |
| `skills/fleet/supervisor.md:110–119` | **S12** Execute holder kill/respawn release handshake, bounded wait and documented fallthrough/abort. | CODE | `fleet kill supervisor`; `fleet respawn supervisor`; `fleet interrupt supervisor`. | shipped | UNMEASURED | Authorized lifecycle mechanics; kill freezes on failed release, respawn aborts. |
| `skills/fleet/supervisor.md:117–119` | **S13** Choose ordinary release/spawn, context reset, interruption, or termination for the task. | MODEL | — | partial | UNMEASURED | Judgement concerns desired work continuity; destructive authorization remains separate. |
| `skills/fleet/supervisor.md:123–128` | **S14** Run status on each beat. | CODE | `fleet status`, called by proposed `fleet sup-beat` (home + activity event → `BEAT outcomes=<summary>`). | partial | UNMEASURED | Event-driven/long beat scheduling removes manual polling. |
| `skills/fleet/supervisor.md:125–177` | **S15** Sweep on beat; do not duplicate autoclean with bare archive; report tier-gate recurrence. | CODE | `fleet autoclean`, integrated through w64-reap shared lifecycle pass; optional `fleet archive --dry-run --nonce <gen>`. | partial | UNMEASURED | E4; old TTL sweep is shipped but inadequate. New operator instruction supersedes the hand-run duty; no second reaper proposed. |
| `skills/fleet/supervisor.md:127;178–184` | **S16** Resume a limited worker only after its horizon, when current policy permits. | CODE | `fleet resume-limited` behind proposed `fleet sup-beat` policy check: roster + horizon + ruling → `RESUMED <names> / PARKED <reason>`. | partial | UNMEASURED | E4 currently forbids Claude lane resumes. Do not blindly automate the old beat. |
| `skills/fleet/supervisor.md:181–184` | **S17** Perform chosen recovery for dead-suspected/stuck native workers. | CODE | `fleet respawn --force <name>` with recorded task and current nonce. | shipped | UNMEASURED | Choosing recovery is S09/S13; no mass-respawn from ambiguous roster. |
| `skills/fleet/supervisor.md:188–198` | **S18** Roll all but three checkpoints losslessly to history at checkpoint and wave-close; preserve text/order/ownership. | CODE | `fleet journal-roll`: claim-bound journal + month → `JOURNAL live=<n> moved=<n> sha256=<preservation proof>`. | proposed | UNMEASURED | First batch, called inside wave-close and sup-checkpoint; E5. No summary/rewrite of old entries; inherited oversized entries remain intact. |
| `skills/fleet/supervisor.md:189;200–206` | **S19** Write meaningful state/decision/lesson checkpoint or proposed GOALS change. | MODEL | — | partial | UNMEASURED | Judgement determines the successor-relevant content and whether a work unit merits a checkpoint. |
| `skills/fleet/supervisor.md:188–206;455–456` | **S20** Validate new checkpoint ≤40 lines, append as claim holder, refresh heartbeat; PROPOSAL never edits GOALS. | CODE | `fleet sup-checkpoint @<file> --kind <kind>` + proposed size check: body + claim → `CHECKPOINT <kind> lines=<n> heartbeat=<time>`. | partial | UNMEASURED | E1 has 17 checkpoint records; no call count inferred. Journal roll is S18. |
| `skills/fleet/supervisor.md:202–204` | **S21** Keep heartbeat younger than 3600s during genuine ongoing work without checkpoint events. | CODE | `fleet sup-heartbeat` through proposed `fleet sup-beat`: recent caller activity + claim → `HEARTBEAT <time> / INACTIVE`. | partial | UNMEASURED | Do not let an unattended timer forge liveness for a stalled model. |
| `skills/fleet/supervisor.md:210–223` | **S22** Route models by current recorded policy; include model/header, setting sources and per-type token ceilings. | CODE | `fleet dispatch-check`: brief + allowed paths + policy + roster → `DISPATCH-CHECK PASS / BLOCK <reason>`. | proposed | UNMEASURED | Latest E4 freeze overrides Opus/Sonnet lane examples; supplied budgets 3000000/800000/300000 are not new CLI defaults. |
| `skills/fleet/supervisor.md:220–223` | **S23** Justify a higher token ceiling where the task needs it. | MODEL | — | partial | UNMEASURED | Judgement assesses necessary scope and resources; code validates the recorded ceiling/reason. |
| `skills/fleet/supervisor.md:228–230` | **S24** Generate gate docket from ratification record; deliver once at daily 09:00 Almaty or urgent item-1 blockage. | CODE | `fleet gate-docket`: OPERATOR-GATES + dated delivery state + priority/block flags → `DOCKET <date> open=<ids> delivered=<state>`. | proposed | UNMEASURED | Second batch; timer owns 04:00Z schedule and dedup. Priority-block judgement is S25; interface is not the daily alarm clock. |
| `skills/fleet/supervisor.md:228–235` | **S25** Interpret gate meaning, whether ratified text must change, item-1 blockage and irreversible action outside existing ruling. | MODEL | — | partial | UNMEASURED | Content judgement maps work to ratified authority and identifies an operator decision; mechanical extraction cannot settle that meaning. |
| `skills/fleet/supervisor.md:231–235` | **S26** Raise/park only for recorded ratified-text change; otherwise journal DECIDED and continue. | CODE | `fleet sup-decision --raise`; `fleet sup-checkpoint` via proposed `fleet decision-record`: approved classification + text → `PARKED <id> / DECIDED <id>`. | partial | UNMEASURED | Meaning is S25; ordinary-decision shortcut never supplies destructive authorization. |
| `skills/fleet/supervisor.md:236` | **S27** Identify and implement content-bearing prerequisites to already-ruled work. | MODEL | — | partial | UNMEASURED | Judgement discovers dependencies and the correct fix; identifying a prerequisite does not automatically create a gate. |
| `skills/fleet/supervisor.md:236` | **S28** Record completed prerequisite in one line. | CODE | `fleet lesson-fold --skeleton`: prerequisite artifact → `PREREQUISITE <path> <status>`. | proposed | UNMEASURED | Second-batch skeleton; authored explanation is supplied by S27. |
| `skills/fleet/supervisor.md:240–244` | **S29** Choose the one operator priority to advance, split useful tasks, and decide whether an audit question blocks build. | MODEL | — | partial | UNMEASURED | Judgement establishes feature scope, useful decomposition and relevance; operator request/build-blocking eligibility is then recorded. |
| `skills/fleet/supervisor.md:240–244` | **S30** Enforce one item, disjoint files, lane/model ceilings, no concurrent backlog, and audit authorization exception. | CODE | `fleet dispatch-check`: wave plan + write sets + roster + rulings → `DISPATCH-CHECK PASS / BLOCK <reason>`. | proposed | UNMEASURED | Include the new combined three-live-lane ceiling and memory check; classification of task intent is S29. |
| `skills/fleet/supervisor.md:244;447–449` | **S31** Keep work dispatched or wait armed; avoid idle polling and use long beats. | CODE | `fleet wait <names> --any` via proposed `fleet sup-beat`: active roster + wake registration → `WAIT-ARMED <names> / READY <events>`. | partial | UNMEASURED | Waiting/execution is mechanical; deciding what new work is useful is S29. |
| `skills/fleet/supervisor.md:246–248` | **S32** Author observable DONE criterion and the task content. | MODEL | — | partial | UNMEASURED | Judgement defines a useful externally observable result rather than a vacuous checkbox. |
| `skills/fleet/supervisor.md:246–248` | **S33** Render title then DONE, model header; run DONE pin against dispatch-home tasks before spawn. | CODE | `fleet brief-render`: approved task fields + template → `BRIEF <path>`; `tests/test_docs_currency.py::test_dispatched_tasks_have_done`. | partial | UNMEASURED | The pin exists; an automatic dispatch barrier is proposed. Exclude generated supervisor boot tasks per shipped pin. |
| `skills/fleet/supervisor.md:249–251` | **S34** Select/run targeted lane checks only. | CODE | `fleet lane-check`: touched paths + declared check map + interpreter → `CHECKS <paths> rc=<n> receipt=<path>`. | proposed | UNMEASURED | Test design is content work; deterministic execution must not trigger full floor per lane. |
| `skills/fleet/supervisor.md:249–251` | **S35** Run full floor once per interpreter on merged tree in fresh git clone --no-local. | CODE | `fleet wave-close --base <sha> --changelog @<sentences>`: derive merged tree/interpreters → `FLOOR tree=<sha> py=<version> result=<counts> receipt=<path>`. | proposed | UNMEASURED | First batch; E2. Cache success by exact tree/interpreter/check environment; explicit job completion, no pgrep-based guesses; no push before validated result. |
| `skills/fleet/supervisor.md:249–258` | **S36** Review unexpected test failures and code/document diff for real correctness. | MODEL | — | partial | UNMEASURED | Judgement distinguishes a defect, a host assumption and inadequate coverage; a known failure count alone does not establish equivalence. |
| `skills/fleet/supervisor.md:252` | **S37** Distill a general lesson and concrete process change into one line. | MODEL | — | partial | UNMEASURED | Judgement identifies a causal lesson that transfers beyond this incident. |
| `skills/fleet/supervisor.md:252–258` | **S38** Refresh derivable NEXT-SESSION/progress rows, remove stale board sections, enforce ≤30 lines, refresh SPEC §0 counts. | CODE | `fleet wave-close`: base + accepted lane records + current board + repository → `PROGRESS <paths> board-lines=<n> counts=<n>`. | proposed | UNMEASURED | First batch derivable half; priority/content selection is S29/S39. Do not overwrite live append-only changes from snapshots. |
| `skills/fleet/supervisor.md:252–258` | **S39** Write current priority descriptions, owning SPEC behaviour changes and host-quirk explanation. | MODEL | — | partial | UNMEASURED | Judgement describes actual behaviour and which knowledge matters; filenames and line counts cannot prove docs are semantically current. |
| `skills/fleet/supervisor.md:254–258` | **S40** Require docs updated line, matching SPEC/progress edits or Docs: n/a reason; include docs check in boot bundle. | CODE | `fleet sup-boot` docs-currency extension: recent diffs + reports + trailers → `DOCS-CURRENCY PASS / FAIL <paths>`. | proposed | UNMEASURED | First batch. Existing tests/test_docs_currency.py is only a bin/*.py/docs-presence proxy; semantic truth remains S39. |
| `skills/fleet/supervisor.md:259–260` | **S41** Supply one CHANGELOG sentence per user-visible landing. | MODEL | — | partial | UNMEASURED | Judgement names the user-visible change in language the operator can assess. |
| `skills/fleet/supervisor.md:259–260` | **S42** Prepend supplied landing sentences, newest first, to CHANGELOG. | CODE | `fleet wave-close --base <sha> --changelog @<sentences>` → `CHANGELOG prepended=<n> wave=<id>`. | proposed | UNMEASURED | First batch. Supervisor supplies only base sha and CHANGELOG sentences; other data comes from recorded lane/wave artifacts. |
| `skills/fleet/supervisor.md:261–266` | **S43** Collect each task’s exact DONE line and evidence; quote it with MEASURED/NOT MET. | CODE | `fleet done-evidence`: briefs + reports + check receipts + git → `DONE <task> <MEASURED/NOT-MET/UNKNOWN> evidence=<paths>`. | proposed | UNMEASURED | Automatic executable criteria only; semantic acceptance supplied by S44. Unknown is never promoted to met. |
| `skills/fleet/supervisor.md:261` | **S44** Review content-bearing DONE criteria and accept/reject the diff. | MODEL | — | partial | UNMEASURED | Judgement is needed when success depends on report coverage, design quality or feature semantics; a worker’s assertion is insufficient. |
| `skills/fleet/supervisor.md:261–266` | **S45** Close only when all accepted DONE criteria are true. | CODE | `fleet wave-close`: accepted DONE ledger + check results → `CLOSE READY / BLOCK <task>`. | proposed | UNMEASURED | First batch; criterion acceptance is S44, deterministic barrier here. |
| `skills/fleet/supervisor.md:262–266` | **S46** Compute THROUGHPUT from git diff --numstat base..HEAD, buckets, registry worker counts, roster Claude tokens. | CODE | `fleet wave-close --base <sha> --changelog @<sentences>` → `THROUGHPUT wave <id>: bin +A/-B, tests +C, docs +D, journal +E; workers: <counts>; tokens: <sum/UNMEASURED>; operator items advanced: <ids>`. | proposed | UNMEASURED | First batch; E3. Use one base/tip/range, non-overlapping bin/tests/docs/journal buckets including history. Record deletions and binary/unclassified paths in receipt; dedup lane identities/sid unions. No zero for unavailable accounting; registry token wording in old ritual is superseded by requested roster source. |
| `skills/fleet/supervisor.md:262–266` | **S47** Prepend/checkpoint computed THROUGHPUT in JOURNAL and relay identical line through sup-notify. | CODE | `fleet wave-close`: persisted accounting receipt + claim → `WAVE-RECEIPT <id> journal=<state> notify=<state>`. | proposed | UNMEASURED | First batch. Use claim-holder journal writer and same computed bytes; do not hand-type a second total. |
| `skills/fleet/supervisor.md:265–266` | **S48** Detect two consecutive waves with no item advanced; announce and constrain next wave to item 1. | CODE | `fleet wave-close`: accepted advancement ledger for two waves → `NEXT-WAVE item=1 / unrestricted-by-this-rule`. | proposed | UNMEASURED | Whether a feature advances an item is S29/S44; evaluating two recorded empty lists is CODE. |
| `skills/fleet/supervisor.md:270–294` | **S49** Measure tier occupancy; trigger at supervisor 350k, enforce 400k ceiling and worker 250–300k band. | CODE | `fleet sup-context`; shipped dispatch ceiling checks; proposed `fleet sup-beat` trigger (usage + wave state → `HANDOFF-DUE / BELOW-BAND`). | partial | UNMEASURED | At ceiling allow read reconciliation and handoff; bare recovery respawn differs from respawn --task. |
| `skills/fleet/supervisor.md:276–279;308;337` | **S50** Choose clean task boundary and successor queue/state summary. | MODEL | — | partial | UNMEASURED | Judgement identifies what is safe to finish and what the next generation should prioritize. |
| `skills/fleet/supervisor.md:306–340` | **S51** Assemble handoff bundle from approved summary, queue, running artifacts and claim; checkpoint first. | CODE | `fleet handoff-bundle`: claim + approved queue + board + registry + artifact paths → `HANDOFF-BUNDLE <path> inc=<id>`. | proposed | UNMEASURED | Second batch. Include still-running gate path/status, never transcribed provisional verdicts; no secret token in public summary. |
| `skills/fleet/supervisor.md:309–326` | **S52** Notify interface with one sanitized SUPERVISOR line before handoff/release. | CODE | `fleet sup-notify <message>`. | shipped | UNMEASURED | Shipped, not provisional. Mechanical template uses incarnation and token file path, never token value. |
| `skills/fleet/supervisor.md:327–340` | **S53** Begin handoff, dispatch successor itself, record successor incarnation/sid and token file. | CODE | `fleet sup-handoff-begin --nonce <gen>`. | shipped | UNMEASURED | Interface must not spawn another successor. |
| `skills/fleet/supervisor.md:341–348` | **S54** Wait up to 300s for HANDSHAKE; complete with expected incarnation and current nonce; exit on success. | CODE | `fleet handoff-run`: claim + begun attempt + deadline → `HANDOFF COMPLETE <inc> / TIMEOUT <attempt>`; invokes `sup-handoff-complete`. | partial | UNMEASURED | Begin/complete exist; timeout/poll/exit choreography is still prompt work. Optional sid mismatch only warns; token verifies transfer. |
| `skills/fleet/supervisor.md:349–372` | **S55** Abort failed/timed-out attempt by sid, or stale sid-less incarnation; unlink plaintext token task. | CODE | `fleet sup-handoff-abort --successor-sid <sid>` or `--successor-inc <inc>` with current nonce. | shipped | UNMEASURED | Record abort; never invent a session to stop; sid-less join window is 300s. |
| `skills/fleet/supervisor.md:374–386` | **S56** Supersede previous attempt; refuse superseded boot; to retry an earlier attempt abort and begin again. | CODE | `fleet sup-handoff-begin`, `sup-boot --handoff-inc`, `sup-handoff-abort`. | shipped | UNMEASURED | Mechanical protocol exists though cited A3 remains UNRATIFIED; code executor cannot ratify it. |
| `skills/fleet/supervisor.md:388–394` | **S57** Retire all eligible sid-less pending attempts; expose sid-bearing recipes and unresolved-token doctor failure. | CODE | `fleet sup-handoff-abort --retire-all [--force] --nonce <gen>`; `fleet doctor`. | shipped | UNMEASURED | Use force only under existing authorization; preserve refusal on unrecorded handle. |
| `skills/fleet/supervisor.md:353–356` | **S58** Surface aborted-handoff marker until operator clears it. | CODE | `fleet doctor`; proposed `fleet operator-action`: explicit clearance + marker path → `CLEARED <marker> / REFUSED`. | partial | UNMEASURED | Choosing/authorizing clearance belongs to operator; rendering and carrying out an authorized file action is CODE. |
| `skills/fleet/supervisor.md:396–405` | **S59** Successor boots claim-pending with token, writes HANDSHAKE, polls claim, does no fleet action until transfer. | CODE | `fleet sup-boot --handoff-inc <inc> --handoff-token <token>` plus proposed `fleet handoff-run successor`: own incarnation → `WAIT / CLAIMED <inc>`. | partial | UNMEASURED | Successor preserves its own minted generation; stdout/nonce handling follows S04/S07. |
| `skills/fleet/supervisor.md:402–405` | **S60** On handoff-refused rc5, terminate with HANDOFF-ORPHAN. | CODE | `fleet handoff-run successor`: boot rc/verdict → `HANDOFF-ORPHAN <inc>`. | proposed | UNMEASURED | Deterministic refusal/termination, never compensate by claiming fresh. |
| `skills/fleet/supervisor.md:333–334;349–356;424–427` | **S61** Resolve abort-resume versus stillborn-release instructions against the authorized stop/band context. | MODEL | — | partial | UNMEASURED | Ritual skill says resume duty after abort while server brief says abort then release; policy meaning needs judgement before an executor encodes the branch. |
| `skills/fleet/supervisor.md:407–437` | **S62** On authorized stop or applicable stillborn fallback, release claim, tombstone own record, reap through w64-reap, then exit. | CODE | `fleet sup-release --nonce <gen> --reason <text>`; w64-reap shared lifecycle pass. | partial | UNMEASURED | Release primitive shipped; reap integration in flight. Failed own tombstone returns PAGE, never force release. |
| `skills/fleet/supervisor.md:439–443` | **S63** Classify gone-body stale claim as seize, fresh as freeze; page rather than remove INCARNATION. | CODE | `fleet sup-boot` verdict logic / proposed `fleet sup-guard`: claim + sid union + roster + clock → `DISPATCH / PAGE <reason>`. | partial | UNMEASURED | Manual INCARNATION deletion is an operator-only action; refusal must not route around it. |
| `skills/fleet/supervisor.md:450–454` | **S64** At worker task boundary, respawn worker in its context band with retained brief and journal. | CODE | `fleet respawn <name>` through proposed `fleet sup-beat`: measured occupancy + completed task boundary + policy → `RESPAWNED <name> / PARKED <reason>`. | partial | UNMEASURED | Current Claude freeze prevents resumes/dispatch; the generic rule must be policy-aware. |
| `skills/fleet/supervisor.md:457–472` | **S65** Render durable report path, disposable journal path, report DONE line, and commit/path-list rules in every brief. | CODE | `fleet brief-render`: lane + branch + writable capabilities + template → `BRIEF <path> report=docs/lanes/<name>.md journal=<path>`. | proposed | UNMEASURED | Codex read-only git metadata means supervisor commits path list; do not order impossible worker commits. Gate branch routing audited in FOUND group. |
| `skills/fleet/supervisor.md:473–476` | **S66** Refuse ambiguous/two-live claim; stopping side records death because claude stop has no Stop hook. | CODE | `fleet sup-guard`; shipped `fleet kill` / `fleet interrupt` event writing. | partial | UNMEASURED | Guard proposed; lifecycle recording exists, must cover externally executed authorized stops too. |
| `skills/fleet/supervisor.md:447–449;477–478` | **S67** Interpret new operator rulings and decide intentional dormancy. | MODEL | — | partial | UNMEASURED | Judgement establishes which directive overrides older GOALS policy; only the operator may authorize dormancy. |
| `skills/fleet/supervisor.md:477–478` | **S68** Apply literal SUPERVISOR-DORMANT only following explicit operator action; recognize it in nag checks. | CODE | `fleet operator-action`: authorized GOALS edit + exact token → `DORMANCY <state>`; keeper GOALS detection. | partial | UNMEASURED | Recognition is shipped; agent never grants itself permission to edit GOALS. |
| `skills/fleet/supervisor.md:439–443` | **S69** Operator-only emergency lever: remove INCARNATION by hand after judging the frozen claim. | CODE | Operator shell `rm <explicit-home>/supervisor/INCARNATION`; input is the explicitly selected claim file, output is the shell exit status. | shipped | UNMEASURED | This is a deterministic file operation reserved to the human; S06 supplies the authority/judgement seam. Refused agents must page and must never execute this lever themselves. |
| `supervisor/briefs/server-standing.md:9` | **B01** Boot with redirected bundle and retrieve latest checkpoint plan. | CODE | `fleet sup-boot` plus proposed `fleet sup-run boot`: caller/home → `BOOT <verdict> inputs=<manifest>`. | partial | UNMEASURED | Use S01–S07 bounded cap/nonce machinery; do not read whole journal tail. |
| `supervisor/briefs/server-standing.md:10` | **B02** Run boot autoclean (superseded hand-run instruction). | CODE | `fleet sup-boot` reap extension: registry + roster + landing/mail state → `reaped: <N> rows`. | proposed | UNMEASURED | First batch; **in flight w64-reap**. Reuse its pass, no colliding design. |
| `supervisor/briefs/server-standing.md:11` | **B03** Discover inbox Markdown tasks and assemble drain queue. | CODE | `fleet sup-boot` inbox extension: state/inbox/*.md → `INBOX pending=<ids> content=<bounded manifest>`. | proposed | UNMEASURED | First batch; no task discarded/marked done before campaign mapping exists. |
| `supervisor/briefs/server-standing.md:11` | **B04** Turn each queued operator task into a campaign entry with appropriate priority/scope. | MODEL | — | partial | UNMEASURED | Judgement interprets the operator request; file ordering alone cannot determine the useful campaign plan. |
| `supervisor/briefs/server-standing.md:11` | **B05** Append campaign link, move inbox file to done, checkpoint after recorded campaign acceptance. | CODE | `fleet inbox-drain`: approved task→campaign mapping + inbox files + claim → `INBOX drained=<ids> pending=<ids>`. | proposed | UNMEASURED | First-batch boot extension component; idempotent collision-safe move and checkpoint, E4. |
| `supervisor/briefs/server-standing.md:15` | **B06** Always pass --setting-sources project,local at worker dispatch. | CODE | `fleet dispatch-check` / dispatch renderer: host profile + brief → `DISPATCH argv=<validated args>`. | proposed | UNMEASURED | Existing passthrough is shipped; default/mandatory host policy remains a manual ritual. |
| `supervisor/briefs/server-standing.md:16` | **B07** Use recorded model policy and target-repo cwd. | CODE | `fleet dispatch-check`: newest ruling + task repo/model → `DISPATCH-CHECK PASS / BLOCK <reason>`. | proposed | UNMEASURED | Stale Opus/Sonnet instruction; E4 all lanes mcx Astra supersedes it and S22’s earlier model table. |
| `supervisor/briefs/server-standing.md:17` | **B08** Select lessons, project quirks and one-line knowledge index description. | MODEL | — | partial | UNMEASURED | Judgement extracts causal/generalizable content; lesson file placement is B09. |
| `supervisor/briefs/server-standing.md:17` | **B09** Checkpoint wave and write approved lessons/project/index entries. | CODE | `fleet wave-close`; `fleet lesson-fold --skeleton`: approved lesson + project + source refs → `LESSON <anchor> files=<paths>`. | proposed | UNMEASURED | Second-batch skeleton, not model-free lesson invention. |
| `supervisor/briefs/server-standing.md:17` | **B10** Commit completed wave including docs/journal after checks. | CODE | `fleet wave-close`: base + CHANGELOG sentences + validated exact path set → `COMMIT <sha> / BLOCK <reason>`. | proposed | UNMEASURED | First batch; supervisor acts on live tree. This worker cannot commit; no git metadata touched. |
| `supervisor/briefs/server-standing.md:17` | **B11** Push; on nonzero retry three times over five minutes; checkpoint exhausted failure. | CODE | `fleet wave-close`: committed wave + upstream + persisted retry state → `PUSH <sha> OK / FAILED attempts=<n>`. | proposed | UNMEASURED | First batch. Initial attempt plus at most three retries across five minutes; checkpoint failure, do not silently mark delivered/pushed. Checks and writes use explicit result dependencies. |
| `supervisor/briefs/server-standing.md:18` | **B12** Measure sup-context; begin graceful end at 350k. | CODE | `fleet sup-context` plus proposed `fleet handoff-run`: measured occupancy + wave state → `HANDOFF-DUE / BELOW-BAND`. | partial | UNMEASURED | Measurement/threshold CODE; discretionary boundary is B13. |
| `supervisor/briefs/server-standing.md:18;24` | **B13** Choose earlier clean stop boundary and successor priorities/in-flight summary. | MODEL | — | partial | UNMEASURED | Judgement determines safe stopping point and useful successor instructions. |
| `supervisor/briefs/server-standing.md:24` | **B14** Checkpoint supplied successor queue with latest nonce. | CODE | `fleet sup-checkpoint @<file> --nonce <gen>`. | shipped | UNMEASURED | Queue assembly from artifacts is handoff-bundle S51; authoring is B13. |
| `supervisor/briefs/server-standing.md:25` | **B15** Notify work:fleet with sanitized routine SUPERVISOR line. | CODE | `fleet sup-notify <message>`. | shipped | UNMEASURED | No repeated spelling grep; S52. |
| `supervisor/briefs/server-standing.md:26` | **B16** Begin successor dispatch and capture successor identifiers. | CODE | `fleet sup-handoff-begin --nonce <gen>`. | shipped | UNMEASURED | Exactly the existing successor mechanism, no interface sup-spawn. |
| `supervisor/briefs/server-standing.md:26` | **B17** Successor boots/writes HANDSHAKE; wait 300s, complete on handshake, then exit. | CODE | `fleet handoff-run`: begun attempt + deadline → `HANDOFF COMPLETE <inc> / TIMEOUT <attempt>`; existing boot/complete verbs. | partial | UNMEASURED | Mechanical polling/continuity/exit sequence; S54/S59. |
| `supervisor/briefs/server-standing.md:27` | **B18** On stillbirth abort by sid/inc, release, stop, allow keeper page/interface revival. | CODE | `fleet handoff-run`: selected fallback policy + attempt/claim → `ABORTED <attempt> RELEASED <inc> / PAGE <reason>`. | proposed | UNMEASURED | Uses shipped abort/release; policy discrepancy with S61 is explicit, not silently chosen by code. |
| `supervisor/briefs/server-standing.md:31` | **B19** Determine whether a gate actually requires parking. | MODEL | — | partial | UNMEASURED | Blanket raise-and-park wording is stale; judgement decides whether ratified spec text must change under newer rule S25. |
| `supervisor/briefs/server-standing.md:31` | **B20** Execute recorded gate/decision disposition and route it to interface. | CODE | `fleet sup-decision --raise` or checkpoint DECIDED; `fleet gate-docket`: disposition + source record → `PARKED / DECIDED / DOCKET <id>`. | partial | UNMEASURED | Never park every ordinary decision automatically. |
| `supervisor/briefs/server-standing.md:35–37` | **B21** Freeze on suspicious roster; block mass-respawn/second supervisor; propose GOALS edits rather than editing. | CODE | `fleet dispatch-check` + `fleet sup-guard`; `fleet sup-checkpoint --kind PROPOSAL`: claim + action → `ALLOW / PAGE / PROPOSAL <id>`. | partial | UNMEASURED | Proposal content and ambiguous-policy resolution stay MODEL; enforcement is CODE. |
| `docs/operator/server-interface-profile.md:9–20` | **I01** On launch/resume rename window fleet and write TMUX_PANE registration idempotently. | CODE | `fleet interface-register`: explicit home + TMUX_PANE → `INTERFACE pane=<id> registered=<path>`. | proposed | UNMEASURED | First batch. Validate pane before atomic registration; keeper only reads; do not rename arbitrary operator windows from a timer. |
| `docs/operator/server-interface-profile.md:22` | **I02** Load fleet startup sources: operator gates and knowledge index. | CODE | `fleet interface-boot`: explicit home → `INTERFACE-INPUTS gates=<path> knowledge=<path>`; shipped `fleet knowledge`. | partial | UNMEASURED | Detailed linked startup surface is in FOUND group; keep delivery on authorized fleet interface. |
| `docs/operator/server-interface-profile.md:22` | **I03** Collect status, supervisor state and run startup autoclean with explicit home. | CODE | `fleet status`; `fleet sup-status`; `fleet --fleet-home <home> autoclean`, in proposed `fleet interface-boot` (home → `STARTUP <summary>`). | partial | UNMEASURED | Reap integration must reuse w64-reap; no manually duplicated archive step. |
| `docs/operator/server-interface-profile.md:22` | **I04** Choose relevant project knowledge and interpret open gates. | MODEL | — | partial | UNMEASURED | Judgement selects sources relevant to the operator’s task and understands unresolved decisions. |
| `docs/operator/server-interface-profile.md:23` | **I05** Report startup supervisor/workers/unpushed/hook-errors in one message under 1500 characters. | CODE | `fleet interface-boot`: status + claim + git + hook-error tail → `STARTUP <bounded summary>`. | proposed | UNMEASURED | Selection of mandatory fields and character budget are deterministic; do not turn read failures into zero counts. |
| `docs/operator/server-interface-profile.md:24` | **I06** If GOALS active and no live supervisor, run guard, dispatch standing brief, announce finding and launch. | CODE | `fleet sup-guard --do`: explicit home + GOALS + claim/union/roster → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch. On DISPATCH executes existing sup-spawn with --setting-sources project,local; revalidate before act. Do not silently revive. |
| `docs/operator/server-interface-profile.md:25` | **I07** Wait and route incoming operator/KEEPER/SUPERVISOR lines. | CODE | `fleet interface-route`: source-tagged incoming line + session state → `ROUTE <handler>`. | proposed | UNMEASURED | Deterministic routing; interpreting a new operator request remains I32. |
| `docs/operator/server-interface-profile.md:31` | **I08** Read human/JSON sup-status; classify released/none/held and heartbeat age. | CODE | `fleet sup-guard`: explicit home + projected claim + clock → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch substep; one structured snapshot replaces duplicate human+JSON reads. |
| `docs/operator/server-interface-profile.md:32–39` | **I09** Resolve claim to registry sid UNION and join roster pids; any pid-bearing union sid is live. | CODE | `fleet sup-guard`: claim_sids + registry + roster → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch; union AND pid, not mere listed sid. Unknown union/failed roster never implies death. |
| `docs/operator/server-interface-profile.md:40–46` | **I10** Page for seized/unknown, pending handoff/HANDSHAKE, live releasing body, fresh-but-gone claim, stale idle live body. | CODE | `fleet sup-guard`: claim + roster union + handoff_pending + HANDSHAKE → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch. PAGE on ambiguity is CODE, not a model classification. Idle WAKE requires applicable recorded w64 wake authorization; otherwise PAGE. Busy/live must not dispatch. |
| `docs/operator/server-interface-profile.md:40–46` | **I11** Resolve ambiguous state or choose an operator-authorized recovery outside the recorded guard policy. | MODEL | — | partial | UNMEASURED | Judgement assesses meaning/authority of recovery; a model is not needed to recognize a named ambiguous state. |
| `docs/operator/server-interface-profile.md:46;73;109` | **I12** Never sup-spawn a second body or a handoff successor from the interface. | CODE | `fleet sup-guard --do`: current claim + union + pending handoff → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch barrier; checking only before a separate model-typed spawn leaves a check/action gap. |
| `docs/operator/server-interface-profile.md:52–62` | **I13** On supervisor-stalled, obtain guard verdict instead of inferring body death from page wording. | CODE | `bin/fleet_keeper.py` verdict-page extension: keeper observation + shared guard evaluator → `KEEPER: <DISPATCH / WAKE name / PAGE reason>`. | proposed | UNMEASURED | Second batch; coordinate with w64-waker. Advisory page is not durable dispatch authorization; --do rechecks fresh state. |
| `docs/operator/server-interface-profile.md:52` | **I14** Execute safe stalled-page revival and report launch id plus resulting sup-status. | CODE | `fleet sup-guard --do`: current observation + authorized standing brief → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch. Guard cannot dispatch over idle retired sid; next notification can render launched id/claim facts. |
| `docs/operator/server-interface-profile.md:56–62` | **I15** Explain idle body/heartbeat age; treat retired-sid idle identically; page on union unavailable. | CODE | `fleet sup-guard`: joined roster + heartbeat + union-read result → `PAGE <body,age,reason> / WAKE <name>`. | proposed | UNMEASURED | Mechanical reason rendering. Current profile forbids unasked wake/retire; use new wake ruling only when recorded, not this proposal as authorization. |
| `docs/operator/server-interface-profile.md:57` | **I16** Choose wake versus retirement/relaunch when operator judgement is still required. | MODEL | — | partial | UNMEASURED | Judgement concerns whether preserving this body’s content/work is useful; operator authorization governs mutation. |
| `docs/operator/server-interface-profile.md:65` | **I17** For other keeper pages gather read-only status, sup-status, peek, doctor and git evidence. | CODE | `fleet interface-diagnose`: page rule + home + worker ids → `DIAGNOSIS facts=<receipt> errors=<states>`. | proposed | UNMEASURED | Existing reads shipped; deterministic selection by page type can be scripted. |
| `docs/operator/server-interface-profile.md:65` | **I18** Write concise recommendation from what keeper saw and confirmed evidence. | MODEL | — | partial | UNMEASURED | Judgement diagnoses cause and chooses a useful recommendation beyond the fixed page facts. |
| `docs/operator/server-interface-profile.md:65;104;108–111` | **I19** Require recorded authorization before destructive operations/doctor repair; refuse interface boot, gate ticking and unapproved permission bypass. | CODE | `fleet interface-action`: explicit action + authorization record + role → `ALLOW <action> / REFUSED <reason>`. | proposed | UNMEASURED | Role/provenance guard is not fully shipped. Recognizing literal recorded approvals is CODE; interpreting their scope is I36. |
| `docs/operator/server-interface-profile.md:69–75` | **I20** Acknowledge SUPERVISOR handoff as routine in one topic line. | CODE | `fleet interface-route`: sanitized supervisor handoff event → `HANDOFF starting <inc>; successor booting`. | proposed | UNMEASURED | Fixed routine acknowledgement does not require content judgement. |
| `docs/operator/server-interface-profile.md:76` | **I21** Watch pending successors and claim read-only; completion requires expected incarnation plus pending entry gone. | CODE | `fleet handoff-watch`: expected incarnation + sup-status JSON stream → `HANDOFF WAIT / COMPLETE <inc> / FAILED <reason>`. | proposed | UNMEASURED | Outgoing body owns 300s timeout; interface watcher does not enforce it. |
| `docs/operator/server-interface-profile.md:77` | **I22** Report new incarnation when claim moves, then stop watching. | CODE | `fleet handoff-watch`: verified transfer event → `HANDOFF COMPLETE <inc>`. | proposed | UNMEASURED | Relay fixed fact, no model restatement needed. |
| `docs/operator/server-interface-profile.md:78` | **I23** On stillborn handoff run guard; dispatch only if safe; page released-body wedge. | CODE | `fleet sup-guard --do`: current claim/roster/pending state → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch; aborted attempt alone does not prove absence of all live bodies. |
| `docs/operator/server-interface-profile.md:80` | **I24** Record observed handoff result and host/claim evidence; refresh derivable proof claims. | CODE | `fleet handoff-watch`: before/after claim + host + attempt → `HANDOFF-RECEIPT <path> result=<state>`. | proposed | UNMEASURED | Profile’s eight-stillbirths/no-fixed-default-drill sentence is stale against history 11330 and 12240; update its evidence mechanically. |
| `docs/operator/server-interface-profile.md:80` | **I25** Assess what a successful handoff receipt establishes beyond that exact drill. | MODEL | — | partial | UNMEASURED | Judgement limits generalization: quiet low-context success does not prove ceiling-band success with workers in flight. |
| `docs/operator/server-interface-profile.md:84–86` | **I26** Generate accumulated gates and send one daily 09:00 Almaty docket, or urgent item-1 blocker. | CODE | `fleet gate-docket`: ratification record + priority flags + delivery ledger + clock → `DOCKET <date> open=<ids> delivered=<state>`. | proposed | UNMEASURED | Second batch timer, not interface scheduling. Never mark a gate settled because its docket was delivered. |
| `docs/operator/server-interface-profile.md:87–89` | **I27** Interpret ratified-text change, ordinary decision or irreversible act outside ruling. | MODEL | — | partial | UNMEASURED | Judgement concerns operator intent and authority; no semantic gate decision inferred from a checkbox alone. |
| `docs/operator/server-interface-profile.md:91–96` | **I28** Relay exact THROUGHPUT/tokens and new CHANGELOG lines; load current board/progress. | CODE | `fleet interface-wave`: wave receipt + CHANGELOG delta + current board → `WAVE <id> <verbatim throughput and landing lines>`. | proposed | UNMEASURED | First-batch wave-close sends via sup-notify; this renderer prevents recalculation or stale historical priority reads. |
| `docs/operator/server-interface-profile.md:92–94` | **I29** Offer one next feature with observable DONE and wave estimate. | MODEL | — | partial | UNMEASURED | Judgement chooses the useful next feature and estimates effort; these are not derivable from file counts. |
| `docs/operator/server-interface-profile.md:92–94` | **I30** Format proposal as exactly one feature, DONE, estimate and yes/no/later. | CODE | `fleet interface-proposal`: approved feature + DONE + estimate → `FEATURE <name>; DONE means: <criterion>; waves=<estimate>; yes/no/later`. | proposed | UNMEASURED | No unsolicited extra proposals; feature choice is I29. |
| `docs/operator/server-interface-profile.md:100` | **I31** On explicit revive, run guard then safe spawn and report launch/claim. | CODE | `fleet sup-guard --do`: home + explicit revive event → `DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | First batch; revive does not override ambiguous claim. |
| `docs/operator/server-interface-profile.md:101` | **I32** Interpret incoming task description and scope for the supervisor. | MODEL | — | partial | UNMEASURED | Judgement turns natural-language intent into a useful task without silently inventing requirements. |
| `docs/operator/server-interface-profile.md:101` | **I33** Write dated slug task or inbox file; send @file to live supervisor, otherwise report queued. | CODE | `fleet interface-task`: accepted task text + clock + claim/registry → `TASK <path> SENT <name> / QUEUED`. | proposed | UNMEASURED | Use safe filename/collision handling; live supervisor addressed by claim resolution, not hand-copied launch name. |
| `docs/operator/server-interface-profile.md:102` | **I34** On status, gather fleet and supervisor state and render summary. | CODE | `fleet interface-status`: explicit home + status/sup-status → `STATUS <bounded summary>`. | proposed | UNMEASURED | Both primitives shipped; deterministic summary preserves errors. |
| `docs/operator/server-interface-profile.md:103` | **I35** On operator recycle interface, acknowledge and exit; keeper recreates it next tick. | CODE | `fleet interface-route`: explicit operator recycle event → `INTERFACE recycling`; session exit integration. | proposed | UNMEASURED | Only exact authorized retire action; never context-occupancy recycling. |
| `docs/operator/server-interface-profile.md:104` | **I36** Answer other operator questions and interpret destructive authorization scope. | MODEL | — | partial | UNMEASURED | Judgement connects fleet facts to the question and determines whether existing permission covers the requested act. |
| `docs/operator/server-interface-profile.md:113–128` | **I37** Name explicit fleet home on bare destructive commands; preserve wrong-home refusal. | CODE | `fleet --fleet-home <home> <verb>` via proposed `fleet interface-action`: action + home → `ACTION <verb> rc=<n> home=<path>`. | partial | UNMEASURED | Shipped resolver guard; interface has no registry membership. Do not drop --fleet-home to route around refusal. |
| `skills/fleet/supervisor.md:21–28;249–266` | **A01** Run reap pass as part of wave-close. | CODE | `fleet wave-close`: wave landing state + registry/roster → `reaped: <N> rows`. | proposed | UNMEASURED | First batch; calls w64-reap shared pass. Existing autoclean 0-for-30 is not proof of no eligible bodies. |
| `skills/fleet/supervisor.md:21–28;240–248` | **A02** Print memory check and dispatch capacity in sup-boot: free -m, ≥1.5 GB available, ≤3 live lanes combined. | CODE | `fleet sup-boot` extension: free -m + native/Codex roster → `CAPACITY available=<MB> live_lanes=<n> DISPATCH / BLOCK <reason>`. | proposed | UNMEASURED | First batch; w64-reap prints the rule. Live measurement/enforcement must count one Codex lane, not its node processes, and recheck before dispatch; coordinate, do not duplicate reap. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:11–41` | **F01** <a id="found"></a>**ADDITIONAL RITUAL SURFACE — brief template:** render deliverables, exact paths/fence, model, budget, report/journal, docs, checks and DONE rules. | CODE | `fleet brief-render`: approved lane fields + current template + sandbox capabilities → `BRIEF <path> validated=<state>`. | proposed | UNMEASURED | Found group starts here. Applies current lane instrument; Codex path-list exception must supersede canned commit instruction. All later rows are additional surfaces. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:43–51` | **F02** Run forward-only DONE pins, correct adoption/time cutoffs, runtime home and generated-task exclusions. | CODE | `tests/test_docs_currency.py::test_new_lane_documents_have_done`; `::test_dispatched_tasks_have_done`. | shipped | UNMEASURED | Checks exist; dispatch wrapper S33 must execute them against actual home. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:66–161` | **F03** For cross-home probes, strip session id in child, resolve explicit initialized home; handle creating-home exception and isolate install/global paths. | CODE | `fleet probe-run`: permitted home + isolated install/user dirs + command + creation flag → `PROBE home=<resolved> rc=<n> receipt=<path>`. | proposed | UNMEASURED | Proposal enforces declared fence, normalizes paths and verifies before mutations. No live/global settings or home-list writes; do not reuse stale init examples blindly. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:68–84` | **F04** Enumerate every executed fleet command and actual home in report. | CODE | `fleet probe-run`: executed argv + observed home receipts → `PROBE-LEDGER <path>`. | proposed | UNMEASURED | Actual invocation ledger, not inferred homes from environment alone. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:163–194` | **F05** Explain why containment held and assess evidence against claimed mechanism. | MODEL | — | partial | UNMEASURED | Judgement challenges a plausible but false explanation; a clean file diff alone cannot establish the mechanism. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:196–215` | **F06** Search whole tree for each retracted/restated old claim before grading repair. | CODE | `fleet claim-scan`: quoted old claims + pinned tree → `CLAIM-HITS <paths:lines> / NONE`. | proposed | UNMEASURED | Whole-tree lexical evidence is CODE; finding semantic equivalents is F07. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:196–215` | **F07** Determine whether remaining occurrences contradict the repair or are legitimate history. | MODEL | — | partial | UNMEASURED | Judgement distinguishes meaning and context from matching words, and sets severity independently of the brief. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:217–262` | **F08** Digest actual working tree before/after run, including untracked files and file count; compare within same checkout. | CODE | Template working-tree SHA256 recipe, proposed `fleet tree-digest`: root + declared exclusion set → `DIGEST <sha256> files=<n> root=<path>`. | partial | UNMEASURED | Never git write-tree for unstaged changes; committed-tree comparisons use HEAD tree id; EOL-filtered checkouts differ legitimately. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:264–276` | **F09** Mutant planter reads/writes bytes, proves patch applied, and restores exact original bytes. | CODE | `fleet mutation-run`: isolated tree + explicit patch + original bytes → `MUTANT <id> applied=<state> result=<state> restored=<sha>`. | proposed | UNMEASURED | Include durable recovery guard so a killed planter cannot strand a mutant; snapshot before alteration. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:272–285` | **F10** Choose probe/oracle population that the repair cannot disable and require a meaningful seed. | MODEL | — | partial | UNMEASURED | Judgement identifies what failure the instrument must catch and whether its population actually covers that failure. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:276–285` | **F11** Prove sandbox before failing canary performs forbidden act; compare re-pin target bytes, not name containment. | CODE | `fleet probe-run` + `fleet citation-check`: sandbox proof + old/new anchor bytes → `SANDBOX <state> CITATION <match/mismatch>`. | proposed | UNMEASURED | Cross-document census included; own-file self-citation tests alone cannot cover it. |
| **FOUND** `docs/lanes/BRIEF-TEMPLATE.md:291–329` | **F12** Anchor conflict-marker search, seed known nonzero, and quote command/base/count together. | CODE | `fleet conflict-count`: pinned merge-tree output + nonzero control → `CONFLICTS base=<sha> count=<n or UNKNOWN> control=<state>`. | proposed | UNMEASURED | Pattern ^\+?<<<<<<<; do not count prose mentions. No current count inferred from old 25/26 receipts. |
| **FOUND** `docs/lanes/README.md:3–5;66–103` | **F13** Keep lane report tracked on lane branch; journal disposable; enforce report conventions and docs updated line. | CODE | `fleet report-check`: lane branch + artifact + allowed paths → `REPORT <path> tracked=<state> conventions=<state>`; existing `tests/test_lane_report_durability.py`. | partial | UNMEASURED | Existing pins check instructions/location, not arbitrary future report commitment; supervisor commits for this sandbox. |
| **FOUND** `docs/lanes/README.md:77–87` | **F14** Land detached gate verdict on gated branch; if rejected, retain verdict on supervisor integration branch. | CODE | `fleet report-land`: recorded gate disposition + verdict path + target branch → `REPORT-LANDED <sha> <path>`. | proposed | UNMEASURED | Mechanical branch routing after review judgement. Supervisor landing only; no worker refs moved here. |
| **FOUND** `docs/lanes/README.md:92–97` | **F15** Write MEASURED/BELIEVED findings and WHERE THIS BRIEF WAS WRONG. | MODEL | — | partial | UNMEASURED | Judgement assesses evidence strength and identifies faulty assumptions; a formatter only checks labels exist. |
| **FOUND** `docs/lanes/README.md:89–91` | **F16** Preserve rescued report with source/branch provenance header. | CODE | `fleet report-rescue`: existing artifact + source path + described branch → `RESCUED <tracked path> source=<path>`. | proposed | UNMEASURED | Does not imply the described branch merged. |
| **FOUND** `skills/fleet/SKILL.md:9;28–43` | **F17** Resolve fleet home, load startup gates/status/claim/autoclean/index/project files, guard and announce revival. | CODE | `fleet interface-boot` + `fleet sup-guard --do`: explicit home + recorded active goals → `STARTUP <summary>; DISPATCH / WAKE <name> / PAGE <reason>`. | proposed | UNMEASURED | Imported by interface launch. Supersede immediate-all-gates paging with daily rule; reuse S/I primitives, not a second implementation. |
| **FOUND** `skills/fleet/SKILL.md:9;16–23;28–43` | **F18** Select relevant project knowledge/task decomposition; assess missing CLI prerequisite or ambiguous authority. | MODEL | — | partial | UNMEASURED | Judgement determines task scope and whether a missing command is a real prerequisite; generic interface direct-worker wording conflicts with server profile. |
| **FOUND** `skills/fleet/SKILL.md:21–23` | **F19** Enforce tier provenance: interface cannot become supervisor with sup-boot. | CODE | `fleet role-check`: caller registry provenance + requested verb → `ROLE ALLOW / REFUSED <reason>`. | proposed | UNMEASURED | Skill explicitly calls provenance guard UNBUILT. This is a missing code barrier, not a reason for more model reminders. |
| **FOUND** `skills/fleet/SKILL.md:CLI reference;Doctrine` | **F20** Use compressed status/peek/result and event waits; do not read raw logs or sleep-poll; re-read retained brief after respawn. | CODE | `fleet status`, `fleet peek`, `fleet result`, `fleet wait`, `fleet respawn`; proposed `fleet brief-check`: stored brief hash → `BRIEF COMPLETE / REFUSED`. | partial | UNMEASURED | Brief store is shipped; validate truncation/integrity mechanically rather than reconstructing scope. |
| **FOUND** `skills/fleet/SKILL.md:Doctrine` | **F21** Choose recovery for confused/long-running worker and appropriate permission mechanism. | MODEL | — | partial | UNMEASURED | Judgement assesses confusion, task continuation and required capabilities; context/turn thresholds and enforcement are CODE. |
| **FOUND** `skills/fleet/SKILL.md:Doctrine` | **F22** Respect own-worker refusals, track attach/mail state, require task files outside dispatcher-overwritten path. | CODE | `fleet kill`, `fleet clean`, `fleet respawn` ownership checks; proposed `fleet dispatch-check`: ownership + brief path + attach/mail state → `ALLOW / BLOCK <reason>`. | partial | UNMEASURED | Do not auto-add --yes after foreign-owner refusal; stale attach is a deterministic anomaly. |
| **FOUND** `skills/fleet/SKILL.md:Learning loop` | **F23** Append approved lesson, project facts, one-line INDEX entries and commit knowledge after campaign. | CODE | `fleet lesson-fold --skeleton`: approved content + project + anchors → `KNOWLEDGE files=<paths> receipt=<path>`; wave-close commit. | proposed | UNMEASURED | Second batch. Content is S37/S39; this is file assembly and durable landing. |
| **FOUND** `knowledge/playbooks/campaign-template.md:routing;1;8` | **F24** Choose correct instrument from declared campaign versus lane/gate shape; load current cursor and copy relevant checklists. | CODE | `fleet campaign-init`: declared shape + progress ledger + file map → `CAMPAIGN resume=<row> instrument=<path> checks=<ids>`. | proposed | UNMEASURED | Campaign template says lane/supervisor shape uses BRIEF-TEMPLATE instead. Its retired C/soak gates and Windows command examples are not newly binding. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§1` | **F25** Read progress first, baseline fleet status, doctor rows and bounded knowledge inputs. | CODE | `fleet campaign-init`: home + progress/knowledge files → `CAMPAIGN-INPUTS <manifest> baseline=<receipt>`. | proposed | UNMEASURED | Do not block merely because doctor has an accounted-for FAIL; interpreting new FAIL is F26. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§1` | **F26** Interpret doctor anomalies, campaign resumption and required operator decisions. | MODEL | — | partial | UNMEASURED | Judgement distinguishes known held gates from new blockers and interprets the work still owed. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§1` | **F27** Preflight git and make authoritative campaign input durable before dispatch. | CODE | `fleet campaign-preflight`: worktree + declared inputs + authorized commit/stash disposition → `PREFLIGHT READY / BLOCK <paths>`. | proposed | UNMEASURED | Never indiscriminately stash/delete user work; absent disposition blocks only the dependent action. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§1` | **F28** Set scope/budget envelope and operator touchpoints including expected spend. | MODEL | — | partial | UNMEASURED | Judgement chooses useful scope, forecast assumptions and decisions the operator must answer. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§1` | **F29** Sum lane ceilings including fix loops, batch recorded questions, persist dated answers, seed/advance and commit ledger. | CODE | `fleet campaign-init`: approved plan/loop bounds/answers → `CAMPAIGN budget=<sum> ledger=<path> answers=<anchors>`. | proposed | UNMEASURED | Budget arithmetic and persistence CODE; no fabricated token usage or spending forecast. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§1` | **F30** Arm wait on named workers; on wake collect status/cost/peek/results and validate RESULT fields. | CODE | `fleet campaign-watch`: worker names + check schedule → `CAMPAIGN-EVENT <states> results=<paths>`; uses `fleet wait <names> --all --timeout 300`. | proposed | UNMEASURED | Check-in loop belongs in code. The caller should not spend a model turn on every empty poll. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F31** Author task goal, write set, invariant preservation rationale and exact review disposition. | MODEL | — | partial | UNMEASURED | Judgement defines task scope and explains how proposed behaviour preserves invariants. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F32** Render quoted authority/full agreed findings, ID mapping, exact DONE, repo rules, permissions, RESULT contract and output limits. | CODE | `fleet brief-render`: approved task + pinned authority/finding ids + current host rules → `BRIEF <path> fields=<validation>`. | proposed | UNMEASURED | Template’s state/tasks/<name> overwrite-prone example and py launcher are stale; use current brief store/path and uv doctrine. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F33** Embed environment evidence/BLOCKED rule and preserve full-precision code/spec/receipt zones; provision approved allowlist before spawn. | CODE | `fleet dispatch-check`: brief + allowed capability record + settings paths → `DISPATCH-CHECK READY / BLOCK <reason>`. | proposed | UNMEASURED | Rendering must preserve exact errors/quotes; grants cannot be invented from headless permission failures. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F34** Classify changes as descriptive versus unbuilt, and distinguish present regressions from future-fix pins. | MODEL | — | partial | UNMEASURED | Judgement maps prose promises to code behaviour and separates current obligations from future scope. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F35** Render corresponding tags/lists; enumerate affected call sites with pinned grep command and output; scan stale-claim wording across tree. | CODE | `fleet claim-scan`: approved claims + source anchors + commit → `CLAIM-RECEIPT <path> hits=<locations>`. | proposed | UNMEASURED | No-match is evidence about searched spellings/population only; not semantic proof of nonexistence. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F36** Move rather than delete stale pins; require source evidence and retirement cap. | CODE | `fleet knowledge-retire`: approved move list + pinned receipts + cap → `RETIRE moved=<ids> REVIEW=<ids>`. | proposed | UNMEASURED | Cap and evidence validation mechanical; whether retirement is justified is F37. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2` | **F37** Judge evidence supporting pin/knowledge retirement and semantic stale-claim correction. | MODEL | — | partial | UNMEASURED | Judgement decides that an entry is obsolete rather than merely moved or differently named. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2;§3b` | **F38** Enforce independent reviewer/promotion authority, drafting until disposition, re-review fields and ruling commit on branch before steer. | CODE | `fleet review-check`: author/reviewer ids + status + verdict + ancestry → `REVIEW READY / BLOCK <reason>`. | proposed | UNMEASURED | Ensure FIXED/NOT-FIXED/REGRESSED/SPURIOUS-FIX fields; grants of repro authority must be recorded. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2;§3f` | **F39** Assign re-review verdicts, identify spurious fixes, and recognize structural failure needing escalation. | MODEL | — | partial | UNMEASURED | Judgement assesses correctness/severity and whether another local patch would compound the defect. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2;§3f` | **F40** Count fix/review waves, require re-review every wave, enforce max three; exclude operator-amendment scope from old budget. | CODE | `fleet review-check`: wave ledger + scope-classification records → `REVIEW wave=<n> NEXT / ESCALATE`. | proposed | UNMEASURED | Whether work is a scope amendment is judgement; tallying its recorded classification is CODE. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2;§3c` | **F41** Run existing red/green checks and collect exact outputs under proper interpreter; validate hook demo collect-and-skip under main. | CODE | `fleet lane-check`: declared tests + hook-source environment + interpreter → `RESULT checks=<receipt> red=<state> green=<state>`. | proposed | UNMEASURED | Latest doctrine targeted lanes only; test design/adversarial cases are MODEL, not the test-running harness. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3a` | **F42** Claim draft stub/status/date and persist question/invariant/graveyard references. | CODE | `fleet spec-scaffold`: approved stub + open-question ids + source refs → `SPEC <path> status=drafting`. | proposed | UNMEASURED | Author cannot promote their own spec; actual answers/rationale are F43. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3a` | **F43** Answer every open spec question, explain touched invariants and assess rejected prior ideas. | MODEL | — | partial | UNMEASURED | Judgement creates the design and evaluates compatibility with prior decisions. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3b–e` | **F44** Dispatch distinct spec/break/code reviewers against full pinned diff; isolate code worktrees and same-file chains. | CODE | `fleet campaign-dispatch`: approved review/build tasks + write sets + branch/role map → `DISPATCH <names> / BLOCK <overlap>`. | proposed | UNMEASURED | Generic campaign capability only; this worker launched no workers. Current policy governs models, not historical strongest-model examples. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3b–e` | **F45** Perform adversarial spec review, full-diff code review and design attacks against increment. | MODEL | — | partial | UNMEASURED | Judgement finds missing invariants, content defects and meaningful attack cases rather than mechanically replaying known tests. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3f` | **F46** Adjudicate both reviews into one binding fix list anchored to function roles. | MODEL | — | partial | UNMEASURED | Judgement resolves conflicting findings and selects the correct remedy without precommitting severity. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3f` | **F47** Deliver fix list to original builder, respawn when warranted, use explicit new task for changed scope. | CODE | `fleet send <original-builder> @<fixes>`; `fleet respawn <name> --task @<brief>`. | shipped | UNMEASURED | Plain-respawn truncation assertion is stale: whole brief is now stored; changed scope still needs explicit override. Current freeze may park native worker rather than resume. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3g` | **F48** Verify actual git commits, run declared DONE checks/doctor, compare receipts to RESULT and record evidence. | CODE | `fleet done-evidence`: git log + artifact + test/doctor receipts → `DONE <task> <state> evidence=<paths>`. | proposed | UNMEASURED | Reconcile Codex path-list handover before calling a task unlanded; worker result/cost is not git truth. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3g` | **F49** Review partial uncommitted artifacts after failed turn and choose retain/revert/re-send disposition. | MODEL | — | partial | UNMEASURED | Judgement identifies which partial edits are useful or belong to someone else; cleanup cannot safely be inferred from an error alone. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3g` | **F50** Apply approved cleanup and idempotent re-send after git-based verification. | CODE | `fleet recovery-run`: exact owned paths + approved disposition + brief → `RECOVERY <name> retained=<paths> restored=<paths>`. | proposed | UNMEASURED | Never broad revert based only on a worker idle state or frozen cost. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3h;§8` | **F51** Require lesson entry names a concrete process change and points to actual instrument edit. | CODE | `fleet lesson-check`: approved lesson + instrument diff + source refs → `LESSON linked=<state> process-change=<anchor>`. | proposed | UNMEASURED | Presence/link validation CODE; assessing whether change is useful is F52. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§3h;§8` | **F52** Decide what worked/stalled and which process change fixes the cause; justify no mechanical remedy when appropriate. | MODEL | — | partial | UNMEASURED | Judgement turns experience into a reusable lesson and chooses campaign versus lane/boot instrument by content. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§4` | **F53** Run same-file chain-link checks and compare actual counts with RESULT before next dispatch. | CODE | `fleet lane-check` + `fleet dispatch-check`: branch + relevant tests + RESULT receipt → `LINK PASS / BLOCK <difference>`. | proposed | UNMEASURED | Old full-floor-per-link and py -3.13 examples conflict with newer targeted-lane/once-merged-tree rule; use applicable current checks. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§4 doc variant` | **F54** Check each anchor and exact witness sentence against agreed finding text. | CODE | `fleet claim-scan`: finding ids + expected anchors/witnesses + target files → `WITNESS matched=<ids> missing=<ids>`. | proposed | UNMEASURED | Hollow anchor cannot satisfy a witness match. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§4 doc variant` | **F55** Read blocker injections in full; verify semantic spec-vs-code claims and proofread measurement prose. | MODEL | — | partial | UNMEASURED | Judgement catches vacuous/garbled text and false behavioural claims that literal witness matches cannot establish. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F56** Require no in-flight turns before merge; inspect clean tree and restore only approved fixture captures. | CODE | `fleet merge-preflight`: roster + git status + declared disposable fixture manifest → `MERGE READY / BLOCK <reason>`. | proposed | UNMEASURED | Source template’s unconditional git checkout fixture reset is unsafe for uncommitted real changes; explicit owned corpus disposition required. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F57** Commit real work before fault injection; restore bytes with EOL-aware proof; guard against interrupted planter. | CODE | `fleet mutation-run`: committed isolated tree + byte patch + recovery manifest → `MUTANT <id> result=<state> restored=<sha>`. | proposed | UNMEASURED | Not git-show blob versus CRLF checkout hashes; a known-good control must fail before zero results are trusted. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5 and version 1.11` | **F58** Execute baseline then planted mutants; assert patches applied; subset-green candidates get full isolated floor before survival claim. | CODE | `fleet mutation-run`: explicit mutant + baseline + subset/full test plan → `MUTANT <id> KILLED / SURVIVED / UNKNOWN receipt=<path>`. | proposed | UNMEASURED | New mutant tree/experiment is distinct from repeated ordinary landing-floor runs; no survival declared from subset alone. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5 and version 1.11` | **F59** Design mutations/control shapes, identify new blind spots, choose catching tests and assess carve-out populations. | MODEL | — | partial | UNMEASURED | Judgement determines adversarial hypotheses and whether a passing detector proves the intended property. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5 and versions 1.8–1.11` | **F60** Replay real founding artifacts, preserve vantage/host/sha, enumerate carve-out census, run shared gates on tree they will meet. | CODE | `fleet evidence-run`: actual incident artifacts + check plan + target tree + actor/host metadata → `EVIDENCE <path> result=<state>`. | proposed | UNMEASURED | Do not fabricate fixture incidents or threshold demand-driven evidence; semantic fitness is F59. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F61** Run merged full floor in clean isolated clone and verify hook-source demo skip behaviour. | CODE | `fleet wave-close`: base + supplied CHANGELOG + declared floor → `FLOOR tree=<sha> py=<version> result=<counts> receipt=<path>`. | proposed | UNMEASURED | First batch reuse. Current host uses uv 3.10/3.12; campaign’s old Windows/premerge duplicate-floor commands are superseded. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F62** Review additive schema compatibility and compare campaign diff to spec/RESULT spec field. | MODEL | — | partial | UNMEASURED | Judgement establishes reader/writer compatibility and actual behavioural conformance beyond a syntactic added-key check. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F63** Collect schema/diff/RESULT evidence and stop landing on unmet approved checks. | CODE | `fleet merge-preflight`: full diff + schema tests + accepted review + RESULT → `MERGE READY / BLOCK <reason>`. | proposed | UNMEASURED | Review judgement is F62; wiring its acceptance into the landing gate is deterministic. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F64** Execute authorized merge then ordered live integration/doctor/init-if-template-changed/hook-smoke with no intervening mutation. | CODE | `fleet merge-run`: approved branch + target + ordered integration plan → `MERGE <sha> VERIFIED / RED <check>`. | proposed | UNMEASURED | Generic campaign mechanism; current supervisor uses fresh clone floor. Live deployment/probes require existing authorization and verified home. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F65** If hooks/template changed, spawn authorized throwaway smoke, confirm both hook events before real dispatch. | CODE | `fleet hook-smoke`: approved temp home + hook diff + budget → `HOOK-SMOKE events=<ids> PASS / FAIL`. | proposed | UNMEASURED | Historical Haiku/$0.50 recipe is superseded by current model/cap policy; do not launch a Claude worker under E4 freeze. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§5` | **F66** Apply authorized revert-on-red, dispatch fixes only afterward; re-merge by reverting revert then merging fixes. | CODE | `fleet merge-run`: recorded merge/revert/fix shas + approved rollback policy → `ROLLBACK <sha> / REMERGE <sha> status=<state>`. | proposed | UNMEASURED | Mechanically executes established campaign policy; do not infer permission to revert arbitrary supervisor branch/user work. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§6` | **F67** Choose bypass, contained bypass, accept+allowlist, or initial plan according to needed capabilities. | MODEL | — | partial | UNMEASURED | Judgement chooses a usable permission mechanism based on task trust and required operations; a headless deny is not automatic authorization to widen it. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§6` | **F68** Validate exact allowlist, budgets, temporary homes/worktrees and owned pid targets before dispatch. | CODE | `fleet dispatch-check`: approved capability mechanism + containment manifest → `DISPATCH-CHECK READY / BLOCK <reason>`. | proposed | UNMEASURED | Bare accept does not make unallowlisted headless tools usable; child environment isolation must be verified. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§7` | **F69** For a future authorized soak, compute usage counts/distinct days and check prior signature before gated build. | CODE | `fleet soak-check`: authorized gate definition + event ledger + signature record → `SOAK MET / NOT-MET / UNKNOWN`. | proposed | UNMEASURED | Historical C soak gates retired; these are inactive generic steps, not current blockers. Calendar passage alone cannot pass. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§7` | **F70** Reconcile mail-ledger/outcomes, phantom-live history and incident records against independent evidence. | CODE | `fleet soak-audit`: event/mail/stream ledger + incidents + roster → `SOAK-AUDIT discrepancies=<ids> evidence=<path>`. | proposed | UNMEASURED | Zero incidents only with actual coverage; independent ground-truth collection is mechanical once oracle chosen. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§7` | **F71** Judge soak evidence, obtain human signature and interpret demand-check answer before build. | MODEL | — | partial | UNMEASURED | Judgement and operator preference determine acceptance and actual friction; code cannot sign or invent demand. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§7` | **F72** Persist dated demand answer/signature and gate only build, allowing spec/review while soak/demand is pending. | CODE | `fleet soak-check`: recorded answer/signature + task kind → `BUILD ALLOW / PARK; SPEC ALLOW`. | proposed | UNMEASURED | Only where an active operator-approved gate exists. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§8` | **F73** Write approved lessons/INDEX/project entries, source anchors and verified commit/date; advance progress with evidence. | CODE | `fleet lesson-fold --skeleton`: approved text + project + source anchors + lane commits → `KNOWLEDGE <paths> progress=<rows>`. | proposed | UNMEASURED | Second batch; narrative lessons exempt from atomized code-anchor checks. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§8` | **F74** Amend chosen live instrument, bump version/date and commit knowledge changes. | CODE | `fleet lesson-fold`: approved instrument edit + lesson anchor → `INSTRUMENT <path> version=<version>`; `fleet wave-close` commit. | proposed | UNMEASURED | Content change is F52; deriving version/date and landing approved text is mechanical. |
| **FOUND** `bin/fleet_keeper.py:1008–1047` | **F75** Keeper tick verifies explicit/imported home, selects profile, loads state, collects observations, evaluates and deduplicates. | CODE | `python3 bin/fleet_keeper.py --once --fleet-home <home>`; `main`, `collect`, `evaluate`, `dedup`. | shipped | UNMEASURED | Keeper stays outside model-plan limit. Snapshot behavior is observe/page; w64-waker may extend it under its own brief. |
| **FOUND** `bin/fleet_keeper.py:741–861` | **F76** Collect registry snapshot, active GOALS, supervisor status/decision, roster, git unpushed count/ref/age, hook-error growth. | CODE | `bin/fleet_keeper.py:collect` and `_sup_status`, `_agents`, `_git_unpushed`. | shipped | UNMEASURED | Read-only aggregation; no fleet.lock, repair or registry quarantine. |
| **FOUND** `bin/fleet_keeper.py:99–114` | **F77** Distinguish not-initialized home from unreadable registry and page reason. | CODE | `bin/fleet_keeper.py:rule_registry_unreadable`. | shipped | UNMEASURED | Both cases deterministic; choosing repair action remains operator/model judgement. |
| **FOUND** `bin/fleet_keeper.py:117–135` | **F78** Distinguish missing CLI from failed roster/login observation. | CODE | `bin/fleet_keeper.py:rule_claude_missing`, `rule_login_expired`. | shipped | UNMEASURED | Failure diagnosis beyond observed error may need judgement; emit evidence, not assumed death. |
| **FOUND** `bin/fleet_keeper.py:138–152` | **F79** Page active-goals claim unknown without treating unreadability as death. | CODE | `bin/fleet_keeper.py:rule_claim_unknown`. | shipped | UNMEASURED | Named ambiguous-state detection is CODE. |
| **FOUND** `bin/fleet_keeper.py:170–395` | **F80** Join claim sid union to roster busy/idle/pid states, respect active goals/freshness/pending handoff, emit stalled page. | CODE | `bin/fleet_keeper.py:_claim_activity`, `rule_supervisor_stalled`. | shipped | UNMEASURED | E6; current rule separates busy from simply live. Guard verdict augmentation is I13, second batch, waker-owned. |
| **FOUND** `bin/fleet_keeper.py:398–404` | **F81** Page pending supervisor decision. | CODE | `bin/fleet_keeper.py:rule_supervisor_frozen`. | shipped | UNMEASURED | Carrying question is CODE; resolving gate is MODEL. |
| **FOUND** `bin/fleet_keeper.py:407–420` | **F82** Page anomalous worker statuses and idle plus unread mail. | CODE | `bin/fleet_keeper.py:rule_worker_anomaly`. | shipped | UNMEASURED | Do not infer cleanup eligibility from an anomaly page. |
| **FOUND** `bin/fleet_keeper.py:423–445` | **F83** Page commits reachable from HEAD but no remote when oldest is ≥6h; name observed ref. | CODE | `bin/fleet_keeper.py:_git_unpushed`, `rule_unpushed`. | shipped | UNMEASURED | Counts/oldest use same rev set, not fixed origin/main..main. |
| **FOUND** `bin/fleet_keeper.py:448–458` | **F84** Page hook-errors log growth. | CODE | `bin/fleet_keeper.py:rule_hook_errors`. | shipped | UNMEASURED | Current-minus-previous line count, not a model log-reading ritual. |
| **FOUND** `bin/fleet_keeper.py:481–520` | **F85** Dedup new/changed/repage events, retire cleared fingerprints and atomically save state. | CODE | `bin/fleet_keeper.py:dedup`, `load_state`, `save_state`. | shipped | UNMEASURED | Do not record failed delivery as sent; F89. |
| **FOUND** `bin/fleet_keeper.py:885–958;1049–1058` | **F86** Resolve registered pane first; report two candidates; defer invalid/unreadable registration or failed pane scan. | CODE | `bin/fleet_keeper.py:registered_pane`, `report_interface_candidates`, `_window_disposition`. | shipped | UNMEASURED | Does not write interface registration; interface-register owns I01. |
| **FOUND** `bin/fleet_keeper.py:960–980;1083–1096` | **F87** Create missing interface window only on sound fallback; leave busy pane intact; defer pages on creation tick. | CODE | `bin/fleet_keeper.py:ensure_window`, `main`. | shipped | UNMEASURED | Only operator recycles existing interface. New-window creation does not dispatch supervisor. |
| **FOUND** `bin/fleet_keeper.py:1060–1081` | **F88** Dry-run mirrors pane lookup and window disposition without sends or writes. | CODE | `python3 bin/fleet_keeper.py --once --dry-run --fleet-home <home>`. | shipped | UNMEASURED | Prints would-create/busy/registered disposition without changing state. |
| **FOUND** `bin/fleet_keeper.py:73–84;982–990;1097–1115` | **F89** Sanitize one printable page; literal tmux send plus Enter; retry failed delivery on next tick. | CODE | `bin/fleet_keeper.py:_page_line`, `page`, `main`. | shipped | UNMEASURED | Delivery state advances only on successful send; no model needed to retype keeper facts. |
| **FOUND** `bin/fleet_keeper.py:main; referenced timer` | **F90** Schedule tick outside Claude plan and generate daily docket at 09:00 Almaty from gate record. | CODE | Existing keeper timer for `--once`; proposed `fleet gate-docket` timer: clock + OPERATOR-GATES + last-delivery state → `DOCKET <date> delivered=<state>`. | partial | UNMEASURED | Second batch docket timer is proposed; existing tick scheduling must not be mistaken for shipped daily docket generation. |
| **FOUND** `knowledge/playbooks/campaign-template.md:§2 grep-receipt gate;§5` | **F91** Re-execute pasted spec receipts at pinned commits; run verifier seeds/strict classifier first, retain live/volatile limits. | CODE | `tools/verify_receipts.py --self-test --strict <spec-file>`; `tests/test_receipts.py`. | shipped | UNMEASURED | Executable receipt verification is CODE; deciding what a receipt proves is MODEL. No receipt execution was needed for this audit-only file. |

**Batch verdict and seams to preserve.** All five first-batch names and all four second-batch items are covered. They are not a complete replacement for the rituals: S04/S07 (nonce/bundle handling), S14–S17/S21/S31 (activity/watch scheduling), S22/S30/S33 (dispatch checks), S43–S45 (DONE evidence and acceptance barrier), and S54–S60/I21–I24 (handoff/watch mechanics) still need executors. Keep the proposed smaller names as components/subcommands of the first batch where sensible; this is an ownership inventory, not a demand for dozens of unrelated top-level CLIs.

`wave-close` takes only the supplied base SHA and CHANGELOG sentences as supervisor input. It derives wave identity/membership and accepted DONE/advancement states from existing task, registry, report and checkpoint records. If those cannot be resolved unambiguously, it reports the missing data; it must not ask the model to hand-enter replacement totals. Resolve HEAD to a fixed accounting tip before generating closure artifacts and stamp both base and tip in the receipt. This is the requested `git diff --numstat <base>..HEAD` at accounting time; generated closing checkpoint/CHANGELOG writes belong to a subsequent commit, not an endlessly changing self-count. Bucket supervisor journal/history before generic docs, retain deletions and unclassified/binary paths in the receipt, and derive worker counts separately from registry membership. The requested roster accounting source for Claude supersedes the skill's registry-counter wording. Preserve unavailable inputs explicitly. Commit/push/delivery retries must reuse the persisted receipt and append state, not duplicate journal entries or recalculate with a new tip. The floor result is keyed to the measured merged code tree; a changed tested tree is a new measurement, not a reason to reuse an old green count.

The most valuable MODEL boundaries are narrow: B04 interprets an inbox task while B03/B05 discover and move its file; S25/I27 interpret gate authority while S24/S26 render and route it; S37 chooses a lesson while F23/F73 assemble it; S44 accepts semantic DONE evidence while S43/S45 collect and enforce it; I29 chooses a next feature while I30 formats it. Detecting a named ambiguous claim and emitting PAGE is CODE. Deciding what an operator should authorize after that page is MODEL/operator work.

**WHERE THIS BRIEF WAS WRONG / found limitations.**

- The three named files do not contain the whole ritual surface. The FOUND group audits the referenced fleet startup/learning loop, lane deliverables/safety/gate/digest recipes, report durability, all campaign checklist sections and the keeper's tick phases/rules. The actual campaign path is `knowledge/playbooks/campaign-template.md`, not top-level `playbooks/campaign-template.md`. Its routing banner makes campaign-only and retired soak rules distinct from current lane requirements.
- There is no historical call ledger available for the requested cost unit. Journal events, suite executions, roster counts, command snippets and mocked test-call counts cannot reconstruct Bash/tool invocations or model turns. This prevents a measured savings total, not completion of an honestly labelled audit. A future executor receipt should record generation, wave/base/tip, ritual-row ID, invocation/attempt ID and actual usage provenance so a later audit can count without reading transcripts; costs remain UNMEASURED until that evidence exists.
- Snapshot instructions contradict each other: old hand-run sweep duty versus the new w64 reap mechanism; Opus/Sonnet lane routing versus the board's all-mcx freeze; standing brief's unconditional gate parking versus ratified-text-only parking; generic startup's immediate-all-gates message versus daily docket; old handoff proof language versus recorded successful claim transfers. These are table findings, not edits to concurrent lanes' files.
- The board's one-time split is shipped, but ongoing rolling is not: six checkpoint headers now live in the 330-line board and checkpoint code has no roll. Reuse the proposed journal-roll under the claim holder; do not merge a snapshot rewrite over newer live checkpoints.
- The supplied reap brief is read and the reap half is **in flight**, not assumed landed. It says both landed/idle/no-mail eligibility and “a live pid ... must never be reaped”; the owning lane must reconcile that precise safety seam. This audit proposes no competing eligibility rule. WAKE in sup-guard likewise requires the waker lane's applicable recorded authorization; the old interface profile still says operator permission is needed for waking idle bodies.
- The brief requests `state/journals/w64-rituals.md` but expressly permits edits only to this report. I obeyed the narrower write fence and kept the lane work log here. No separate journal, test, code or other project file was written. No git add/commit/push or mutating fleet command ran; `MCX_WORKER=1` remained set. No subagents or independent workers were launched.

**Work log and reproducible checks (audit work, not historical turn telemetry).**

1. Read `CLAUDE.md`, the mcx worker skill, all three requested rituals with line numbers, the live w64-reap brief read-only, lane template/README, campaign template, fleet startup/doctrine/learning loop, checkpoint/parser and keeper source. Repository AGENTS search found no applicable AGENTS.md in this worktree; unrelated projects were not used as instructions.
2. Read the full current journal, located the last three incarnation ranges in history and inspected relevant boot/sweep/inbox/floor/handoff/push records. Searched the lane reports for tool-call/turn-count evidence. Full record counts and parser presence were measured with the stdlib-only script below; source reads were read-only.
3. Wrote one classification table, split mechanical/content steps, checked both requested batches and separated additional surfaces with FOUND markers. Proposed names are contracts only; no live commands were driven to validate them.
4. Verified report structure, unique row IDs, seven columns, allowed classifications/statuses, every proposed row's input/output contract, every MODEL justification and all cost cells. Ran the existing DONE helper and new-lane-document pin directly with Python (no pytest installation or full floor required for this prose-only change). Ran whitespace and file-fence checks. Exact final results are recorded below after validation.

Reproduction for the measured journal/parser census (run from this checkout; it reads files only):

```python
from pathlib import Path
import ast, collections, re
sources = ['supervisor/journal-history/2026-07-to-09.md', 'supervisor/JOURNAL.md']
records = []
for source in sources:
    for number, line in enumerate(Path(source).read_text().splitlines(), 1):
        match = re.match(r'## (\S+) (BOOT|CHECKPOINT|PROPOSAL|SEIZED|RELEASED|LIMIT-TRANSFER|HANDOFF-BEGIN|HANDOFF-COMPLETE|HANDOFF-ABORT) inc=(\S+)', line)
        if match:
            records.append((*match.groups(), source, number))
for incarnation in list(dict.fromkeys(row[2] for row in records))[-3:]:
    print(incarnation, dict(collections.Counter(row[1] for row in records if row[2] == incarnation)))
print('board checkpoints', sum(row[1] == 'CHECKPOINT' and row[3] == sources[1] for row in records))
tree = ast.parse(Path('bin/fleet.py').read_text())
verbs = [node.args[0].value for node in ast.walk(tree)
         if isinstance(node, ast.Call) and isinstance(node.func, ast.Attribute)
         and node.func.attr == 'add_parser' and node.args
         and isinstance(node.args[0], ast.Constant)]
print('sup verbs', sorted(verb for verb in verbs if verb.startswith('sup-')))
for verb in ['wave-close', 'sup-guard', 'interface-register', 'journal-roll',
             'sup-beat', 'inbox-drain', 'gate-docket', 'handoff-bundle']:
    print(verb, 'present' if verb in verbs else 'absent')
```

Output at `ef35df139f41ddaa32d72211096b7ea32a07596c`:

```text
inc-20260910T041513Z-181d {'SEIZED': 1, 'CHECKPOINT': 7, 'HANDOFF-BEGIN': 1, 'HANDOFF-COMPLETE': 1}
inc-20260910T075355Z-4f99 {'CHECKPOINT': 9}
inc-20260910T153011Z-0a56 {'SEIZED': 1, 'CHECKPOINT': 1}
board checkpoints 6
sup verbs ['sup-boot', 'sup-checkpoint', 'sup-context', 'sup-decision', 'sup-handoff-abort', 'sup-handoff-begin', 'sup-handoff-complete', 'sup-heartbeat', 'sup-notify', 'sup-release', 'sup-spawn', 'sup-status']
wave-close absent
sup-guard absent
interface-register absent
journal-roll absent
sup-beat absent
inbox-drain absent
gate-docket absent
handoff-bundle absent
```

Presence/absence is against this snapshot parser, not a claim about another lane's unmerged implementation. `cmd_sup_checkpoint` was inspected at `bin/fleet.py:17746`: it calls `supervisor_journal_append`, refreshes heartbeat and emits notices; no history-roll call is present. Source search `rg -n 'journal-history|def cmd_sup_checkpoint' bin/fleet.py` confirms only the checkpoint definition matches.

The initial independent search named nonexistent `tests/test_done_means.py` and returned rc 2; the DONE pin was located in `tests/test_docs_currency.py` and used instead. Broad combined source reads exceeded tool output budgets; missing sections were re-read in bounded slices. No failed search was used as absence evidence.

**Supervisor handover:** commit this report on the lane's behalf using the PATH LIST below. Do not apply any snapshot journal/changelog rewrite from this lane; there is none.

Docs: n/a -- classification audit only; no described runtime behaviour changed.

docs updated: docs/lanes/w64-rituals-audit.md

Final validation log:

```text
PASS: one table; 220 unique rows; 129 requested/addition rows, 91 FOUND rows
PASS: CODE=167 MODEL=53
PASS: 105 proposed contracts have inputs/output arrows; all MODEL notes present; 220 UNMEASURED costs
PASS: first/second batch names; source groups; immediate DONE line; existing new-lane-document pin
PASS: whitespace check stdout='' stderr='' (git diff --no-index --check rc=1 denotes added file)
PASS: tracked git diff empty; only authored addition is docs/lanes/w64-rituals-audit.md
MCX_WORKER: 1
```

No runtime behaviour tests/full floor were run: this lane adds classification prose only. No historical tool-call-cost measurement or savings claim is available. `.mcx/` was already untracked at the initial status check; it is worker-run metadata, not an authored deliverable. The requested separate state journal was not created because of the explicit report-only write fence.

PATH LIST
- docs/lanes/w64-rituals-audit.md
