# GREEN — bounded native live-acceptance review

**Head:** `6e85cf3299eee73f407c75d1e08bad817d5ca6dc`.

**Verdict:** GREEN for truthful evidence; the inference lifecycle remains BLOCKED. The Interface run records three distinct disposable-home claims bound to one exact thread and Codex ancestor PID/start identity. Registration code issues only exact `thread/read`; no mutation journal exists. Host/app-server identity checks, `/proc` tree-reference scans, and removed-tree receipts close cleanup.

The one gated lifecycle registered read-only, stopped that host, and started a fresh owner-only `CODEX_HOME`. Its first preflight was `account/rateLimits/read`; public error `-32600` exited before `thread/start`. The receipt truthfully records no provider thread, accepted turn, retry, or usage. No private auth file is copied or read by the harness. Two fixed-schema low-effort turns, 45-second deadlines, and terminal 64-byte acceptance checks bound any future authorized pass.

**Checks:** Receipt/output and input hashes independently recomputed. Python 3.10/3.12 offline live/auth/register tests: `26 passed, 2 skipped` each under fail-fast provider stubs. Fake acceptance: `22 passed`, no provider process. Diff check passed.

**Blocker:** supported public auth handoff into the disposable home.
