# Next session — after M-E + the reconcile campaign (closed 2026-07-22; doc pass 2026-07-23)

Previous handoff (M-D) is superseded and closed. M-E campaign record: `knowledge/lessons.md#2026-07-21-me`. Supervisor journal has the blow-by-blow; the ledger (`docs/PLAN-PROGRESS.md`) has every wave and verdict.

---

# FIRST ACTION — put the ratification docket to Altai, before any work

Four campaigns of decisions are now queued behind the operator, and the queue is the bottleneck: the nonce slice blocks the three-tier re-draft, which blocks the supervisor tier, and eleven contract markers have been sitting `PENDING` since M-D. **Do this in your first turn, in one message, before spawning anything.**

**The docket is now a live file, not prose in this handoff.** `docs/OPERATOR-GATES.md` is the queue, and
step 1 of the `fleet` skill's startup ritual reads it — so you will have seen the open items before
reading this. (A SessionStart hook used to inject them; it leaked into every unrelated project on the
machine and was removed on 2026-07-22 — `docs/specs/terminal-surface.md` D7.) The copy below is the
reasoning behind each; the file is the source of truth, and ticking a box there is the operator's act
alone.

Present the docket below and ask for a decision on each. Use `AskUserQuestion` where the choice is genuinely multiple-choice (items 2 and 3), plain prose for the rest. Give your recommendation on each — the operator's standing preference is a named recommendation with the alternative stated fairly, not a menu.

**Rules that bind you while doing this:**
- **You may not ratify any of it yourself, and neither may a worker.** An author never promotes its own spec; a reviewer may not ratify one either. Record each answer as a dated line in `knowledge/lessons.md` — never session memory.
- If the operator defers an item, mark it deferred **in this file** with the date, and carry on with the work that is not gated on it (§"The job" below has ungated items).
- If the operator ratifies, the follow-through is a *commit*, not a claim: flip the markers, promote the `Status:` line, and say which commit did it.

## The docket

> **Docket outcome, 2026-07-23 (put to Altai in-session):** item 2 **decided — (b), a
> knowingly-bypassable gate**; the nonce spec is **promoted to spec-of-record**; three-tier (item 4)
> is **unblocked — re-draft now**, folding in two new operator requirements (tier-model assignment:
> highest-tier model on the interface session, second-tier on the supervisor; ~200k supervisor swap
> band, down from 300–500k). Items 1 and 5 were briefly deferred, then **settled later the same day
> by the operator's small-council docket**: substrate rows partially ratified (three dead-daemon
> claims carry `RATIFICATION WITHHELD` pending a quiet-machine capture), M-F dogfood **preempts**
> (envelope still open), fleet-index queued behind M-F, providers doc re-statused spike-negative,
> C/soak framing to be retired after doctrine re-homing. `docs/OPERATOR-GATES.md` is the authority.
> Record: `knowledge/lessons.md#2026-07-23-operator-decisions`; ticks in `docs/OPERATOR-GATES.md`.
> Same-day requirement addendum: role→model config is tier-based and provider-agnostic
> (`lessons.md#2026-07-23-three-tier-inputs` addendum).
>
> **Second docket outcome, 2026-07-23 (3-agent advisory council — risk/delivery/governance lenses — verdicts put to Altai; operator adopted):** item 1 **decided — PARTIAL ratification** (all markers ratified except the three dead-daemon manager-report claims, now `RATIFICATION WITHHELD` inline pending a quiet-machine capture); the worker-providers contradiction **decided — re-statused** to `spike-negative — §4 dead as written`; the two-roadmaps question **decided — retire the C/soak framing**, conditioned on re-homing PLAN §0's still-binding doctrine first; fleet-index M1 **decided — queued behind M-F** (sub-decisions (a) bundle model and (b) tool-call counter still open); item 5 half-decided — **M-F preempts the queue**, budget envelope still open. Remaining open gates: fleet-index (a)+(b), the M-F envelope, and `providers.md` re-base-or-park. Record: `knowledge/lessons.md#2026-07-23-operator-decisions-council`.
>
> **Third docket outcome, 2026-07-23 (council round 2; operator adopted (a)/(b), replaced the envelope question):** fleet-index **(a) collapse to gitignored-only** (finding 2b re-dispositioned: committing the index is documented-unsupported in M1); **(b) tokens-primary** — criterion 1 re-bases onto the existing `input_tokens` outcome telemetry, ≥3 paired runs, transcript counter demoted to a volatile `tools/` diagnostic; M1 fully unblocked, queued behind M-F. **M-F envelope: none — NEW CAP DOCTRINE**: operator is on Claude Max 20x, so **no fleet-enforced token/USD ceilings for workers or managers** (plan limits cap everyone; limit-park/resume is the recovery path), cost counting becomes a default-off flag, and **workers adopt the three-tier 150–200k context band** (binding on the in-flight re-draft). PLAN §0.4's ceiling denomination retires with the queued C/soak retirement. **M-F is unblocked to dispatch; every operator gate is closed** — only `providers.md` re-base-or-park stays parked. Record: `knowledge/lessons.md#2026-07-23-operator-decisions-caps`.

**1. `docs/specs/native-substrate.md` — 11 `[PENDING OPERATOR RATIFICATION]` markers across 8 rows.** Ten are the 2.1.212 set from M-D (transient daemon and its idle-exit; `claude daemon stop`'s machine-wide blast radius; the `gone`→success precondition; `stop` firing no Stop hook; the 3-way `rm`/`stop` ambiguity). One is M-E's new 2.1.216 row: the `daemon.lock` PID-reuse wedge. Ratifying makes them contract-of-record — the thing every future spec and pin test is written against. **Recommendation: ratify all eleven.** Each carries executed receipts, and the 2.1.216 row additionally has a shipped detector and a live green pin behind it. The one honest caveat, stated in the rows themselves: three dead-daemon claims remain manager-report-only, because a `--bg` worker is a live daemon client and structurally cannot observe that path.

**2. `docs/specs/claim-nonce.md` — `Status: drafting`, and five open questions.** Two full dual-lens design gates; final verdicts `sound` (break lens, which verified the env-var spoof by *executing* the pure decision function against shipped code, v3's rules and v2's clause) and `fix-list(F1,F2) → closed` (spec lens, 49/49 receipts sound, harness seeded by the reviewer). Adjudication + the manager's own three corrections: `docs/reviews/ME-NONCE-ADJUDICATION-2026-07-21.md`.

  **The load-bearing question, which the others hang off:** on this box there is **no authorization input that is neither view-derivable nor an environment variable**. Every worker can read `FLEET_HOME`; `sup-status` publishes the holder's identity. So:
  - **(a) detection-only, no gate** — cheap, ships alone, and **does not close the dual-supervisor incident** (the 2026-07-16 zombie ran only `fleet send`, and non-presenters never enter the mechanism);
  - **(b) a gate that is knowingly bypassable** — the only option that *does* close it, at the cost of documenting a bypass in the design;
  - **(c) build an authorization input first** — new scope, new slice, and nothing ships until it exists.
  **Recommendation: (b), explicitly documented as bypassable.** Its honest failure mode is a determined actor with shell access — who already has everything. Option (a)'s failure mode is the incident that motivated the whole slice recurring silently.

  Then: promote the spec to spec-of-record, or send it back. **Do not build until this is answered.**

**3. `docs/superpowers/specs/2026-07-18-sdd-drift-control-design.md` — SETTLED, nothing to ask.** Altai ratified R1–R4 on **2026-07-20**; the spec is `v4`, build-ready pending the M-F slot. R1 already sequences the Phase-1 `spec verify` gate first behind `sdd.enabled` (default off), with the live Stop-fence following once Phase 1 is proven — so the old "narrow to Phase 1" recommendation is discharged, not pending. Record: `docs/OPERATOR-GATES.md` §Settled + `knowledge/lessons.md#2026-07-22-operator-decisions`.

**4. `docs/specs/three-tier-command.md` — still `PROPOSAL`.** Restructure ratified by review, never by the operator. Gated on item 2: the nonce is its hard prerequisite. Nothing to decide today beyond confirming it stays queued behind the nonce.

**5. Confirm the standing budget envelope for M-F**, and whether the dogfood-outward run (§"The job" item 5) should preempt the queue. It has been overdue since 2026-07-09 and is the highest-value defect report available.

---

## State (all verified at close)

- **M-E shipped and pushed** at `6cd4fa7`. Four branches merged (`me/ul`, `me/daemon`, `me/defects`, `me/nonce`). **6-pin FLEET_LIVE tier green; doctor 23 PASS / 0 FAIL.**
- **Branch state (re-measured 2026-07-23): `main` is the only branch.** `61d63a2` collapsed `fleet-impl` onto it; `origin/fleet-impl`, `origin/posix-port`, `origin/feat/worker-providers` and `origin/worktree-*` are remote history only. Local `main` is **ahead of `origin/main`** — push it.
- **Numbers, re-measured 2026-07-23:** unit suite **1403 passed / 8 skipped, identical on `py -3.13` and `py -3.10`** (the declared floor — run both); `claude` **2.1.217**, `state/pin-pass.json` stamped 2.1.217; `bin/fleet.py` **9091 lines**; `fleet doctor` **23 PASS / 0 FAIL**; fleet registry **empty** (19 dead workers cleaned 2026-07-23, journals backed up to `logs/journal-backup-20260723/`).
- **The 9th live catch was a SUBSTRATE failure, not a contract break.** A stale `~/.claude/daemon.lock` naming a **recycled pid** (Windows gave 15740 to `WacomHost`, whose start time is unreadable to a normal-token probe) killed every `--bg` dispatch machine-wide for ~16h while `fleet doctor` read all-PASS. `claude daemon stop --any` does **not** clear it; removing the lock does. Fleet now has `_doctor_check_daemon_wedge` (file-only signal, starts no daemon) and a dispatch-error classifier that names the cause and a precondition-first remedy. Receipts in `docs/specs/native-substrate.md`.
- **New standing tooling: `tools/verify_receipts.py` + `tests/test_receipts.py`.** Every pasted receipt in `docs/specs/**` is re-executed and diffed, resolved at its `# at <sha>` pin via `git archive`. Unpinned receipts, moving pins (`# at HEAD`) and absent commits are errors under `--strict`, never skips. `CLAUDE.md` carries the doctrine line. **Run `--self-test` before trusting a green run.**
- **Campaign template → v1.8** (vantage gate, founding-incident gate, demand-driven-evidence rule, reviewer-remedies-are-fix-waves, receipts-as-executable-claims, merge-target testing, commit-only, RESULT-line exemption).
- **Reconciled after the close-out: `origin/feat/worker-providers` (`92fa85e`) merged into this branch.** It carried the posix/unix port (`f438bce` outcome store + test tier on macOS/Linux, `8601cc1` crontab autoclean scheduling — code only, no docs of their own), the external-provider design and its **negative** `--bg` spike, and the LongCat operator guide. It also carried a **second, older copy of the M-E documents** by a different route; on every M-E document `main`'s copy is the newer one and was kept. The exception is `knowledge/lessons.md` and `supervisor/JOURNAL.md`, which are append-shaped and were **unioned** — theirs' NFC ship-arc entry (`#2026-07-20-nfc`, the anchor `knowledge/INDEX.md` points at) and 38 supervisor journal entries from 2026-07-18 to 2026-07-20 exist only on that branch. The reconcile also brought a **second supervisor lineage** into the journal: `inc-20260718T195311Z-4c82` and `inc-20260720T122844Z-fba2` ran in the other worktree while `main`'s `inc-20260717T011200Z-f1d0` sat idle, so `main`'s `2026-07-21T15:06:30Z SEIZED` now reads as seizing an incarnation the merged record shows was already superseded twice. Both records are true; the interleaving is the honest one.

- **The reconcile made `main` actually cross-platform, and proved it was badly broken before.** The unix implementation was never the risk: it is confined to the platform adapter, so it merged with **zero** conflicts — all 14 conflict hunks were duplicated M-E work, and a break-lens review falsified the "unique work was destroyed" hypothesis three independent ways (line-level presence audit of theirs' whole `bin/` diff, a four-commit test-name census, and a Linux failure-set comparison). What the merge exposed is the finding: on Ubuntu 24.04, **`main` at `56308cd` failed 200 distinct tests**, because `_PosixPlatform` was a stub that raised on every call, so no M-E work had ever run on a non-Windows host. The merge took that to 21; a follow-up wave took it to 0. Final: **Windows 1376 passed / 13 skipped / 0 failed, Linux 1380 passed / 9 skipped / 0 failed.** Verification is `wsl -d Ubuntu` against a native-filesystem clone (`~/fleetlinux`, pytest via `--user` bootstrap; **never run pytest from `/mnt/c`** — line-ending and permission-bit noise). The Linux box has no `claude` CLI, so the FLEET_LIVE tier cannot run there.
- **Five product defects were found by running on a second platform, none of them caused by the merge.** (a) ND-5's *closed* fixed-UTC allowlist resolved through `zoneinfo`, so a closed set depended on the host tzdb — 7 of its 8 members are tzdb backward-links that stock Ubuntu does not ship, and a Linux host reading `resets 4am (Zulu)` null-parked a worker forever where Windows auto-resumed it; now resolved to `timezone.utc` directly, no tz lookup. (b) `_fleet_task_is_ours` lowercased paths, which on a case-sensitive filesystem is a false **MATCH**, not the "false MISS" its comment claimed — the crontab tag is a constant, so two fleet homes differing only in case could overwrite each other's scheduled job; now the adapter declares path semantics. (c) `exc.add_note()` (3.11+) inside a `except BaseException: … raise` turned Ctrl-C into an `AttributeError` at the declared floor — **the second time a 3.11+ API beat a grep-based audit**; the floor is now `MIN_PYTHON_VERSION`, declared once, determined by running at it. (d) `run_py.sh`'s `exec $FLEET_PYTHON` broke on an interpreter path containing spaces — a hook exiting nonzero with no output, straight through invariant 2. (e) two `test_destructive_guard` tests patched a symbol dead since M5/T12 and so really shelled out to the operator's `claude` binary, passing by accident.
- **The adapter lint had a hole and two pins were missing.** `TestPlatformAdapterBoundary` scanned `fleet.py` + two named scripts but **not `bin/hooks/`**; it now globs `bin/**` and `tools/**` with a single `ast`-excised exemption for `stop_outcome.py`'s sanctioned second `os.name` branch. Separately, fault injection found two behaviours with no pin at all — reverting `_render_successor_task` to `py -3.13` (which makes supervisor handoff unrunnable off Windows) and deleting the hook's posix short-write guard (a torn JSONL line, which `read_outcomes` silently skips) each left **all 1376 tests green on both platforms**. Both are now pinned, each verified by re-running the exact mutation.
- **macOS is developed-on but unreceipted.** The port was written and exercised on the branch author's macOS box, which is why the tz gap survived — macOS ships the full backward links. No macOS run exists *in this repo*, so by our own receipts doctrine it is a claim, not evidence. The POSIX case-sensitivity declaration was deliberately made conservative (APFS is usually case-*in*sensitive) so it can only produce the safe error. **Ask the author for a `pytest -q` tail from macOS** — it is one command and it closes the last platform.

## Operator gates open (need Altai — nothing below is ratified)

1. ~~Ratify or reject the `native-substrate.md` rows~~ — **CLOSED 2026-07-23, PARTIAL ratification**: all markers ratified except the three dead-daemon manager-report claims (`RATIFICATION WITHHELD` inline, pending quiet-machine capture / G9 probe). Record: `knowledge/lessons.md#2026-07-23-operator-decisions-council`.
2. **Claim-nonce spec — `docs/specs/claim-nonce.md`, `Status: drafting`.** Two full dual-lens design gates; final verdicts **`sound`** (break) and **`fix-list(F1,F2)` → closed** (spec, 49/49 receipts sound, harness seeded by the reviewer). Not spec-of-record, no build has started. Adjudication + addendum: `docs/reviews/ME-NONCE-ADJUDICATION-2026-07-21.md`. **Five questions are waiting on you**, the load-bearing one being: on this box there is **no authorization input that is neither view-derivable nor an environment variable**, so the honest options are (a) detection-only with no gate — which *does not close* the dual-supervisor incident, (b) a gate that is knowingly bypassable — the only option that does close it, or (c) build an authorization input first, as separate scope.
3. ~~SDD / drift-control design~~ — **CLOSED 2026-07-20**, R1–R4 ratified; spec is `v4`, build-ready pending the M-F slot. Not a gate any more.
4. **Three-tier command** — `PROPOSAL`, restructure ratified by review only. The nonce slice it depends on is now gated-and-sound; a re-draft is the next step *after* decision 2.

## The job: pick and run M-F

Priority-ordered:

1. **Decide the nonce gate question (2 above), then either build the detection slice or re-draft three-tier on top of it.** This is the critical path for the supervisor tier and everything queued behind it.
2. **Two of the three nonce-gate defects remain.** (a) *"a worker turn can hold the supervisor claim"* — **DISPROVED and closed**: it did not reproduce, and `81d4caf` + `f3ec329` (merged at `230803a`) added end-to-end pins to keep it disproved rather than deleting the investigation. Still open: (b) `supervisor/INCARNATION.tmp` and `supervisor/HANDSHAKE.tmp` are not gitignored; (c) the published exit-code contract says 0/2/3 while the code has a 4 with no seam. Both small.
3. **Residuals carried from M-E's final gates**, all named with exact fixes: nonce break-lens F1/F3 (a `fleet clean` double standard; a "stops" that is really a two-TTL bound); spec-lens F3 note (three latent harness gaps, all documented); `me/defects` G-nits (G1 the shape guard's newline case is fixed but its class deserves a lint; G4's stale line-anchored receipt — now exactly what `verify_receipts.py` exists to prevent, so point it at more documents).
4. **Point `verify_receipts.py` at the rest of `docs/specs/`.** Only `claim-nonce.md` is enforced today; `native-substrate.md` pastes evidence with no `# at <sha>` pins and is declared UNENFORCED with a reason. Adopting the convention there is that spec's owner's call — and it is the highest-value place to adopt it, since those receipts are the vendor contract.
5. **Dogfood outward** (standing goal 5): now badly overdue — last external run was stupidbox, 2026-07-09. An external campaign is still the highest-value defect report available.
6. ~~Decide what the external-provider work is now~~ — **half-CLOSED 2026-07-23**: the design doc is re-statused (`spike-negative — §4 dead as written`; `docs/longcat-fleet-usage.md` is the working alternative of record). Still open: does `docs/specs/providers.md` get re-based on the negative result or parked? Take it when the providers line next moves.
7. **Two dispatch residuals filed by theirs' M-E final gate that are still live on `main` — both verified against this tree, both LOW.** (a) `_join_roster_by_short_id` has no H1 guard: a **full-window roster blackout** (zero successful fetches) returns `None`, indistinguishable from "no such session", so a live worker reads as a join failure. `_await_attach` got the CANNOT-VERIFY treatment for exactly this reason and the join path did not. (b) `cmd_sup_handoff_begin`'s name-join fallback has the same shape and raises the `successor-doa` abort flag on a blackout, though the successor may be live. Theirs also filed a MED on `TimeoutExpired` after a daemon-side accept — **not reachable on `main`**, whose successor keeps its own argv path (SPEC §6.1) and therefore never enters `dispatch_bg`'s wedge-retry; `TimeoutExpired` does not appear in `bin/fleet.py` at all. Recorded so it is not re-derived.
8. **`autoclean_task_remove` deletes by task name with no ownership predicate** (`bin/fleet.py`, the platform adapter) — a name-squatting foreign scheduled task is removable, while `_autoclean_task_is_ours` guards only the *creation* side. Documented in-code; filed by theirs' final gate; still true here.

9. ~~`docs/SPEC.md` is merge-stale in two places~~ — **DONE 2026-07-23** (the doc pass). The header's portability-gap list is re-measured (all three v3 gaps closed or re-classified), §13's check count is 23, §18 gained M-D / M-E / Reconcile, and §0 gained a pin-staleness banner. **What is still owed is the actual re-pin**: `c63d7dd` is four milestones back, so every `@NNNN` anchor in the body is off by hundreds of lines. That is a campaign — `SPEC.md` carries no `# at <sha>` blocks, so `tools/verify_receipts.py` cannot help until it adopts the convention (see job item 4, same root).
10. **Two LOW items filed by the reconcile's break-lens review, neither blocking.** (a) *Husk binding:* the merged `cmd_sup_handoff_begin` name-join dropped theirs' life-signal requirement (`"status" in e or "pid" in e`), so one wedged dispatch — zero exit, unparseable stdout, the documented FORCE_COLOR/ANSI case — binds a corpse. Bounded: it surfaces at the 300 s handshake timeout instead of immediately, the claim never moves, no double-spawn. (b) *`sup-release` has no refuse-during-handoff arm:* ours' §6.3 rewrites `INCARNATION` with `state: "released"`, and after a release both `sup-handoff-complete` and `sup-handoff-abort` refuse for want of a claim holder, so the successor cleanup path dead-ends. Spec-only — `sup-release` is UNBUILT — and it feeds the nonce spec rather than standing alone.

## Standing rules (unchanged, they keep earning it)

- CLAUDE.md binds (forward slashes in hook commands; no Git-Bash `&`; views read-only; **receipts re-executed by `tools/verify_receipts.py`**). **`py -3.13` is this box's preference, not the floor** — the floor is `fleet.MIN_PYTHON_VERSION` (3.10) and changes must be run at it (`py -3.10 -m pytest -q`), because a grep-based audit for 3.11+ APIs has now missed one twice. On Windows post-restart the Git-Bash PATH can come up broken — call `py -3.13 bin/fleet.py` directly and `export PATH=/usr/bin:/bin:/mingw64/bin` in Bash.
- **Run the live pin tier on every `claude` bump.** It has now caught nine things, and the 9th was neither our code nor the vendor's contract but the substrate underneath both. **State the probe context AND the vantage** of every receipt: who ran it, and from which worktree / `FLEET_HOME` / commit. The manager filed a phantom regression this campaign for want of the second half.
- **Replay the founding incident through any detector before believing it**, and when the real artifact exists, the fixture *is* the real artifact. A test that fabricates its own evidence is how a check that cannot fire ships green.
- Adversarial + spec review pairs on every branch; **new-defect hunt on every wave, including waves the reviewer itself prescribed** (fired 6/6); grep-receipt gate on every enumeration; no author promotes its own spec; **`ESCALATE` beats a third wave** — used correctly this campaign, twice, with a named restructuring instead of a third finding list.
- **Disputes carry receipts, in both directions.** A builder overturned a manager ruling with PEP 495 and an 8784-sample sweep and was ratified; a reviewer withdrew its own CRITICAL's remedy on an author's three receipts. Both outcomes are the process working.
- `git log` is the only truth a turn landed. A Monitor until-loop survives session teardown better than `fleet wait ... run_in_background` — but re-arm it after any conflicted merge, since it parses a broken `fleet.py` as garbage and exits.
- Push `main` at every green milestone — **there is no `fleet-impl` any more**, `61d63a2` collapsed it. Supervisor `GOALS.md` binds the manager (frugality, long beats, 300–500k handoff band). **Claim wart, still live until the nonce ships:** after a >60m heartbeat gap `sup-boot` refuses the same-sid holder — recover with `sup-heartbeat`, not `sup-boot`.
