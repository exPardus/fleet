# w97 — item28-supervisor-openrouter-substrate

DONE means: `fleet sup-spawn --model openrouter:<slug>` and
`fleet sup-handoff-begin --model openrouter:<slug>` dispatch a supervisor
body that runs on that OpenRouter model, exactly as `fleet spawn
--model openrouter:<slug>` already does for workers since w93, with tests
that fail without the change.

## Result: DONE, no blockers

The dispatch half pre-existed: `_dispatch_supervisor_body` routes through
`dispatch_bg`, which applies w93's `_openrouter_dispatch_args` to every
caller, so argv already carried the bare slug and the inline
`env`+`apiKeyHelper` settings (interface measured a live openrouter
supervisor body on 2026-09-18T15:53Z). This lane pins that half by test and
builds the missing recording half:

- The gen-0 record in `_dispatch_supervisor_body` and the successor record
  in `cmd_sup_handoff_begin` stamp `substrate=_openrouter_substrate(model)`
  (plain models keep `None`) — the one registry line the interface's
  known-good starting point named, plus the handoff twin.
- `sup-status` names what the body runs on: `--json` gains a `body` key
  (`{model, substrate}` from the claim holder's registry row, read through
  `_registry_records_or_none` — no lock, no probe, no quarantine;
  `tests/test_views_doctrine.py` stays green), and the human form prints
  `body model: X substrate=Y`.
- Supervisor journal entry headers carry an optional trailing
  ` substrate=<s>` token: `supervisor_journal_append` writes it when given,
  `_SUPERVISOR_ENTRY_RE` parses it (legacy headers parse with substrate
  `None`), and `cmd_sup_boot` (BOOT/SEIZED/LIMIT-TRANSFER) plus
  `cmd_sup_checkpoint` stamp the holder row's recorded substrate via the new
  `_record_substrate_for_sid`.
- Missing/unreadable key file refuses sup-spawn with the worker path's exact
  message before the runner is called; rollback leaves zero registry rows.
  Claim, nonce and handoff protocols untouched;
  `_require_openrouter_key_readable` untouched.

13 new tests in `tests/test_openrouter_substrate.py`; 10 of 13 fail against
the pre-change tree (red-first verified by stash; the argv/refusal/plain-model
pins pass pre-change because the dispatch half already worked). Targeted
floor 829 passed on 3.10 and 3.12. Full floor 5423 passed / 6 failed on both
interpreters — exactly the six documented pre-existing host-venv artifacts
w93 reported (TestPathContainment x3, TestOutlinePathContainment x1,
TestCollaboratorInstall x2), not adopted, reported as claims per item 17.
The ~90 inserted lines shifted every self-citation below; recomputed and
fixed (`test_self_citations.py`/`test_retired_sid_citations.py` green).

Docs: SKILL.md gained the supervisor-tier openrouter line. Lane result file
was renamed from `w94.json` (stale name from the lane's first, dead attempt)
to `w97.json` by the supervisor at landing; content unchanged.

Model: `openrouter:moonshotai/kimi-k3`. Reviewer findings: 0 (supervisor
diff review at landing).
