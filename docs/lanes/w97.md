# w97 — item28-supervisor-openrouter-substrate

DONE means: `fleet sup-spawn --model openrouter:<slug>` and
`fleet sup-handoff-begin --model openrouter:<slug>` dispatch a supervisor
body that runs on that OpenRouter model, as `fleet spawn
--model openrouter:<slug>` does for workers since w93.

## Result: DONE, no blockers

The dispatch half pre-existed: `_dispatch_supervisor_body` routes through
`dispatch_bg`, which applies w93's `_openrouter_dispatch_args` to every
caller (interface measured a live openrouter supervisor body
2026-09-18T15:53Z). This lane pins that half by test and builds the missing
recording half:

- Gen-0 record (`_dispatch_supervisor_body`) and handoff successor record
  (`cmd_sup_handoff_begin`) stamp `substrate=_openrouter_substrate(model)`;
  plain models keep `None`.
- `sup-status` names the body's model/substrate: `--json` gains a `body`
  key and the human form prints it; the read goes through
  `_registry_records_or_none` (no lock/probe/quarantine — views doctrine
  green).
- Journal entry headers carry an optional trailing ` substrate=<s>` token;
  legacy headers parse with `None`; boot and checkpoint stamp it via
  `_record_substrate_for_sid`.
- Missing/unreadable key refuses sup-spawn with the worker path's exact
  message; claim/nonce/handoff protocols and the key check untouched.

13 new tests; 10 fail pre-change (red-first by stash; dispatch-half pins
pass pre-change because it already worked). Targeted 829 passed on 3.10 and
3.12. Full floor 5423 passed / 6 failed on both — exactly the six documented
pre-existing host-venv artifacts w93 reported, not adopted, in claims per
item 17. Self-citations recomputed after the ~90-line insertion.

Docs: SKILL.md gained the supervisor-tier openrouter line. Result file
renamed w94.json -> w97.json at landing (stale name from the lane's first,
dead attempt); content unchanged. Report written by the supervisor from the
worker's journal + result json.

Model: `openrouter:moonshotai/kimi-k3`. Reviewer findings: 0.
