# `w48-c` — multi-fleet slice (c): the dispatch bakes the home in

**Lane:** build. Worktree `C:/proga/fleet-w48-c`, branch `w48/hookargv`, forked from `main` at
`fa236cb`. Mode bypass. No push, no merge, no other ref moved.

**Shipped:** `--fleet-home` baked into all four dispatched hook commands and into both supervisor
task renders; the four hooks taught to read it in §5's own order, with an argv parser that is
structurally unable to raise; one new `fleet doctor` row, `home-witness`; one divergence record
appended to the spec.

**Every line is MEASURED unless it says BELIEVED.**

> **THIS REPORT HAS TWO ERAS. READ §14 FIRST IF YOU ARE THE SECOND GATE.**
> §§0–13 are the build report at **`1161d39`**, the sha gate `w48-gc` measured. They are left
> unrewritten on purpose — a report edited to agree with its own gate destroys the evidence the
> gate was reasoning about. **§14 is the discharge**, and it supersedes three specific claims
> above:
>
> | claim above | status now |
> |---|---|
> | the `home-witness` doctor row (**§0, §3**, the "Shipped" line, §11.3, §13.1) | **REMOVED from this branch.** Preserved whole on `w49/home-witness` at `1161d39`; parked on an operator ruling. §14.1 |
> | *"`_render_successor_task` renders `{fleet_py}` UNQUOTED … deliberately left"* (**§11.1**) | **FIXED and pinned.** §14.2 |
> | *"`bin/fleet.py` 21,447 → 21,533 lines (+86)"* (**§8**) | **wrong; base is 21,438 and the delta was +95.** §14.4 |
>
> Everything else above — the hook argv work, the `_render_sup_spawn_task` sibling fix, the exit-0
> invariant, the mutant table, the citation method, the divergence record — stands unchanged and is
> what this branch lands.

---

## 0. THE SCOPE ANSWER FIRST — what "witness" means in Sequencing §3(c)

The brief asked for a defended answer before anything else. The honest headline is the
uncomfortable one.

### 0a. The spec does not decide it. MEASURED.

`witness` occurs in `docs/specs/multi-fleet.md` **exactly twice**:

- §5 step 1 — *"Flag/lookup disagreement → mutating verbs refuse without `--yes` + witness line."*
- the Sequencing §3 slice list itself, i.e. the very phrase being interpreted.

No third occurrence. **No sentence anywhere in the ratified document defines a hook-plane or
successor-plane witness obligation.** The brief's Reading B has no textual ground in the spec.
That is a finding, and I am reporting it rather than manufacturing a ruling.

### 0b. Reading A is slice (a)'s, and it is built. MEASURED.

§5 step 1's witness belongs to the global flag, which Sequencing §3 assigns to slice **(a)**.
`docs/mf-slice-a-price.md` says so twice in its own scope table — item 5 (*"destructive via
env/legacy requires `--yes` + witness"*) and item 7 (*"flag/lookup disagreement ⇒ mutating verbs
refuse without `--yes` + witness"*). It shipped: `apply_resolved_home` builds
`[fleet] WITNESS: --fleet-home names …`, pinned by `tests/test_home_resolution.py`.

Sequencing §3 calls its slices **disjoint** in its own words. So (c)'s "witness" cannot be the
same line as (a)'s without the spec contradicting itself one clause later.

### 0c. What I built, and the three sentences it stands on

Not a new concept — the existing one, applied to the plane this slice creates:

1. **§5's own title:** *"Resolution — one order for every caller."* A hook is a caller. Slice (c)
   is what first gives it step 1.
2. **The cross-fleet interference audit, `Daemon env` row:** a hosted body's other env vars are
   *"donor facts; **fenced by hook argv**, blob sid, the lookup, and the destructive tier."* The
   spec names the argv as the fence and the env as what is fenced. That is the precedence ruling,
   quoted, not assumed.
3. **This repo's own meaning of the word**, at `bin/fleet.py`'s identity block: *"Fleet identity:
   THE REGISTRY JUDGES, THE ENVIRONMENT ONLY WITNESSES."* A witness is a second channel whose
   disagreement with the authority is a **detected leak that `fleet doctor` reports and never acts
   on** — the shape `_doctor_check_identity_witness` already implements, and which
   `docs/AUTONOMOUS-2026-07-26.md` states in the same words.

Applied to homes that yields exactly one disagreement worth a row, and it is not the one I first
reached for. §3 below.

### 0d. What I did NOT build — ESCALATED to the operator

Reading B's other half — **a hook writing its own witness record at runtime** when its baked argv
disagrees with its env — is NOT built. Three reasons:

- **The pair a hook can see is not a §5 disagreement.** A hook can compare argv against env, i.e.
  step 1 against step 3. §5 makes no disagreement of that pair: step 1 wins and step 3 is never
  consulted. §5's only disagreement is flag-vs-**lookup**, and a hook cannot run the lookup —
  §5 step 2 costs one `read_registry_at` per listed home on **every PostToolUse**, i.e. per tool
  call.
- **Every candidate surface is worse than the hole.** `hook_errors_path()` is documented as
  *"swallowed hook exceptions"* and `_doctor_check_hook_errors` FAILs on non-empty; a witness is
  not an exception, and on an armed multi-fleet box a donor-env mismatch would be **routine**, so
  routing it there manufactures a permanently-red doctor row. This codebase names that failure in
  its own words, inside `cmd_sup_handoff_begin`'s own neighbourhood: *"a permanently-red row
  trains the operator to ignore the row that will one day be real."* A dedicated per-session log
  is new litter no verb sweeps.
- **The fence removes most of the motive.** After this slice the argv WINS, so a donor-env
  mismatch is no longer a leak — it is a leak prevented. Witnessing a prevented leak is worth less
  than witnessing the one the fence cannot stop, which is what §3's row does.

**For the operator:** if (c)'s "witness" was meant as a runtime hook-side record, it is unbuilt
here and its surface is undesigned. I would want that ruled, not invented.

---

## 1. (c)(i) — hook argv

### 1a. The dispatch surface

`worker-settings.template.json`, all four hook commands, now:

    "\"{{PYTHON}}\" \"{{FLEET_INSTALL}}/bin/hooks/<hook>.py\" --fleet-home \"{{FLEET_HOME}}\""

**`render_worker_settings_template` was not touched.** `{{FLEET_HOME}}` already substituted; the
slice needed the template to *use* it. Slice 0 finding 4 therefore holds by construction —
`fleet_install` still defaults to the home, both placeholders stay legal, and an out-of-tree
template written against `{{FLEET_HOME}}` still renders byte-identically. Pinned by
`test_the_legacy_placeholder_contract_is_untouched`.

Forward slashes and quoting (root `CLAUDE.md`): `{{FLEET_HOME}}` renders through
`Path(...).resolve().as_posix()` and the value is double-quoted inside the command. Driven against
a home literally named `a home with spaces`.

**Both `--settings` sites are covered by one edit — which is why the spec bundled this slice.**
MEASURED: the worker dispatch resolves `settings = Path(settings_path) if settings_path else
instance_settings_path()`; the successor dispatch names `instance_settings_path()` directly. And
`instance_settings_path()` is `<home>/state/worker-settings.json` — **home-relative**, so
`fleet init` already renders one instance per home and the bake is per-home for free, exactly as
the spec's `**Hooks**` line asks (*"argv baked per-home … both `--settings` sites"*). The manager's
steer that these are one mechanism with two dispatch callers is confirmed, and this is the
mechanism.

### 1b. The dispatched plane

Each hook gains `_ARGV_HOME_FLAG` + `_argv_fleet_home()` and an ordered `_fleet_home()`. **No
cross-module import was introduced** — the four stay textually independent per `stop_outcome.py`'s
doctrine, and the *behavioural* agreement test grew a second axis instead
(`TestTheFourStillAgree`, including agreement with `fleet.py`'s own `strip_global_fleet_home`).

| §5 step | in a hook | why |
|---|---|---|
| 1 `--fleet-home` | **NEW** | the baked argv IS the flag |
| 2 sid→lookup | skipped | one registry read per listed home, per tool call |
| 3 validated env | kept, unchanged | `FLEET_HOME` stays a **working** input |
| 4 legacy install root | kept, unchanged | the `__file__` fallback |
| 5 terminus | skipped | its content is *"mutating verbs refuse"*; invariant 2 is exit-0 hooks |

**Grammar matches `strip_global_fleet_home` deliberately** — `--fleet-home V`, `--fleet-home=V`,
`--` ends options — because both consume the same option string, and a hook that disagreed with
`fleet.py` about its own flag's spelling would be drift by construction.

### 1c. Two design calls that could have gone the other way

**An argv home is NOT validated.** §5 step 1 validates (`is_dir`, initialized) and lets a mutating
verb refuse. A hook has no refusal. If it validated and fell through on failure, a broken bake
would be silently traded for **a write into the donor's home** — the exact incident class the
fence closes. So a present, non-empty value wins outright; a failed write stays a logged, exit-0
event *in the home the dispatch named*. Pinned by `test_an_unvalidated_home_is_still_the_answer`.

**A repeated flag with two different values returns `None`** (falls to step 3) rather than picking
one. `fleet.py` refuses it (*"One invocation names one home"*); a hook cannot refuse, so it
declines the contradictory input. Same decision, tiered by what the plane can do.

### 1d. The exit-0 invariant, and an asymmetry the brief did not mention. MEASURED.

**The four hooks are not equally netted.** `bin/hooks/stop_outcome.py`'s entrypoint is a bare
`sys.exit(main())` with **no `except`**, and it calls `_fleet_home()` **above** its own inner
`try`. The other three wrap `main()` in `except Exception` and `sys.exit(0)` unconditionally. So a
raise inside `_fleet_home()` reaches the session as a traceback from exactly one of the four — the
one whose call site is outside the net.

`_argv_fleet_home` is therefore written so it cannot raise: every operation is a bounds-checked
index or a `str` method on a `sys.argv` element. And it is **driven, not read** —
`TestHostileArgvNeverRaises` runs all four hooks end to end (real argv, real JSON on stdin) over
seven hostile shapes: trailing valueless flag, unknown flag, flag-eats-a-flag, empty `=` value,
bare positional, embedded newline, 5000-char value — asserting rc 0 and no `Traceback`.

---

## 2. (c)(ii) — the supervisor task renders

Both renders now carry `--fleet-home "<home>"` on **every** rendered `fleet.py` invocation —
successor: `sup-boot`, `sup-status --json`, `sup-checkpoint`; gen-0: `sup-boot`, `sup-checkpoint`.

The flag is rendered in **global position** (before the verb), and MEASURED that this is safe:
`fleet.py` has **no `--fleet-home` on `build_parser()` at all**. a2's reconciliation consumes the
token in `main()` via `strip_global_fleet_home` before argparse sees it, so
`['sup-boot','--fleet-home','H']` and `['--fleet-home','H','sup-boot']` both yield `'H'`. (I first
mis-measured this by driving `build_parser().parse_args` and reading `SystemExit 2` as a defect;
the parser being flag-free is the design.) The pin does not check a string — it feeds each
rendered line back through `strip_global_fleet_home` itself, so a quoting or spelling slip is
caught by the consumer rather than by a regex that agrees with the bug.

### `_render_sup_spawn_task` HAS THE IDENTICAL HOLE, AND I FIXED IT — stated, not silent

Sequencing §3 names only `_render_successor_task`. MEASURED: `_render_sup_spawn_task` rendered two
`fleet.py` invocations with no `--fleet-home` either. Fixed deliberately, because leaving one of
two sibling renders unfenced is this repo's own named recurring defect — *"fixing only the
reported site is how this project reproduces a miss at the next site."* Slice 0's symbol list
names **both** functions, so §3 naming one reads as an omission rather than an exclusion.

**One honest difference the spec's justification does not cover:** the successor's pre-claim window
is 33.0–63.1s — the *unaccepted* residual. The gen-0 body goes through `cmd_sup_spawn`, which
replicates `cmd_spawn`'s pre-claim→dispatch→stamp choreography, so its window is the **worker**
class, 6.8–10.6s — the *accepted* residual. The hole is identical; the urgency is not. If the
operator wants slice (c) held to the letter of §3, this is the one hunk to revert (mutant M5 is
exactly that revert, and it goes RED).

---

## 3. (c)(iii) — the `home-witness` doctor row

> **NOT ON THIS BRANCH ANY MORE.** The row this section describes was removed by the discharge and
> preserved whole on `w49/home-witness` at `1161d39`, with `docs/lanes/w49-home-witness.md`
> recording why and what it would cost to land it. This section is kept verbatim because it is the
> derivation the operator's ruling is about, and because gate `w48-gc` finding 2 quotes it. **Read
> it as a record of what was built, not as a claim about what ships.** §14.1.

`_doctor_check_home_witness()`, registered immediately after `instance-grants` — the three instance
rows read as one answer about one file. Report-only, like every doctor row.

**Authority:** the home the settings instance LIVES in. **Witness:** the `--fleet-home` baked into
that instance's hook commands. Same fact recorded twice. Disagreement means every worker this home
dispatches runs hooks that drain mailboxes and file outcomes **in a different home**, while every
surface here still reads this home and sees nothing wrong. Nothing else in the tree can see that.

Three reachable states, all from ordinary operation rather than tampering: an instance copied
between homes; `fleet init` run against one home from a shell whose `FLEET_HOME` named another;
and — the common one for a while — an instance rendered before this slice, whose commands carry no
`--fleet-home` and so have no fence.

**Deliberately NOT this row's finding:** an unreadable or unparseable instance. The
`worker-settings-instance` and `instance-grants` rows above own that fault; duplicating their FAIL
here would train the operator to read two rows for one problem. It returns a NOTE at PASS instead,
and `test_it_never_raises_on_a_corrupt_instance` asserts doctor's per-check isolation never has to
catch it.

**Driven live, not only unit-tested.** `fleet init` into a fresh temp home, then `fleet doctor`:

    [PASS] instance-freshness: instance is up to date with the template
    [PASS] home-witness: every dispatched hook command names this home (…/drivehome)

and the same home with the flag stripped from its instance and the instance back-dated:

    [FAIL] instance-freshness: worker-settings.json instance is older than the template -- run `fleet init`
    [FAIL] home-witness: 4 of 4 dispatched hook command(s) carry no --fleet-home, so they fall
    back to the environment -- on a hosted body that is the daemon donor's home, not this one.
    The instance predates the baked argv: re-run `fleet init`

### The manager asked whether `instance-freshness` fired. It did — and here it is CORRECT, not the known false alarm.

The known false alarm is freshness reporting stale when the rendered template is byte-identical to
the instance. **This change makes the template genuinely different**, so every pre-existing home's
instance really is out of date and really does need `fleet init`. Freshness is right for once; it
is not evidence of breakage, and `home-witness` says the same thing independently, by content
rather than by mtime. On a fresh `fleet init` both PASS, because init renders both at once.

---

## 4. The RED, watched

    py -3.13 -m pytest -q tests/test_hook_fleet_home_argv.py
    40 failed, 34 passed in 10.53s

**One of the 40 was my own instrument, and I am reporting it rather than quietly deleting it.**
`test_the_hook_still_exits_zero[nul-in-value-*]` failed on all four scripts *in the parent*:
`subprocess` rejects a NUL in an argument before the child exists, so that shape can never reach a
hook's argv and the param tested only the harness. Replaced with an embedded newline and a
5000-character value, both of which do reach the child.

---

## 5. Mutants — ALL SIX DIED

The planter **asserts `occurrences == 1` before running anything** and aborts otherwise, because a
clean floor and a green mutant are the same output and a planter that silently refused and ran
anyway is a measured wave-47 failure. Each restore is proven by sha256.

| # | mutant | result |
|---|---|---|
| M1 | argv no longer beats env (steps 1 and 3 swapped) in ONE hook | RED — 1 failed, 3 passed |
| M2 | a contradictory repeated flag resolves by position instead of declining | RED — 1 failed, 3 passed |
| M3 | the argv parser raises on an unknown token (exit-0 broken) | RED — 3 failed, 25 passed |
| M4 | one hook command loses its baked home (the fix-one-site defect) | RED — 2 failed, 2 passed |
| M5 | the sup-spawn render loses the flag (the unnamed sibling) | RED — 2 failed, 4 passed |
| M6 | the witness compares nothing (a foreign baked home reads as agreement) | RED — 1 failed, 3 passed |

M3's **3 of 28** is the discriminating number, not a shortfall: the mutant raises only on the
`else` arm, which exactly three of the seven hostile shapes reach (trailing valueless flag,
unknown flag, bare positional). The other four are consumed as values and correctly do not raise.

---

## 7. The divergence record — §5 step 1's refusal is not tiered by verb class

MEASURED, and appended to `docs/specs/multi-fleet.md` after §5's existing divergence record. **No
ratified sentence was edited**; step 1's text is byte-identical.

Step 1 reads *"Flag/lookup disagreement → **mutating verbs** refuse without `--yes` + witness
line."* Driven with a two-home list, the session's sid a member of home B, `--fleet-home` naming
home A: **all nine `TERMINUS_VIEW_VERBS` refuse at rc 1** — `status`, `peek`, `result`, `home`,
`doctor`, `q`, `knowledge`, `sup-status`, `sup-context` — alongside `clean` and `spawn`, which
refuse as ratified.

The asymmetry sits inside one function: §5 step 5's terminus **does** carve views out
(`print(NO_HOME_LINE); return 0`), twenty lines below a step-1 arm that carves nothing out. So the
code already holds the concept and step 1 does not use it. It also collides with the repo's
standing view rule in root `CLAUDE.md`.

**Why it is slice (c)'s business at all:** slice (c) puts `--fleet-home` on a rendered **view** for
the first time — the successor's step-3 ten-minute `sup-status --json` poll. Under a genuine
disagreement that poll refuses for the whole window and the successor ends `HANDOFF-ORPHAN`, and
the `sup-checkpoint` in the same body refuses too, which the render's own text warns *"files a
false second-body row in `fleet doctor`"*. A rendered body has no `--yes` and no way to acquire
one. **Not fixed** — it is slice (a)'s guard and §5's text is under an open operator gate.

**Not reached in ordinary operation**, measured in the same drive: with the flag naming the home
the body's sid belongs to, every verb proceeds; and in the pre-claim window (sid a member of no
home) the lookup misses, falls through, and the flag wins — precisely the hole slice (c) exists to
close, working.

Receipts: `py -3.13 tools/verify_receipts.py --self-test --strict docs/specs/multi-fleet.md` →
**rc 2 (INCONCLUSIVE)**, `parsed receipts: 0/0 … 0 fenced blocks`, `VERDICT: pass -- 0 failure(s)`.
That is the expected and correct outcome, not a dodge: this document is in `test_receipts.py`'s
`UNENFORCED` set with zero fenced blocks, so the harness has nothing to seed — and the *existing*
divergence note explains that adding one fenced line would flip the file into the enforced set and
redden four tests. `tests/test_receipts.py`: 64 passed.

---

## 8. The self-citation tax — MEASURED: twelve, not forty and not zero

> **CORRECTED (§14.4, gate finding 5).** The line below said *"21,447 → 21,533 lines (+86)"*. The
> base is **21,438**, so the delta is **+95**; `21,447` was base plus my own `_render_successor_task`
> hunk — an intermediate state presented as the starting point. The repair itself is unaffected: it
> ran difflib against the base blob, and the alignment figure in this very section
> (*"21,433 of 21,438 base lines"*) names the correct base and reproduces exactly. **Only this
> summary line was wrong.** Corrected in place below, with the original quoted here so the
> correction is legible rather than invisible.

`bin/fleet.py` 21,438 → 21,533 lines (**+95**). **12 rewrites across 9 distinct cited line numbers,
0 refusals.**

Method, and it is not "add the delta":

- old→new map by **difflib** against `fa236cb`'s own blob (alignment covered 21,433 of 21,438 base
  lines; the five unaligned are the lines this change edited, and no citation pointed at any of
  them — hence zero refusals).
- **every rewrite content-verified**: `old_lines[cited-1]` must equal `new_lines[mapped-1]`, else
  REFUSE and report. The script exits non-zero on any refusal rather than writing a partial repair.
- **which numbers are self-citations is decided by the suite's own classifier**
  (`tests/test_self_citations.py`'s `_in_prose` + `_document_prefixed`), applied **per match, not
  per value** — two prose numbers can share a value while only one is a self-citation, and a
  value-keyed filter would rewrite both.
- **ranged forms handled explicitly.** A range END has no colon before it, so `:(\d+)` is blind to
  it; `` `(\w+):(\d+)-(\d+)` `` is scanned separately. (In this file at this commit the range ends
  needed no rewrite — but they were *checked*, which is the point.)
- **fixpoint:** the pin files ran clean on pass 1 and were re-run three times consecutively, all
  green. Repairing the whole derived population rather than the reported failures is what buys
  that.

### A CRLF hazard the tool would have caused, caught before it applied. MEASURED.

`bin/fleet.py` is pure CRLF (21,447 of 21,447 newlines — that count carries the same intermediate-
state error §14.4 corrects; the *property* it asserts, pure CRLF with zero bare LF, is re-measured
true on the discharged tree at 21,453 of 21,453). The scan ran on `Path.read_text()`, which
decodes with **universal newlines** — one byte shorter per line. The first apply asserted the text
at each offset equalled the number it was about to replace and hit `'IS LI'` at offset 842763,
roughly 250 lines past the citation it meant to fix. Offsets are now translated exactly
(`offset + count of '\n' before it`, sound because the file has no bare LF), and both the total
CRLF count and the no-bare-LF property are asserted before the write. Without that assert this
would have been twelve silent corruptions inside a file whose diff nobody could then read.

---

## 9. Floors — predicted, then measured

**All runs at `fa236cb`-based `w48/hookargv`, HEAD proven identical before and after each run
(`fa236cbf7925b9bf080bba57ad9473cf9eee8f14` both sides). No mutant was on disk for any of them.

**Round 1 — the baseline, and one MISSED prediction I did not adjust.**

| run | predicted | measured | verdict |
|---|---|---|---|
| py 3.13 baseline @ `fa236cb` | passed 3800–4200; **14 failed** | `4139 passed, 14 skipped, 1 xfailed` in 437.47s | pass-band **HIT**; **failure count MISSED** |
| py 3.13 full, first build | `4217 passed, 0 failed` | `5 failed, 4212 passed, 14 skipped, 1 xfailed` in 407.69s | **MISSED** |

The baseline miss was mine and it was a misreading, not a floor anomaly: I took
`docs/NEXT-SESSION.md`'s recorded `3647/14/1` triple to be passed/failed/xfailed and predicted 14
FAILURES. The middle term is **skipped**; the baseline has **zero** failures. Reported rather than
retconned, because a prediction that is quietly corrected afterwards proves nothing.

The full-floor miss was five real coupled-edit failures in two classes I had not anticipated —
the doctor check COUNT pinned in three shipped docs, and two position-addressed pins. Both are
dissected in §10e. Fixed, then re-predicted from scratch:

**Round 2 — after the five fixes.**

| run | predicted | measured | verdict |
|---|---|---|---|
| py 3.13 full | `4217 passed, 14 skipped, 1 xfailed, 0 failed` | `4217 passed, 14 skipped, 1 xfailed` in 415.90s | **HIT** |
| py 3.10 full | identical triple to 3.13 | `4217 passed, 14 skipped, 1 xfailed` in 379.60s | **HIT** |
| py 3.10 baseline @ `fa236cb` | `4139 / 14 / 1`, matching 3.13's | `4139 passed, 14 skipped, 1 xfailed` in 366.25s | **HIT** |

**Round 3 — a line-ending regression I caught in my own diff, and did not claim through.**

`worker-settings.template.json` had been rewritten with LF endings while this repo's
`core.autocrlf=true` produces CRLF on checkout, so the working file no longer matched a fresh
checkout. Content-identical (the file is read with universal newlines and every template test was
green either way), but the round-2 floors were run on the LF variant. Converted back to CRLF —
the diff is a clean 4 changed lines with no line-ending noise — and re-verified on the FINAL tree
rather than reasoned through:

| run | measured |
|---|---|
| py 3.13 full, final tree | `4217 passed, 14 skipped, 1 xfailed` in 406.78s |
| py 3.10, the three template-reading files | `139 passed` in 7.22s |

Net: **+78 tests, all from `tests/test_hook_fleet_home_argv.py`**, and the two floors agree exactly
— on the baseline and on the branch. The 3.10 baseline was measured in a detached worktree at
`fa236cb` created under the job's tmp dir and removed afterwards, so the branch's own tree was
never reverted to take it.**

---

## 10. WHERE THIS BRIEF WAS WRONG

### 10a. #1 "(c)(iii)'s scope" — right direction, wrong ground

B is the right *direction* (it is not (a)'s line), but the brief offered *"the spec does not decide
this"* as a possible fallback. It is the answer, not the fallback: the word appears twice and
neither occurrence defines a hook- or successor-plane obligation. The correct answer is neither A
nor B as stated — A is (a)'s and built, B has no textual ground, and what I built is a derivation
from three sentences that do exist (§0c). **Its size did not exceed this lane**, because the
derivation shrank it; the literal Reading B is escalated unbuilt (§0d).

### 10b. #2 "`_render_sup_spawn_task` is out of scope" — the brief was right to doubt it

Identical hole, fixed, stated. The brief's guess that the spec "may simply have missed it" is
supported: slice 0's symbol list names both. One correction — the two do **not** share the
justification (§2).

### 10c. #3 "the precedence order is obvious" — argv-over-env is right, and better-grounded than an assumption

It is quoted, not assumed: the audit's `Daemon env` row names hook argv as the fence.

**"A baked argv is not literally any of §5's five steps" — this is where the brief is wrong.** It
IS step 1: the same flag, the same spelling, the same grammar. §5's title is *"one order for every
caller"*, and a hook is a caller that until now could not run step 1 at all.

**"Whether the disagreement machinery now fires for every dispatched hook is a question with
teeth" — measured, three-part answer:**

1. **Never for hooks.** A hook never enters `main()`; `bin/hooks/*.py` never imports
   `bin/fleet.py` by its own doctrine, so `apply_resolved_home` is unreachable from that plane.
   The hook plane's step 1 is the parser this slice adds.
2. **Not for supervisor bodies in ordinary operation**, and specifically **not in the pre-claim
   window** — the lookup misses, falls through, the flag wins. That is the slice working.
3. **Yes, and hard, under a genuine cross-home condition — including on views**, which is a
   divergence from §5 step 1's own words. §7.

### 10d. #4 "the self-citation cost … could be zero or forty" — twelve. §8.

### 10e. What the brief did NOT warn about, and what it cost

- **The four hooks are not equally protected against a raise** (§1d). The brief states the exit-0
  invariant but presents the four as uniform; the one with no outer `except` is also the one whose
  `_fleet_home()` call sits outside its inner `try`.
- **`fleet doctor`'s check COUNT is pinned in three shipped docs**, not only in the test suite
  (`tests/test_doc_claims.py::test_doctor_check_counts_match_the_registered_checks` over
  `README.md`, `docs/getting-started.md`, `docs/launch-readiness.md`). Adding one doctor row is a
  six-site documentation edit. This is what broke my first full-floor prediction, and neither the
  brief's cost section nor my own reading of the doctor-registration pin
  (`tests/test_native.py::TestCmdDoctorRegistersNewChecks`, which pins ORDER, not count) predicted
  it.
- **Two existing pins address their subject BY POSITION rather than by what it is** —
  `test_install_home_split.py`'s hook-path extraction took the LAST quoted token, and
  `test_supspawn_fixwave2.py`'s quoting class asserted `"{fleet_py}" sup-boot` by adjacency. Both
  go RED for any change that inserts an argument, without the property they pin having moved. Both
  re-addressed by subject, with the reason recorded in-place.

---

## 11. Findings reported and NOT fixed (fence discipline)

1. ~~**`_render_successor_task` renders `{fleet_py}` UNQUOTED**~~ — **FIXED IN THE DISCHARGE, §14.2.**
   The original text, for the record: *"while its sibling `_render_sup_spawn_task` quotes
   `"{fleet_py}"`. An install path containing a space splits that command. Pre-existing, one token
   away from my own edit, deliberately left — it is not slice (c)'s scope, and 'one token away' is
   exactly how a fence erodes. My own added argument IS quoted, and `test_supspawn_fixwave2.py`'s
   quoting class covers only the sibling."* Every clause of that is true; the last one is now
   measured rather than believed (§14.2's RED control), and it is why the new pin covers both
   renders instead of the three lines that were wrong.
2. **§5 step 1's refusal is not tiered by verb class** — nine view verbs refuse at rc 1. Recorded
   as a divergence (§7), not fixed: slice (a)'s guard, §5's text under an open operator gate.
3. **No migration for pre-slice settings instances.** They keep working (env fallback) but have no
   fence; `home-witness` FAILs on them naming `fleet init`, the same remedy `instance-freshness`
   names. Deliberate — a silent auto-re-render at read time would be a write from a view.
   **STILL TRUE, MINUS THE DETECTION.** With the row gone (§14.1) nothing on this branch reports an
   unfenced instance at all; `instance-freshness` still fires on the mtime, which is what actually
   caught it in the live drive. The gap is now larger, not smaller, and it is named in
   `docs/lanes/w49-home-witness.md` as part of what the parked row buys.
4. **The hook plane still cannot run §5 step 2.** By design here (cost), but it means a hook whose
   dispatch predates the bake has no route to its home other than the env. Named so a later slice
   does not rediscover it as a defect.

---

## 12. Fence compliance

- Commits on `w48/hookargv` only. No push, no merge, no other ref moved.
- `supervisor/GOALS.md`, `docs/OPERATOR-GATES.md`: untouched.
- `docs/specs/multi-fleet.md`: **appended** one divergence record after §5's existing one. No
  ratified sentence edited; §5 step 1's text byte-identical.
- A detached worktree at `fa236cb` was created under the job's tmp dir to measure the py-3.10
  baseline, and removed afterwards. It created no branch and moved no ref.
- Repair and fault-injection scripts live outside the repo (job tmp), deliberately: neither is a
  deliverable of this slice and neither was asked for.

## 13. For the gate

The three things most worth attacking, in the order I would attack them:

1. **§0's derivation.** It is a derivation, not a quotation, and I have said so. If the gate
   concludes (c)'s "witness" meant something else, `_doctor_check_home_witness` is one function
   and one check-list line plus a six-site doc count — cleanly revertible.
2. **The `_render_sup_spawn_task` inclusion.** Not named by the spec. M5 is the revert.
3. **The unvalidated argv home** (§1c). I chose "a named home is never silently traded for an
   unnamed one" over §5 step 1's `is_dir` validation, because a hook cannot refuse. The opposite
   choice is defensible and would be a two-line change plus one flipped test.

---

# 14. THE DISCHARGE — gate `w48-gc`, 0 BLOCKING / 3 MAJOR / 4 MINOR

**Every line in this section is MEASURED unless it says BELIEVED.** Written for the second
adversarial gate. Where a number below disagrees with §§0–13, this section is the current one and
§§0–13 are left standing as the evidence the first gate reasoned about.

The gate is `git show w48/gc:docs/lanes/w48-gc.md`, 629 lines. I read all of them. It cleared four
attacks against this work under measurements stronger than mine, and its §7 says plainly where its
own brief was wrong. **Two of its three MAJORs are discharged by DELETION, not by patching** — the
operator ruled the `home-witness` row out of this slice, and removing it dissolves findings 1, 3
and 6 rather than fixing them.

## 14.0 What changed, and what did not

| gate finding | grade | discharge |
|---|---|---|
| 1 — `docs/launch-readiness.md` says `29 checks … 28 PASS / 0 FAIL` in one bullet | MAJOR | **DISSOLVED.** Every doctor-count site reverts to `28`; the bullet is self-consistent with **no edit aimed at it**. §14.1 |
| 2 — the `home-witness` row is originated content | MAJOR | **REMOVED from the slice**, preserved whole on `w49/home-witness`. §14.1 |
| 3 — the row parses one of three `--fleet-home` spellings; three decisions unpinned | MAJOR | **DISSOLVED** with the row. Recorded as the re-land conditions in `docs/lanes/w49-home-witness.md`. §14.1 |
| 4 — `_render_successor_task` renders `{fleet_py}` unquoted | MINOR | **FIXED and pinned**, over both renders and over the whole file. §14.2 |
| 5 — the report's line-count arithmetic | MINOR | **CORRECTED** in §8, in place, with the original quoted. §14.4 |
| 6 — a CR-suffixed baked home renders a LEAK between two identical strings | MINOR | **DISSOLVED** with the row. Carried to `w49/home-witness` as a re-land condition. §14.1 |
| 7 — an unmakeable baked home loses the outcome record silently | MINOR / observation | **NOT FIXED, and not mine.** The gate measured it identical at `fa236cb` through `FLEET_HOME`; slice (c) adds a second channel with the same property. Left recorded so a later slice does not rediscover it. |

**NOT touched, deliberately:** the hook argv work, `_render_successor_task`'s `--fleet-home` argv,
the `_render_sup_spawn_task` sibling fix (the gate examined it specifically — *"in scope by the
repo's own sibling-defect doctrine; correct as written"* — and mutant M5, its exact revert, is RED),
and the `docs/specs/multi-fleet.md` divergence record. §14.1 explains why that last one stays.

## 14.1 TASK 1 — the row is preserved, then removed

### Preserved first

`git branch w49/home-witness 1161d39` — the exact sha the gate measured, carrying the complete
slice including the row, its four tests and the derivation. On it, one commit **`da04c80`**,
`docs/lanes/w49-home-witness.md`: what the branch holds (with line numbers at that sha), findings 2
and 3 in substance, **the four properties that must be repaired before it could land** (P1 the
shipped spelling defect; P2 `normcase`, P3 the empty value, P4 the registration position — all
three of those survived the full 4217-test floor as gate mutants X2/X4/X5), finding 6, and a
six-step re-land recipe. Written for an operator returning to the docket and for a successor who
was not here. That branch is now left alone.

### Then removed — and the scope was DERIVED, not taken from the brief

The brief named its own list a starting point and said it was probably incomplete. It was, in one
direction and not the one it expected.

| the brief said | MEASURED |
|---|---|
| `_doctor_check_home_witness` + its `functools.partial` | correct — fn `bin/fleet.py:11252–11323`, registration `:12564`, **plus 5 comment lines above the registration** that argue the row's position and are dead without it |
| *"the `home-witness` tests … 5 occurrences by grep"* | **4 TESTS.** Five grep hits, in four test bodies. And the removal is wider than the class: the `rendered_home` fixture and the `_FakeVersion` stub are used by **nothing else in the file** (verified by grep over the whole file), so both go too |
| *"six doctor-count doc sites … `README.md` ×3, `docs/getting-started.md` ×3, `docs/launch-readiness.md` ×1"* | that is **seven**, and the true census is **EIGHT** — 6 in the `N checks` phrasing and **2** in `N PASS / 0 FAIL`. Seven were bumped to 29; the eighth (`docs/launch-readiness.md`'s own tally) was not, which IS finding 1 |
| *"assume an eighth exists and go looking"* | **found, and it is that one.** Searched the whole tracked tree for `N checks`, `N PASS`, `checks: N` and `N rows`. Everything else that names a doctor count (`knowledge/lessons.md`, `docs/PLAN-PROGRESS.md`, `docs/reviews/**`) is a claim about a PAST tree at 13/21/22/23 checks and is correctly pinned history — the repo's own ratified rule (`docs/OPERATOR-GATES.md`, 2026-08-05) says a claim about a past tree is not rot |

**One thing the sweep found that is NOT mine to fix, stated so the next gate does not report it as a
miss.** `docs/SPEC.md:281` says *"**23** checks (`grep -c "def _doctor_check_" bin/fleet.py` → 23…)"*
— stale **at the base too**, since `fa236cb` registers 28. It is pre-existing drift in a file that
is not in `test_doc_claims.py`'s `ENTRY_DOCS`, it is not touched by this branch in either direction,
and fixing it would be exactly the origination the operator just ruled against. Recorded, not
fixed. (`knowledge/lessons.md` already names this file as *"the one doc `tools/verify_receipts.py`
does not cover"*.)

### The brief's last uncertainty, checked before committing as ordered

> *"my claim that the `+37` lines in `docs/specs/multi-fleet.md` are about hook argv rather than the
> witness. **Check that last one specifically** — if your spec addition documents the row, it leaves
> with the row."*

**MEASURED: it documents the divergence, not the row. It STAYS.** `git diff fa236cb..HEAD --
docs/specs/multi-fleet.md` is a single appended block titled *"SECOND DIVERGENCE RECORD — step 1's
refusal is not tiered by verb class at all"*. It contains the word `witness` exactly once, inside a
quotation of step 1's own ratified sentence (*"`--yes` + witness line"*). It names
`_render_successor_task`, `apply_resolved_home`, `TERMINUS_VIEW_VERBS` and `TERMINUS_EXEMPT_VERBS`;
it never names `home-witness`, `_doctor_check_home_witness` or `fleet doctor` except to explain that
a refused `sup-checkpoint` *"files a false second-body row in `fleet doctor`"* — a different row, and
one that exists at the base. Every claim in that block is still true of this branch.

### Verified by EXECUTION, not by diff

Driven on a temp home with this branch's `bin/fleet.py`, `INSTALL_ROOT` a throwaway worktree,
`CLAUDE_CODE_SESSION_ID` removed, and the whole run gated on `fleet home` (§14.7):

```
--- fleet doctor BEFORE init ---  rc=1
    ROWS=28  PASS=24  FAIL=4
      [FAIL] worker-settings-instance      [FAIL] instance-freshness
      [FAIL] instance-grants               [FAIL] hook-registration
    'home-witness' present in output: False

--- fleet init (no --home, no --statusline)  rc=0

--- fleet doctor AFTER init ---   rc=0
    ROWS=28  PASS=28  FAIL=0
    'home-witness' present in output: False
```

**28 in both places, which is what the brief predicted.** The gate measured 29 rows / 25 PASS /
4 FAIL / then 29 PASS on the row-bearing tree; mine is exactly that minus one row, with the same
four FAILs. And `docs/launch-readiness.md` now reads *"28 checks; on a fresh home it fails 4 … goes
to 28 PASS / 0 FAIL"* — **both halves independently measured true in that one drive**, which is more
than the bullet has ever had behind it.

Static corroboration, by two independent measurements that must agree:
`grep -c "^def _doctor_check_" bin/fleet.py` → **28**; `cmd_doctor`'s `check_calls` by AST → **28**.
`git diff fa236cb -- README.md docs/getting-started.md docs/launch-readiness.md` is **empty** — the
three count-bearing docs are byte-identical to base.

### The citation tax, again — 12 rewrites, 0 refusals

Removing 80 lines from `bin/fleet.py` (21,533 → **21,453**) moves every self-citation below them.
Repaired by the same method §8 describes and gate §2E verified, run against `1161d39`'s blob rather
than the base: **21,451 of 21,534 lines paired** by difflib with every integer masked; each cited
line mapped through the alignment; **every rewrite content-verified byte-identical** before it was
written; a cited line with no aligned successor would have been REFUSED and nothing written.

**12 rewrites, 0 refusals, 31 citations already correct.** The shifts are **−74 or −80** depending
on whether the registration removal sits above the target as well as the function removal — which is
the point of aligning rather than subtracting a constant. The ranged form `` `cmd_respawn:8470-8472` ``
sits above both removals and correctly needed no rewrite; the range-end scanner was **controlled**
(fed `` `cmd_foo:100-200` `` and confirmed to capture the end) rather than assumed working from a
zero result. `tests/test_self_citations.py` green on pass 1 and on three consecutive runs;
`test_retired_sid_citations.py` and `test_doctrine_citations.py` green with it. CRLF preserved:
21,453 CRLF, **0 bare LF** — the hazard §8 records, asserted rather than hoped.

## 14.2 TASK 2 — the unquoted `{fleet_py}`, fixed and pinned by property

Three lines in `_render_successor_task` now render `"{fleet_py}"`. After the fix, **all 12 path
placeholders in `bin/fleet.py` (`{py}` and `{fleet_py}`, on 6 lines, in 3 functions) are quoted** —
enumerated by scanning the character on each side of every placeholder, not by editing the three
lines the gate named. The third function is **`_steer_supervisor_release`** (`:9107`), which
neither render test can reach and which was already correct at the base — so the source-scan half of
the pin covers a site the behavioural half is structurally blind to. Ownership derived by AST
innermost-`def`, not by proximity.

**The pin is deliberately not the shape of the defect.** The gate's warning was explicit: *"a pin
that only checks the one line you edited will not stop the next sibling from being added unquoted."*
So `TestEveryRenderedCommandQuotesItsPaths` holds it twice:

1. **Behaviourally, over both renders**, parametrized, with a space in **both** roots
   (`Fleet Install With Spaces` / `Fleet Home With Spaces`) — each emitted command is split with
   `shlex`, the consumer's own parser, and the script path, the interpreter path and the home must
   each come back as a **single argv token**. A regex that agrees with the bug cannot pass this.
2. **In source, over the whole file** — every `{fleet_py}` / `{py}` placeholder anywhere in
   `bin/fleet.py`, in any function, must be written quoted. That half needs no fixture, no
   signature and no call, so a **third** render added tomorrow is covered without anyone
   remembering to extend a parametrize list.

Plus a **seed** (`test_the_scan_can_see_a_bare_placeholder`): the source scan's assertion is that a
set is EMPTY, which is the shape that fails silently, so the scanner is fed a bare placeholder, a
half-quoted one in each direction, and a correct one, and must classify all four.

**RED WATCHED, with the sibling as a control.** One line — the `sup-status` poll — un-quoted on
disk, planter asserting `occurrences == 1` before running anything:

```
2 failed, 86 passed in 39.94s
  FAILED …TestEveryRenderedCommandQuotesItsPaths::test_a_space_…[successor]
  FAILED …TestEveryRenderedCommandQuotesItsPaths::test_no_bare_path_placeholder_is_left_anywhere_in_fleet_py
```

Both halves of the new pin fire on a **single** un-quoted line — and
**`tests/test_supspawn_fixwave2.py` stayed GREEN throughout**, which measures the claim §11.1 made
on belief: the pre-existing quoting class genuinely covers only `_render_sup_spawn_task`. Restore
verified sha256-identical.

## 14.3 TASK 3 — the doc-claim pin widened, and watched RED

`_CHECK_COUNT` is anchored on the word `check`, so `28 PASS / 0 FAIL` was invisible to it — and
**that module's own docstring names this exact string as its worked example of what it does not
hold.** Added `_PASS_FAIL_TALLY` and
`test_doctor_pass_fail_tallies_sum_to_the_registered_checks` over the same six `ENTRY_DOCS`.

Three design calls, each with a reason:

- **Pinned as a PAIR against `N + M == registered`**, not `N == registered`. The sum is the row
  count whether or not the run was clean, so the pre-`init` `25 PASS / 4 FAIL` shape is held too,
  and requiring both halves stops the regex firing on any lone number that happens to precede the
  word PASS.
- **`re.I` is safe, and it was checked rather than assumed:** `\bPASS\b` cannot match inside
  `passed` (the boundary fails on the following `e`), so a pytest summary like
  `1403 passed / 8 skipped` — which appears in this repo's docs — is invisible to it. Pinned in the
  seed test.
- **Held as a separate test, not folded into the existing one**, so a reader is told *which*
  phrasing drifted. The third occurrence of this drift was a document stating both, one stale.

**The docstring's hole list was edited, because leaving it would make the module lie about itself.**
`28 PASS / 0 FAIL` came off the list — with the note that it came off *the expensive way*, and that
the remaining entries should be read as the next defect rather than as a disclaimer.

**CONTROL, on a scratch copy, watched.** `docs/launch-readiness.md` planted back to the gate's
finding-1 state exactly (`28 checks … **29 PASS / 0 FAIL**`), sha256 restore verified:

```
1 failed, 25 passed
  AssertionError: docs/launch-readiness.md states a `N PASS / M FAIL` tally summing to [29];
  cmd_doctor registers 28 check(s).
  FAILED …::test_doctor_pass_fail_tallies_sum_to_the_registered_checks[docs/launch-readiness.md]
```

**Exactly one failure — and `test_doctor_check_counts_match_the_registered_checks[docs/launch-readiness.md]`
stayed GREEN on the same planted file**, which is the measurement that matters: it demonstrates the
old pin's structural blindness on the very document and phrasing the gate found, rather than
asserting it. `tests/test_doc_claims.py`: **20 → 26 tests**, green.

## 14.4 TASK 4 — the arithmetic, corrected

§8 said *"21,447 → 21,533 lines (+86)"*. Re-derived: `git show fa236cb:bin/fleet.py | wc -l` →
**21,438**; `git show 1161d39:bin/fleet.py | wc -l` → **21,533**; delta **+95**. `21,447` was base
plus my own `_render_successor_task` hunk — an intermediate state presented as the starting point.
Corrected in place in §8, with the original quoted above the correction so the change is legible.
**The repair was never affected**: it ran difflib against the base blob, and §8's own alignment
figure (*"21,433 of 21,438 base lines"*) names the correct base and reproduces exactly. The
adjacent CRLF paragraph carried the same intermediate number; it is annotated rather than silently
rewritten, and its *property* — pure CRLF, zero bare LF — is re-measured true on this tree at
21,453 of 21,453.

## 14.5 TASK 5 — the brief-template correction, and a collision the brief could not have known about

**The brief's premise was false at the sha it was written against, and true at `main`.**
`docs/lanes/BRIEF-TEMPLATE.md` at `fa236cb` — this branch's base — has **no safety stanza at all**;
the `--fleet-home <temp>` instruction was hand-written into four separate wave-48 briefs
(`w48-c`, `w48-gc`, `w48-launch`, `w48-glaunch`). But `main` moved: **`44f056e docs(lanes):
FLEET_HOME is not a fence -- add the safety stanza every brief was missing`** landed the stanza
between `fa236cb` and `cef230f`. Editing my base copy would have produced a duplicate section and
an unresolvable add/add conflict.

**So my branch's copy is `cef230f`'s text with the correction folded in.** `diff` against
`cef230f:docs/lanes/BRIEF-TEMPLATE.md` is **three hunks, all mine**. The integrator should take my
side of this file wholesale; nothing of main's stanza is dropped.

What the correction adds, and why it is not a nit: main's stanza says *"pass `--fleet-home <temp>`
EXPLICITLY on every `fleet` invocation meant for a temp home"* — **unusable for the one case it
matters most in**, the `init` + `doctor` drive that every lane touching home resolution ends up
running. MEASURED here, independently of the gate that first measured it:

```
fleet home --fleet-home <fresh temp dir>
  rc=1
  fleet: --fleet-home <dir> is not initialized (not_initialized) -- an initialized home is one
  whose `state/fleet.json` exists and parses (docs/specs/multi-fleet.md, Definitions). Nothing
  was created.
```

And in the same drive, **`fleet init` does not create `state/fleet.json` either** (checked after a
successful `init`: still absent), so the flag never becomes usable on that home by initialising it.
The stanza now splits the two cases — the flag for homes that already exist, `FLEET_HOME` **plus
removing `CLAUDE_CODE_SESSION_ID`** for homes that do not — and names `fleet home` as the gate in
both. One further line was earned by my own instrument failing (§14.7): **compare the gate's output
NORMALISED**, because `fleet home` prints `as_posix()` and a literal compare against a Windows path
reads as a breach that is not one. `tests/test_lane_report_durability.py` green (15 passed).

## 14.6 TASK 6 — the floors

**Derived by `pytest --collect-only -q` on both sides and both interpreters, never by arithmetic on
a diff and never by counting `def test_` lines.**

| tree | py 3.13 | py 3.10 |
|---|---|---|
| `main` at `cef230f` | **4154 collected** | **4154 collected** |
| this branch, discharged | **4238 collected** | **4238 collected** |

`main` gained **zero** tests since the fork point — `git log fa236cb..cef230f` is eleven commits and
`git diff --stat` names no test file, so `cef230f` collects exactly what `fa236cb` did (4154, which
gate §4 independently confirmed). The branch moved 4232 → **4238**: −4 witness tests, +4 quoting
tests in the same file (net 0), **+6** in `tests/test_doc_claims.py` (20 → 26, from one new
parametrized test over six `ENTRY_DOCS`). The arithmetic is shown only because it corroborates the
collection; the collection is the derivation.

### PREDICTIONS, written here BEFORE either run

- **P10:** `py -3.13 -m pytest -q` → **4223 passed, 14 skipped, 1 xfailed, 0 failed**
  (4238 collected − 14 − 1).
- **P11:** `py -3.10 -m pytest -q` → **identical triple to P10.** The floor is 3.10, not this
  machine's 3.13 preference.
- **P12:** `git rev-parse HEAD` identical before and after each run, and `git status --porcelain`
  reports **only my eight modified files** after each — in particular
  `worker-settings.template.json` untouched (it has been clobbered to `{}` by the suite before) and
  byte-identical to `1161d39`. *(I first wrote "six" here and corrected it to eight by counting
  `git status` — before the runs reported, and it changes nothing the prediction asserts. Recorded
  rather than silently fixed, because an edited prediction is worth exactly as much as the reader's
  ability to see it was edited.)*

If P10 misses, I report the miss and do not adjust the prediction.

**Merge note, measured while deriving the above.** `main` touched **four** of the files this branch
touches — `README.md`, `docs/getting-started.md`, `docs/launch-readiness.md` and
`docs/lanes/BRIEF-TEMPLATE.md`. Reverting the doctor count removes this branch's change to the
first three entirely (`git diff fa236cb -- <them>` is empty), so they cannot conflict: base and my
side agree, and main's edits win unopposed. That leaves `BRIEF-TEMPLATE.md` as the **only**
doc-level collision, and §14.5 resolves it by carrying main's text forward. `main` changed **no
test file and nothing under `bin/`** (`git diff --name-only fa236cb cef230f`, nine files, all
docs), which is why its collection count is unmoved at 4154.

### MEASURED

**P10, P11 and P12 all HIT.** Both interpreters return the identical triple, and it is the
predicted one.

| run | predicted | measured | verdict |
|---|---|---|---|
| `py -3.13 -m pytest -q` | `4223 passed, 14 skipped, 1 xfailed, 0 failed` | `4223 passed, 14 skipped, 1 xfailed in 415.81s (0:06:55)` | **HIT** |
| `py -3.10 -m pytest -q` | identical triple | `4223 passed, 14 skipped, 1 xfailed in 367.55s (0:06:07)` | **HIT** |

`4223 + 14 + 1 = 4238`, which is the collected count derived independently above — so the two
measurements close on each other rather than one being computed from the other.

**Tree integrity around both runs (P12), checked after each, not once at the end:**

- `git rev-parse HEAD` → `1161d39…` before, after the 3.13 run, and after the 3.10 run. Unmoved.
- `worker-settings.template.json` sha256 →
  `8091bbf055e6c16ec07e2503b306fb954cb58c941dc2b5c067c325f7173e7fbe` at all three checkpoints, and
  **byte-identical to `1161d39`'s blob** (compared by hashing the blob with its CRLF endings
  restored, not by trusting `git status`). This is the file the suite has previously overwritten
  with `{}`, and it is the same digest gate `w48-gc` §4 recorded — so two lanes now agree on it.
- `git status --porcelain` after each run → exactly the eight files this discharge edits, and
  nothing else. No suite-written damage to any tracked file.

**Relative to the branch under gate:** `4217 → 4223 passed` (+6), all six from
`tests/test_doc_claims.py`; the four `home-witness` tests that left and the four quoting tests that
arrived cancel in `tests/test_hook_fleet_home_argv.py`, which still collects 78. `0 failed` on both
interpreters, which is the same clean state the gate reproduced at `1161d39`.

## 14.7 SAFETY LEDGER — every `fleet` command, and the home it touched

`FLEET_HOME` is **not** a fence inside a fleet-launched session, so it was not used as one. Every
invocation ran as a child process of my own script with **`CLAUDE_CODE_SESSION_ID` removed**, and
`INSTALL_ROOT` was a detached worktree under the job tmp dir so that even a §5 step-4 install-root
fallback could not reach `C:/proga/claude-fleet`.

| command | count | home it touched |
|---|---|---|
| `fleet home` | 2 | temp home — used as the **GATE**: the script exits before running anything else unless `fleet home`, invoked identically, prints the temp path |
| `fleet doctor` | 2 | temp home (once before `init`, once after) |
| `fleet init` | 1 | temp home. **No `--home`** (that is the flag that appends to the homes list). **No `--statusline`** |
| `fleet home --fleet-home <fresh temp>` | 1 | **nothing** — refused `not_initialized`, rc 1, and it says so: *"Nothing was created."* |

**Six commands total. None named `C:/proga/claude-fleet`.**

- **`fleet init` was NEVER run against the live home.**
- **`~/.claude/fleet-homes.list`: ABSENT before and ABSENT after**, checked programmatically both
  times rather than assumed.
- **`~/.claude/settings.json`: unchanged** — `(mtime, size)` identical before and after
  (`1786210442, 1859`).
- No statusline, no spawn, no push, no merge, no ref moved except the one branch task 1 ordered
  (`w49/home-witness`, created at `1161d39`, one commit `da04c80`).
- `supervisor/GOALS.md`, `docs/OPERATOR-GATES.md`, and every RATIFIED section of
  `docs/specs/multi-fleet.md`: **untouched**.
- Two detached worktrees under the job tmp dir (one at `cef230f` for the collection derivation, one
  at `HEAD` as the drive's `INSTALL_ROOT`), plus one at `w49/home-witness` to commit `da04c80`. All
  three removed; `git worktree list` names none of them. The temp homes were deleted.
- Repair and drive scripts live outside the repo (job tmp), as in §12.

**INSTRUMENT FAILURE, recorded because a report that hides these is asking to be trusted on the
rest.** The drive's `fleet home` gate failed on its first run and refused to proceed. The cause was
mine, not the subject's: `fleet home` prints `as_posix()` and I compared it literally against a
Windows path, so the separators disagreed. **The gate working, not a breach** — and it is the
reason the template stanza now says to compare normalised (§14.5). Fixed by
`os.path.normcase(os.path.normpath(...))` on both sides; nothing ran in between.

## 14.8 WHERE THIS BRIEF WAS WRONG

**Wrong 1 — TASK 5's premise, and it changed the work.** *"`docs/lanes/BRIEF-TEMPLATE.md`'s safety
stanza tells every lane on this machine to pass `--fleet-home <temp>`."* At this branch's base that
file has **no safety stanza**; the instruction lives in four hand-written briefs. On `main` it does
exist, added at `44f056e` after this branch was cut. Following the brief literally against my own
base would have shipped a second, duplicate stanza and an add/add conflict. §14.5.

**Wrong 2 — the doc-count census.** *"The six doctor-count doc sites … `README.md` ×3,
`docs/getting-started.md` ×3, `docs/launch-readiness.md` ×1."* That enumeration is seven, and the
true census is **eight** in two phrasings. The brief's own instruction (*"assume an eighth exists
and go looking"*) is what found it, so this is the brief being right about being wrong.

**Wrong 3 — *"5 occurrences by grep"* for the tests.** Five grep hits, **four tests** — and the
removal is wider than the class, because a fixture and a stub die with it. The brief told me to
derive the real set and not trust its count; both halves of that instruction earned their place.

**Wrong 4 — the last uncertainty resolves the OTHER way, in my favour, and I checked it rather than
assumed it.** *"my claim that the `+37` lines in `docs/specs/multi-fleet.md` are about hook argv
rather than the witness."* Measured: the block documents the §5 step-1 divergence and never names
the row. It stays. §14.1.

**RIGHT, and it decided the shape of the work:** *"Removing the row does not fix findings 1, 3 and 6
— it dissolves them."* Exactly so, and it is why finding 1 needed no edit aimed at it. Also right,
and expensive to have got wrong: *"derive the real set, do not trust my count"*, three times over.

**Where I was wrong, in my own first report:** §11.1 called the unquoted `{fleet_py}` *"not slice
(c)'s scope"* while §2 justified the `_render_sup_spawn_task` fix with *"fixing only the reported
site is how this project reproduces a miss at the next site."* The gate is right that those two
sentences cannot both be load-bearing, and it is right about which way the tie breaks: the unquoted
line sits inside the function Sequencing §3 **does** name. The rule I applied was "smallest diff"
and the rule the repo actually holds is "the sibling goes with it."

## 14.9 FOR THE SECOND GATE

The three places I would attack this discharge, in order:

1. **The removal's completeness.** I derived it by grep over the whole tracked tree plus the suite,
   and the two independent doctor counts (AST registrations, `def` grep) agree at 28 — but an
   author's enumeration of their own change is exactly the claim the first gate taught me not to
   trust. The cheapest attack: re-derive the doctor row count from a live `fleet doctor` on a tree
   you build yourself, and re-run my doc-site census with a different regex than mine.
2. **The new quoting pin's source-scan half.** It is a regex over `bin/fleet.py` looking at one
   character either side of a placeholder. It has a seed and it has been watched RED, but it would
   not see a path interpolated under a different name, or one built by concatenation. State it as
   narrow rather than assume it is wide.
3. **`docs/lanes/BRIEF-TEMPLATE.md`.** My copy is `cef230f`'s text plus three hunks, taken from a
   ref my branch has not merged. That is the least conventional thing in this diff. Check that I
   dropped nothing of main's stanza — `diff <(git show cef230f:docs/lanes/BRIEF-TEMPLATE.md)
   docs/lanes/BRIEF-TEMPLATE.md` should show three hunks and no deletions of substance.

And the thing I would most like checked that is *not* a defect claim: `w49/home-witness` is now the
only place a real mitigation for a cross-home hook leak exists. Nothing on this branch reports an
unfenced settings instance. If the operator's ruling goes against re-landing it, that gap should be
recorded somewhere that outlives the branch.
