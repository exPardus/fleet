# w73 — Four-verdict supervisor guard
DONE means: the heartbeat/liveness table is implemented; OK pages and wakes nobody; keeper and --do accept it without action; every row is tested; SPEC, fleet skill, and validator agree; guard/keeper suites pass on Python 3.10 and 3.12.

- Outcome: DONE on `w73/guard-verdict-table`, based at `d3ab326`; no commit.
- Fresh heartbeat + live busy/idle PID returns plain/JSON `OK`.
- Stale live idle yields WAKE; stale PID-less yields DISPATCH; fresh PID-less pages.
- Registry/roster failures and existing ambiguity checks retain PAGE.
- Stale busy remains PAGE; a live body never permits DISPATCH.
- Released-body SID-union PIDs block DISPATCH even without a supervisor roster name.
- OK suppresses the keeper's supervisor page without relying on `quiet`.
- Only WAKE can retry deferred fork retirement and send; OK/PAGE do neither.
- Guard and keeper never spawn; DISPATCH pages the interface for sup-spawn.
- Tests cover each table row, ambiguity precedence, no-action OK, and PID safety.
- Python 3.10: 447 passed, 2 skipped (19.37s), exit 0.
- Python 3.12: 447 passed, 2 skipped (18.14s), exit 0.
- All 10 guard/keeper suites ran, plus liveness readers, views doctrine, and round-7 pins.
- Two skips are existing views-doctrine cases; no guard/keeper failures.
- Commands use the required offline uv form; `git diff --check` passed.
- Full logs and exact commands: `state/journals/w73-logs/`.
- Journal: `state/journals/w73.md` (runtime state is gitignored).
- No live-home actions, tmux input, commits, or subagents; MCX_WORKER=1 retained.
- Blockers: none.

PATH LIST
- `bin/fleet.py`
- `bin/fleet_keeper.py`
- `tests/test_sup_guard.py`
- `tests/test_keeper_wake.py`
- `tests/test_keeper_rules.py`
- `docs/SPEC.md`
- `skills/fleet/SKILL.md`
- `docs/lanes/w73.md`
- `state/journals/w73.md`
- `state/journals/w73-logs/`
