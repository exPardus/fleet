# Lane w80 — fleet brief and product-line citation
DONE means: `fleet brief <item>` emits a checkable brief and refuses an absent or stale `Serves:` citation.

MEASURED — `fleet brief w80` is registered in `build_parser()`, bypasses fleet-home resolution, and emits the full DONE line, current base SHA, authored file list, test command, structured-result contract, blank `Judgement:`, and verified citation.
MEASURED — `bin/fleet_brief.py` is a leaf importing only `FleetCliError`; it does not import `fleet` or copy parser globals.
MEASURED — citations require exactly one `Serves: <## section> — "<phrase>"` line; the section and phrase are checked against `product.md`.
MEASURED — the dispatched-task pin uses 2026-09-11 21:00 UTC, preserving older briefs and rejecting missing, malformed, misplaced, or stale citations thereafter.
MEASURED — `brief` is explicitly unclassified pending the operator-owned effect table; the leaf is in the implementation census.

Checks: code suites — Python 3.10: 480 passed, 4 skipped; Python 3.12: 480 passed, 4 skipped.
Checks: docs suites — Python 3.10/3.12: 184 passed, 1 failed, 2 xfailed each.
Blocker: the docs failure is pre-existing at untouched base `6cfdd28`: `569411d` changed `bin/*.py` without docs or `Docs: n/a`; it was not changed here.
Baseline: untouched-base code was 472 passed, 4 skipped; untouched-base docs were 181 passed, 1 failed, 2 xfailed.

WHERE THIS BRIEF WAS WRONG

The section name is useful, not redundant: moving an otherwise identical phrase between `##` sections changes what the feature serves, and the validator now catches that move instead of silently accepting it.

docs updated: `skills/fleet/SKILL.md`, `docs/lanes/BRIEF-TEMPLATE.md`, `docs/lanes/w80.md`

PATH LIST:
- `bin/fleet.py`
- `bin/fleet_brief.py`
- `tests/fleet_sources.py`
- `tests/test_fleet_sources.py`
- `tests/test_round7_defect_pins.py`
- `tests/test_docs_currency.py`
- `tests/test_fleet_brief.py`
- `skills/fleet/SKILL.md`
- `docs/lanes/BRIEF-TEMPLATE.md`
- `docs/lanes/w80.md`
- `docs/lanes/w80.json`
