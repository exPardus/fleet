# `w49-gc2` — adversarial gate over the slice (c) DISCHARGE

**GATING** — 2 MAJOR, 3 MINOR, **0 BLOCKING**. Merge is not blocked by anything below.

**Lane:** gate. Worktree `C:/proga/fleet-w49-gc2`, branch `w49/gc2`. Mode bypass.
**Under gate:** `w48/hookargv` @ **`2841c95`** — re-derived, not taken from the brief:
`git branch --format` reports `w48/hookargv 2841c95`, still the tip.
**Measured against:** base `fa236cb`; the previous sha `1161d39`; `main` read at **`1c0d807`**.
**Predecessor:** `docs/lanes/w48-gc.md` (GATING, 3 MAJOR / 4 MINOR / 0 BLOCKING), carried into this
branch by the manager so it lands with the slice it gates.

My branch is `2841c95` plus that one docs file and nothing else — verified, not assumed:

```
$ git diff --stat 2841c95 HEAD
 docs/lanes/w48-gc.md | 629 +++++++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 629 insertions(+)
```

**Every line below is MEASURED unless it says DERIVED or BELIEVED.** Every measurement whose good
answer is zero/empty has a named known-non-zero control that ran first. Two controls caught an
instrument failure before it could become a finding (§6).

---

## 0. THE ANSWER

**The discharge is sound, and the lane's self-report is accurate everywhere I could check it.**
All six task claims reproduce — several under measurements stronger than the ones reported. Both
floors hit the claimed triple exactly on both interpreters. The removal is complete to zero residue.
The citation re-pin is clean. The trial merge reproduces to the file. All four of the lane's
corrections to the manager's brief are **true**, including the one that says the brief's premise was
false.

It is **GATING**, not GREEN, on two things — and both are the *same shape*: a pin this commit added
makes a claim about its own coverage that is **wider than the coverage it has**, and in each case
there is a live instance of exactly what it claims to hold sitting outside its reach today.

| # | Grade | What | Cost to clear |
|---|---|---|---|
| 1 | **MAJOR** | The quoting pin's *"a **third** render added tomorrow is covered"* is false. It is placeholder-**name**-scoped, not property-scoped. A **fourth** render exists today — `_install_statusline` (`bin/fleet.py:6134`) writes a shell-executed command with **both** paths bare — and both halves of the pin are blind to it. Driven: rc=127/rc=1, control rc=0. | one line + a widened scan |
| 2 | **MAJOR** | The widened doc pin has the **same class of blind spot it was built to close**, demonstrated by two surviving mutants on a real entry doc. And the census is **nine**, not eight: `docs/SPEC.md:281` states `**23** checks` behind a pasted `grep -c` receipt; the truth is **28**. Three separate instruments cannot see it. | one number + one regex |

Neither is a regression. Neither is in the mechanism. Both underlying defects are **pre-existing**;
what this commit added is the claim that they are covered. That is what moves them from MINOR to
MAJOR, and it is the whole of my case — I say so plainly rather than dressing a pre-existing defect
up as this branch's.

---

## 1. FINDINGS

### FINDING 1 — MAJOR. The quoting pin is scoped to two placeholder *names*, not to the property; and a bare rendered command is in the file right now.

`tests/test_hook_fleet_home_argv.py`, the new `TestEveryRenderedCommandQuotesItsPaths` docstring,
and `docs/lanes/w48-c.md` §14.2 both state the coverage claim:

> **In source, over the whole file** — every `{fleet_py}` / `{py}` placeholder anywhere in
> `bin/fleet.py`, in any function, must be written quoted. That half needs no fixture, no signature
> and no call, so a **third** render added tomorrow is covered without anyone remembering to extend
> a parametrize list.

The scan is `_PATH_PLACEHOLDER = re.compile(r"(.?)\{(fleet_py|py)\}(.?)")`. It holds the property for
**two spellings**. A render that names its paths anything else is invisible to it, and the
behavioural half only drives `_render_successor_task` and `_render_sup_spawn_task`.

**(a) The mechanism, proved by a discriminating pair.** Both mutants unquote the *same* render —
`_steer_supervisor_release`, the third function, which the shlex half structurally cannot reach —
and differ only in what the placeholders are called. Clean subset floor first: `861 passed, 1 skipped`.

| # | mutant | result |
|---|---|---|
| **Q4** | `_steer_supervisor_release` unquoted, placeholder names **KEPT** | **RED** — 1 failed, 860 passed (`test_no_bare_path_placeholder_is_left_anywhere_in_fleet_py`) |
| **Q3** | the **same render**, same unquoting, placeholders **RENAMED** to `{interp}` / `{script_path}` | **GREEN — SURVIVED** the subset (861 passed, 1 skipped) and the **full floor** (§1.1) |

Q4 proves the scan does cover that function. Q3 proves the survival is attributable to the
**spelling** and to nothing else. That is the brief's hypothesis — *"a source scan over placeholders
is exactly the kind of instrument that misses a placeholder spelled differently"* — confirmed with
its own control.

**(b) It is not hypothetical. The uncovered render is shipped, and it is on the documented quickstart
path.** `bin/fleet.py:6134`, inside `_install_statusline`, with the author's own comment above it
noting that the string is shell-executed:

```python
        # Forward slashes: this command string is executed through a shell.
        "command": f"{Path(sys.executable).resolve().as_posix()} {script}",
```

Neither path is quoted. `script` is `statusline_script_path()` = `INSTALL_ROOT/bin/fleet_statusline.py`.
A census of every rendered `"command":` in the tree at `2841c95` returns exactly six sites: four in
`worker-settings.template.json` (**all correctly quoted**, and pinned by
`test_the_rendered_home_is_forward_slashed_and_quoted`), `bin/fleet.py:5955` (which stores an
incumbent command verbatim and renders no path), and this one. **It is the only unquoted rendered
command in the tree, and it is the one both new pins miss.**

**(c) Driven, with a real spaced interpreter and a real spaced install root.** A venv built at
`Interp With Spaces` gives a genuine working `python.exe` behind a space; `_install_statusline` was
called for real with `user_settings_path` redirected to a temp file. Subject = what
`fleet init --statusline` writes; control = the same command with both paths quoted:

```
SHIPPED RENDER: C:/.../Interp With Spaces/Scripts/python.exe C:/.../Fleet Install With Spaces/bin/fleet_statusline.py

  SUBJECT  [git-bash] rc=127  marker=False  sh: C:/.../tmp/Interp: No such file or directory
  SUBJECT  [cmd.bat ] rc=1    marker=False  'C:/.../tmp/Interp' is not recognized as an internal or ex
  CONTROL  [git-bash] rc=0    marker=True   STATUSLINE-RAN-OK
  CONTROL  [cmd.bat ] rc=0    marker=True   STATUSLINE-RAN-OK
```

shlex tokenises the shipped string into **seven** argv entries where there should be two. And while
this is true, `find_bare_path_placeholders(bin/fleet.py)` returns `[]`.

**Reachability is wider than finding 4's was**, which is the part that matters. The predecessor's
finding 4 needed the *fleet install root* to contain a space. This one breaks if **either** root
does — and the interpreter half fires on `C:\Program Files\Python313\python.exe` (the all-users
python.org default) and on Microsoft Store Python (`C:\Program Files\WindowsApps\...`). `fleet init
--statusline` is **step 4 of the README quickstart**.

**PRE-EXISTING**, and I checked rather than assumed: byte-identical at `fa236cb`, `1161d39` and
`2841c95`. No test pins its quoting at any of the three. So the *defect* is not this branch's and
would be MINOR on its own — exactly where the predecessor put finding 4. **What earns MAJOR is that
this commit added an instrument, said in writing that it holds this property file-wide, and it does
not** — while an instance sits nine thousand lines above the pin. The lane's stated reason for
building the property pin instead of a three-line pin was *"fixing only the reported site is how this
project reproduces a miss at the next site"*. The next site was already there.

*Remedy:* quote both paths at `:6134` (one line), and widen the scan from two names to the property —
e.g. flag any `"command"`-valued f-string whose interpolations are not each `"`-delimited, or scan
for `sys.executable` / `.as_posix()` interpolated without adjacent quotes. Minimum viable: correct
the docstring so the next reader does not trust a guarantee that is not there.

### FINDING 2 — MAJOR. The widened doc pin has the same class of hole it was built to close, and the census is nine.

Task 3's whole purpose was that a *phrasing* escaped `_CHECK_COUNT`. The fix added
`_PASS_FAIL_TALLY = re.compile(r"\b(\d+)\s+PASS\s*/\s*(\d+)\s+FAIL\b", re.I)` and rewrote the
module docstring's hole list, ending: *"Read the remaining entries in this list the same way — as
the next defect, not as a disclaimer."* Read that way, the list is incomplete.

**(a) Two surviving mutants, on a real entry doc, in the pin's own scope.** Positive control first:

| # | mutant, all in `docs/launch-readiness.md` | result |
|---|---|---|
| **P1** | the tally drifts: `**27 PASS / 0 FAIL**` (sums to 27, not 28) | **RED** — `test_doctor_pass_fail_tallies_sum_to_the_registered_checks[docs/launch-readiness.md]` |
| **P2** | *the same wrong tally*, numbers markdown-bolded: `**27** PASS / **0** FAIL` | **GREEN — SURVIVED** |
| **P3** | *the same wrong tally*, comma form: `**27 PASS, 0 FAIL**` | **GREEN — SURVIVED** |

P1 proves the pin fires and the file is in scope. P2 and P3 are the identical falsehood in the
identical file, and it passes. `\d+\s+` cannot cross a `*`, and `/` is hard-coded.

**(b) The ninth site, which is the same blindness in the wild.** `docs/SPEC.md:281`:

> **23** checks (`grep -c "def _doctor_check_" bin/fleet.py` → 23; it was 22 on the hook-removal
> branch and 21 at §0's `c63d7dd` pin …)

```
$ grep -c '^def _doctor_check_' bin/fleet.py
28
```

**Stale by five, and it carries a pasted receipt asserting the wrong number.** Three independent
instruments miss it, each for a different reason:

1. `find_check_count_claims(whole SPEC.md)` → `[]`. The number is bold-**wrapped**, so `\d+\s+checks`
   never matches. Controls: `23 checks` → `[23]`; `**23 checks**` (bold outside) → `[23]`. The
   blindness is specifically bold *inside* — and that form is **not** in the docstring's hole list.
2. `docs/SPEC.md` is **not in `ENTRY_DOCS`** (`README.md`, `docs/getting-started.md`,
   `docs/concepts.md`, `docs/README.md`, `CONTRIBUTING.md`, `docs/launch-readiness.md`), so a fixed
   regex would still not run there.
3. `tools/verify_receipts.py` covers `docs/specs/`. `docs/SPEC.md` is not under `docs/specs/`.

**This exact defect has happened in this exact file before, and the repo wrote it down.**
`knowledge/INDEX.md:35`: *"`21 checks` was wrong in 3 files incl. a pasted grep receipt in `SPEC.md`
— **the spec of record is the one doc `tools/verify_receipts.py` does not cover**."*

**Grading it honestly.** The branch did not create this and was not asked to fix it: `docs/SPEC.md`
is byte-identical at `fa236cb` and `2841c95` (`git diff --name-only fa236cb 2841c95 -- docs/SPEC.md`
is empty), and it was already stale at base. The lane's "eight sites" is **correct** as a census of
*what the branch had to touch* — I verified all eight and found no ninth of that kind. It is
incomplete as a census of *the tree*. So: **MAJOR as census completeness and as a live hole in a
brand-new pin; pre-existing as a defect.** The predecessor made the same split for its finding 2
("MAJOR as governance, not as defect") and it is the right shape here too.

*Remedy:* `**23** checks` → `**28** checks` in `docs/SPEC.md` (or delete the receipt, which is the
better fix — the number is derivable); tolerate `**` inside both regexes; and either add
`docs/SPEC.md` to `ENTRY_DOCS` or bring it under the receipt harness.

### FINDING 3 — MINOR. The new comment's worked example is a tally its own pin would reject.

`tests/test_doc_claims.py:239-241`:

> the sum is the row count whether or not the run was clean (so the pre-`init` `25 PASS / 4 FAIL`
> shape is held too)

25 + 4 = **29**. The tree registers **28**. Driven on a fenced temp home with this branch's
`bin/fleet.py`, the pre-`init` state is **`24 PASS / 4 FAIL`** (§3.1). So the comment offers, as its
example of a shape the pin holds, a tally the pin would **fail** if it appeared in an entry doc. It
is a leftover of the 29-row world the lane occupied before it removed the row — harmless
operationally (comments are not scanned), but it is a wrong number in prose this commit added,
inside the very block explaining why wrong numbers must be pinned. The neighbouring seed assertion
`find_pass_fail_totals("**25 PASS / 4 FAIL**") == [29]` is correct arithmetic about the *detector*
and is not affected.

### FINDING 4 — MINOR. The safety stanza attributes the throwaway `INSTALL_ROOT` to the wrong step.

`docs/lanes/BRIEF-TEMPLATE.md`, added by this commit:

> Point `INSTALL_ROOT` at a throwaway worktree too, so a **step-4 install-root fallback** cannot
> reach the real home.

True but minor; its load-bearing effect is on **step 2**, not step 4. `resolve_home` calls
`lookup_home_for_sid(sid, population=population, install=install)`, and with no homes list the
population **is the install root alone**. Measured, with my own real sid:

```
step-2 lookup with install=<throwaway worktree>:
   state      : miss
   population : ['C:/Users/Techn/.claude/jobs/3bc4ec56/tmp/doctorwt']
   live home in population? False
```

Consequence, measured: `fleet home` with `FLEET_HOME=<temp>` answered the temp home **whether or not
the sid was present** — because the throwaway install root had already removed the live home from
step 2's candidate set. The stanza's headline (*"that removal, not the env var, is what makes the
fence hold"*) is right about the general case and I would keep it, but a lane that reads the
`INSTALL_ROOT` sentence as being only about a step-4 fallback may skip it in situations where it is
the sentence that matters. One clause.

### FINDING 5 — MINOR / residual, recorded so a later slice does not rediscover it.

With the row parked, **nothing in the tree detects a settings instance that bakes a *foreign* home.**
That is the deliberate consequence of gate 4's disposition and I am not re-arguing it. But the
residual should be stated where the merge can see it: the only remaining channel is
`_doctor_check_instance_freshness`, which compares **mtimes**, not content — the row the removed
docstring itself called *"a known false alarm"* — and it detects a **stale** instance, not a
correctly-fresh instance pointing somewhere else. Since this commit changes
`worker-settings.template.json`, an operator merging it will see `instance-freshness` FAIL and re-run
`fleet init`, which is what makes the baked argv actually reach existing installs. That path works;
it is just not equivalent to what was removed.

---

## 1.1 THE SURVIVOR, ESCALATED

Protocol from the predecessor, kept: *a mutant that goes GREEN on a subset is not a survivor until
the full suite has had its chance.* Q3 was re-planted alone in a detached worktree at `2841c95` and
run against the **full** floor.

```
worktree HEAD 2841c95000fc, bin/fleet.py sha b76dc65d6007ba71
Q3 planted. Running the FULL floor on py 3.13 ...
FULL FLOOR with Q3 on disk -> rc=0  4223 passed, 14 skipped, 1 xfailed in 461.57s (0:07:41)
VERDICT: SURVIVED the full suite
RESTORE: byte-identical=True  git status=clean
```

The clean full floor immediately preceding it was itself green with the identical triple (§4), so
the GREEN is a real survival and not a broken harness.

---

## 2. THE SIX CLAIMS, CHECKED ONE BY ONE

### Claim 1 — the row is removed and the counts return to 28. **CONFIRMED, and stronger than reported.**

```
check_calls registrations (AST): 28
def _doctor_check_* (AST):       28
grep -c '^def _doctor_check_':   28
registered MORE THAN ONCE:       none
DEFINED but NOT registered:      none
REGISTERED but not a def:        none
```

The lane reported the two totals; I also checked they are a **bijection**, which the totals alone do
not give you — a defined-but-unregistered row and a doubly-registered one would cancel out and both
counts would still read 28.

**The removal is COMPLETE to zero residue.** `git grep -i -e 'home.witness' -e 'home_witness'` over
the whole tree at `2841c95`: **no hits at all** outside the two lane reports (28 in `w48-c.md`, 12 in
`w48-gc.md` — history, correctly kept). No orphaned fixture, stub, import, docstring or test name
survives. The `rendered_home` fixture and the `_FakeVersion` stub both left with their only
consumers; `_FakeVersionResult` in `test_native.py` is a distinct symbol and is untouched. The one
grep hit for `rendered_home` is the *name* of `test_the_rendered_home_is_forward_slashed_and_quoted`,
which is about the template's baked home — slice (c)'s actual subject — and is not orphaned.

**The three count-bearing docs are byte-identical to base**, with a control:

```
IDENTICAL  README.md                582449e5c1250ee1dcbdcda77026e23777cba403
IDENTICAL  docs/getting-started.md  b8b8f127f74754b2b34cb7e5e07e95333a684c91
IDENTICAL  docs/launch-readiness.md c051e83570b1145c66c54f438a5b114aaed10543
DIFFERS    bin/fleet.py / BRIEF-TEMPLATE.md / test_doc_claims.py   (control: the comparison can see a change)
```

So the predecessor's finding 1 **did** dissolve with no edit aimed at it. That is a real and
unusual result and it deserves saying: the branch reverted an addition and a self-contradiction
three files away disappeared, because the contradiction only ever existed relative to the number the
addition changed.

### Claim 1, driven. **CONFIRMED exactly.**

Temp home, `CLAUDE_CODE_SESSION_ID` stripped, `INSTALL_ROOT` a throwaway detached worktree, the whole
run gated on `fleet home` invoked identically and compared normalised (§7):

```
BEFORE init : rc=1  rows=28  PASS=24  FAIL=4
              FAILs: worker-settings-instance, instance-freshness, instance-grants, hook-registration
AFTER  init : rc=0  rows=28  PASS=28  FAIL=0
              'home-witness' present in output: False
              row count == AST check_calls (28): True
```

### Claim 2 — the quoting is fixed and pinned by property. **The fix is correct; the pin's reach is finding 1.**

The fix itself is right and I watched it die. All the lane's structural claims about it check out
exactly:

```
placeholders: 12   lines: 6 [9107, 17140, 17168, 17382, 17392, 17394]
functions: ['_render_successor_task', '_render_sup_spawn_task', '_steer_supervisor_release']
```

*"all 12 path placeholders … on 6 lines, in 3 functions"* — **exact**. *"The third function is
`_steer_supervisor_release`, which neither render test can reach … so the source-scan half covers a
site the behavioural half is structurally blind to"* — **true, and Q4 proves it** (unquote it with
its names intact and the scan alone catches it). That is a genuinely good pin design; finding 1 is
that the docstring claims one property wider than the one it implements.

| # | mutant | result |
|---|---|---|
| Q1 | **the exact revert** — all three `_render_successor_task` lines lose their quotes | **RED** — 2 failed, 859 passed. Both halves fire. |
| Q2 | partial revert — only the `sup-status` poll line | **RED** — 2 failed, 859 passed |
| Q5 | the poll line loses the quotes on `--fleet-home "{home}"` instead | **RED** — 2 failed, 859 passed (`test_successor_every_invocation_names_the_home` + the shlex half) |

`tests/test_supspawn_fixwave2.py` stayed green through all of them, confirming §11.1's belief that
the pre-existing quoting class covers `_render_sup_spawn_task` alone.

### Claim 3 — the doc pin was widened. **The widening is real (P1 RED); its reach is finding 2.**

### Claim 4 — the report arithmetic is corrected. **CONFIRMED.**

```
fa236cb 21438      1161d39 21533      2841c95 21453
```

Base **21,438**; delta at `1161d39` **+95**; `21,533 → 21,453` after the 80-line removal. The
original wrong figure (*"21,447 → 21,533 (+86)"*) is quoted in place at `w48-c.md:23` and `:330`.
All correct.

*Non-finding, checked and dismissed:* §14.1 says *"21,451 of 21,534 lines paired"* where I measured
21,450 of 21,533. Both are internally consistent — the difference is `split("\n")` (which yields one
extra element for a trailing newline) versus `splitlines()`. The lane flags the same basis question
itself at `:361`. Not an error.

### Claim 5 — `BRIEF-TEMPLATE.md`. **The lane's correction of the manager's brief is TRUE.**

`git show fa236cb:docs/lanes/BRIEF-TEMPLATE.md | grep -i 'safety\|FLEET_HOME'` → **empty**. There was
no stanza at the branch's base. `44f056e docs(lanes): FLEET_HOME is not a fence -- add the safety
stanza every brief was missing` is real and is on `main`. So following the manager's brief literally
would have shipped a duplicate section and an add/add conflict — exactly as the lane said. The
branch's version is a coherent **superset** of main's (it splits the initialised/uninitialised cases,
adds the normalisation warning, the `INSTALL_ROOT` sentence, and the `not_initialized` receipt); the
diff shows no contradiction, so the conflict resolves by taking the branch. Finding 4 is one clause
inside it.

### Claim 6 — floors. **CONFIRMED exactly, on both interpreters.** §4.

---

## 3. WHAT I TRIED TO BREAK AND COULD NOT

### A. THE CITATION RE-PIN — **CLEARED**, re-derived independently because `bin/fleet.py` moved again.

Masked every integer in both blobs, aligned with difflib, mapped every cited target line through the
alignment, and classified every citation on every paired line.

```
1161d39: 21533 lines   2841c95: 21453 lines   delta -80
aligned (masked) 21450 of 21533 base lines

  ok-rewritten     12
  ok-unchanged     66
  MISSED-REWRITE   0
  WRONG-REWRITE    0
  UNPAIRED         0
```

The shifts are **−74** above the registration removal and **−80** below it (a 74-line `def` plus a
6-line registration), which is what an alignment gives you and a constant subtraction would not. An
AST innermost-`def` map over the new file confirms every named rewrite lands inside the function it
names: **9 named citations, 9 matches.** Control: the checker flags a fabricated
`_doctor_check_autoclean` at `:100`.

*Instrument note:* my named-citation regex initially flagged a tenth, `_acting_worker_identity` at
`:204`. That line is `SPEC.md:204` — a cross-**file** citation my pattern over-matched. My regex, not
a defect; recorded because a gate that hides these is asking to be trusted on the rest.

### B. THE MECHANISM IS BYTE-IDENTICAL — the predecessor's cleared attacks stay cleared **by construction**.

`git diff --name-only 1161d39 2841c95 -- bin/hooks/` is **empty**, so attack B (the exit-0 hook
invariant over 192 runs) has an unchanged subject. AST-extracted, sha256-compared across the two shas:

```
apply_resolved_home              IDENTICAL
resolve_home                     IDENTICAL
TERMINUS_VIEW_VERBS              IDENTICAL
strip_global_fleet_home          IDENTICAL
render_worker_settings_template  IDENTICAL
_render_successor_task           DIFFERS   <- control: the extractor CAN see a change
```

So attack C (the §5 step 1 divergence, CLEARED as pre-existing) and attack F (the sibling fix) are
untouched. I did not re-drive them; I proved their subjects did not move, which is the cheaper and
stronger claim.

### C. THE `+37` LINES IN `docs/specs/multi-fleet.md` — the lane's correction is **TRUE**.

The appended block is the *"SECOND DIVERGENCE RECORD — step 1's refusal is not tiered by verb class
at all"*. Read in full: it names `apply_resolved_home`, `TERMINUS_VIEW_VERBS`, `TERMINUS_EXEMPT_VERBS`
and `_render_successor_task`; it contains `witness` exactly once, inside a quotation of step 1's own
ratified sentence; it never names `home-witness` or `_doctor_check_home_witness`. It documents the
divergence, not the row, and it correctly stays.

### D. THE `"5 occurrences by grep"` DECOMPOSITION — **TRUE.** At `1161d39`, `_FakeVersion` occurs 5
times in `tests/test_hook_fleet_home_argv.py` (one class definition plus four uses), and the removed
block is exactly four tests plus the `rendered_home` fixture plus that stub. Both statements
describe the same removal; the lane's decomposition is the accurate one.

### E. OTHER RENDERED COMMANDS — swept, and the census is **exhaustive**. Six rendered `"command":`
sites in the tree; four in the template, all correctly quoted; one storing an incumbent verbatim;
one bare (finding 1).

---

## 4. THE FLOORS — both reproduce the claim exactly

| run | claimed | measured | verdict |
|---|---|---|---|
| py 3.13 full | `4223 passed, 14 skipped, 1 xfailed` | `4223 passed, 14 skipped, 1 xfailed in 445.08s` | **HIT** |
| py 3.10 full | identical triple | `4223 passed, 14 skipped, 1 xfailed in 424.75s` | **HIT** |

Run in a detached worktree at `2841c95` with **no mutant on disk** and no mutant ever planted there
before the runs completed. `CLAUDE_CODE_SESSION_ID` stripped and `FLEET_HOME` unset, so the suite's
step-4 fallback resolved to the throwaway worktree rather than to any real home.

**Tree integrity proven around every run.** A sha256 manifest of all **233** tracked files, taken
before the first collect and re-taken after every collect and every floor — `5d7b3511df93c3c8…6f0`,
**identical five times out of five**. `git status --porcelain` empty after both floors.
`worker-settings.template.json` — the file the suite has previously overwritten with `{}` —
byte-unchanged at `8091bbf055e6c16ec07e2503b306fb954cb58c941dc2b5c067c325f7173e7fbe`, the same sha the
predecessor recorded.

**Collection, derived on both sides, and the delta fully attributed:**

```
fa236cb  TOTAL 4154   test_hook_fleet_home_argv  (absent)   test_doc_claims 20
1161d39  TOTAL 4232   test_hook_fleet_home_argv        78   test_doc_claims 20
2841c95  TOTAL 4238   test_hook_fleet_home_argv        78   test_doc_claims 26
```

4238 = 4223 + 14 + 1, on both interpreters. The **+6** is entirely `test_doc_claims` — the new tally
test parametrized over the 6 `ENTRY_DOCS`. And the argv file stays at **78 across a 225-line diff**,
because the four removed witness tests are exactly offset by four added quoting tests (two
parametrized cases, the scan, the seed). Nothing was quietly dropped.

---

## 5. THE TRIAL MERGE — re-run, with a known-non-zero control first

`git merge-tree --write-tree` does not exist at git 2.34.1 (`git version 2.34.1.windows.1`), so both
merges were **real merges in detached worktrees**, moving no ref and creating no branch.

**Control first**, as the brief requires — `main` × `w35/nd4c`:

```
conflicted files: 6   (bin/fleet.py, docs/NEXT-SESSION.md, knowledge/INDEX.md,
                       knowledge/lessons.md, tests/test_identity_registry.py, ...)
total '<<<<<<<' markers: 26 across 6 files
```

**26** — which reconciles the brief's remembered figure exactly and calibrates the instrument.

**Subject** — `main (1c0d807)` × `w48/hookargv (2841c95)`:

```
Auto-merging docs/lanes/BRIEF-TEMPLATE.md
CONFLICT (content): Merge conflict in docs/lanes/BRIEF-TEMPLATE.md
conflicted files: 1
```

**Exactly one conflict, in exactly the one file the lane named.** And the three count-bearing docs
did better than "auto-merged clean" — the merged blobs are **byte-identical to main's own**, i.e. the
branch contributes nothing to them at all:

```
README.md                -> merged == main's blob
docs/getting-started.md  -> merged == main's blob
docs/launch-readiness.md -> merged == main's blob
```

Because `main` already states **28** at all its sites, including `28 PASS / 0 FAIL` in both
`getting-started` and `launch-readiness`. Resolving the one conflict by taking the branch (the
superset) leaves **zero markers**, and on that merged tree:

```
pytest -q tests/test_doc_claims.py   ->  26 passed
merged tree check_calls (AST)        ->  28
```

**Green AND correct**, where the predecessor measured the merged tree green-and-wrong. That is the
single clearest improvement this discharge makes and it is worth as much as either finding.

### Cross-branch coherence with `main`'s gate 4

`main` moved to `1c0d807` — *"open gate 4 — slice (c)'s `witness` names nothing the spec defines"* —
and the two are coherent, which I checked rather than assumed. Gate 4 records the disposition the
branch implements (*"the row does NOT land … preserved intact on branch `w49/home-witness` with a
re-land recipe at `docs/lanes/w49-home-witness.md`"*), and both halves of that are real:
`w49/home-witness` is `da04c80`, the recipe file is present, and the row is still in its `bin/fleet.py`.

One live pointer worth naming for the merge: gate 4 cites its evidence as *"`docs/lanes/w48-gc.md` on
branch `w48/gc`"*, which is an unmerged branch. Landing **this** branch resolves that pointer, because
`w48-gc.md` rides along on it. That is exactly what the manager's cherry-pick was for, and it works.

---

## 6. INSTRUMENT FAILURES — two, both caught by a control before they became findings

1. **My first statusline control failed the same way the subject did.** `cmd /c` mangled the quoting
   so the *control* also errored — the identical trap the predecessor hit in its attack F. I knew the
   instrument had failed rather than the subject because a control that fails is not a control. Redone
   through a `.bat` and with a marker script, after which subject and control separated cleanly
   (rc=127/rc=1 marker=False vs rc=0 marker=True on both shells).
2. **My first planter's mutant patterns were destroyed by shell escaping.** The `\\n\\n` inside a
   quoted heredoc reached Python as real newlines, so the Q3 pattern matched **0** times. The planter's
   `occurrences == 1` precondition **aborted before running the suite** — the guard working, not a
   survivor. Had it asserted nothing, Q3 would have "passed" against an unmutated file and I would
   have reported a false GREEN. Rewritten with AST-scoped line surgery and no escapes.

*Non-fatal:* my named-citation regex over-matched a cross-file `SPEC.md:204` citation (§3.A).

**Tree integrity across all 10 plants** (Q1, Q2, the aborted partial Q3, then Q3, Q4, Q5, P1, P2, P3,
and the Q3 escalation)**:** every restore verified sha256 byte-identical against a
baseline taken before planting, `git status --porcelain` re-checked clean after each, and both trees
(`fleet-w49-gc2` and the floor worktree) confirmed clean at exit. No floor run was ever started with
a mutant on disk.

---

## 7. SAFETY LEDGER — every `fleet` command, the home it touched, and **why** I was contained

| command | count | home it touched |
|---|---|---|
| `fleet home` | 2 | temp home — used as a **gate**: nothing else runs unless this, invoked with the identical env, prints the temp path |
| `fleet doctor` | 2 | temp home (once before `init`, once after) |
| `fleet init` | 1 | temp home. **No `--home`**, **no `--statusline`** |
| `fleet home --fleet-home <fresh temp>` | 1 | a fresh temp dir; **refused** `not_initialized`, created nothing |

Every invocation ran as a child process with `CLAUDE_CODE_SESSION_ID` **removed** and `FLEET_HOME` set
to the temp home, executing `bin/fleet.py` out of a **throwaway detached worktree**, never the live
install root.

**Why I was contained, not merely that I was.** Three layers, and I can name which one actually did
the work — the brief is right that this is the part a clean audit does not prove:

1. **The throwaway `INSTALL_ROOT` is the layer that held.** `lookup_home_for_sid` builds §5 step 2's
   candidate set from the install root plus the homes list. Measured, with my **real** sid
   (`3bc4ec56…`): `state: miss`, `population: ['…/tmp/doctorwt']`, live home in population: **False**.
   The live home was never a candidate, so step 2 could not answer and step 3 (validated env) did.
2. **The sid removal is the layer that would have held if the homes list existed.** It did not do the
   work here — the discriminator shows `fleet home` returned the temp home with the sid **present**,
   because layer 1 had already emptied the population. It is still correct to do, and it is the layer
   the branch's own stanza names.
3. **The absent homes list is an unearned third layer.** `~/.claude/fleet-homes.list` does not exist on
   this machine — **ABSENT before and ABSENT after**, snapped and compared programmatically. One
   `fleet homes --add` and layer 3 is gone; my run would still have been contained by layer 1, and
   *that* is why I point at layer 1 rather than reporting "the audit came back clean."

- **`fleet init` was NEVER run against `C:/proga/claude-fleet`.** No command in this gate named it.
- **`~/.claude/settings.json` unchanged** — `(mtime_ns, size, sha256)` identical before and after:
  `1786210442854189800, 1859, 578bde7b898c6011`. `_install_statusline` was exercised **in-process only**,
  with `user_settings_path` monkeypatched to a temp file; the `fleet init --statusline` CLI was never run.
- **`state/pin-pass.json` not stamped.** No statusline installed. No spawn into any project. No push,
  no merge committed, no ref moved. `supervisor/GOALS.md` and `docs/OPERATOR-GATES.md` untouched.
- **Six** detached worktrees under the job tmp dir (`floor`, `doctorwt`, `mrg`, `mrg-ctl`, `mrg-ctl2`,
  `mrg-ctl3`) created no branch and moved no ref; every merge was aborted or left uncommitted, and all
  six were removed at exit.
- Litter: a venv and temp homes under `$CLAUDE_JOB_DIR/tmp`, which the job cleanup removes.

---

## 8. WHERE THIS BRIEF WAS WRONG, AND WHERE IT WAS RIGHT

**Right, and it set my order of work:** *"gate on RE-PLANT, not on the diff"*, and *"a self-reported
fault-injection table is a claim about the mutants its author thought of."* Finding 1 exists only
because I built a mutant aimed at the *instrument* rather than at the code — Q3/Q4 differ in nothing
but a variable name.

**Right, and it was not rhetorical:** *"assume there is a ninth and go find it."* There is one, and
it is in the spec of record.

**Wrong 1 — "the removal is the risky part; the new pins may be."** The brief hedged and the hedge
was correct: the removal is flawless (zero residue, perfect bijection, docs byte-identical to base),
and **both** MAJORs are in the new pins. The riskiest thing in this commit was the instrument, not
the deletion.

**Wrong 2 — the census framing.** The brief said six, the lane said eight, and I say the lane's eight
is *right for the question it answered*. The ninth is a different question — a pre-existing stale
site the branch never had to touch. Reporting it as "the lane undercounted" would be unfair, so I
have not.

**Wrong 3 — "re-check the citation re-pin, since `bin/fleet.py` moved again."** Correct to ask, and it
came back clean: 12/66/0/0/0. Worth the run precisely because it could have gone the other way.

**Wrong 4 — nothing here supports a BLOCKING grade, and I want that said as plainly as the findings.**
The mechanism this slice ships is unchanged from the tree the predecessor cleared; the removal is
complete; both floors hit; the merge is now green *and* correct where it was green *and wrong*. The
corroborating measurement the brief mentions from the statusline lane — `FLEET_HOME` not reaching a
`--bg` child, so a worker's Stop hook wrote to the wrong home — is the defect this baked argv closes,
and nothing I found argues against landing it. **Manufacturing a BLOCKING result to match a precedent
would have been this gate's failure mode**, and the two MAJORs I do have are both one line of code
plus one regex away from cleared.

---

## 9. WHAT I DID NOT DO

- Did not re-run the predecessor's 192-run hostile-argv drive (attack B). I proved its subject is
  byte-identical instead — `bin/hooks/` is untouched between `1161d39` and `2841c95`.
- Did not re-drive attack C's two-home world. Same reason: `apply_resolved_home`, `resolve_home` and
  `TERMINUS_VIEW_VERBS` are byte-identical across the discharge.
- Did not run the full floor on `py -3.10` with Q3 planted; the survivor was escalated on 3.13 only.
  The mutant is a pure-Python string change with no version-sensitive behaviour, so I do not expect a
  difference — but that is DERIVED, not measured.
- Did not run `fleet init --statusline` as a CLI anywhere. Finding 1 was driven by calling
  `_install_statusline` in-process with `user_settings_path` redirected.
- Did not fix anything. Nothing outside `docs/lanes/w49-gc2.md` on `w49/gc2` was changed; every mutant
  was restored byte-identically and verified against a pre-planting baseline.
