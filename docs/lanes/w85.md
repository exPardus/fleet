# w85 — the 400k context ceiling was unreachable for a Codex-dispatching supervisor
DONE means: a supervisor's context occupancy and band verdict are recorded on its incarnation
record, published by `sup-status --json`, and a held, fresh-heartbeat, over-band claim makes
`sup-guard` return PAGE — the keeper catches an over-ceiling body without its cooperation.

WRITTEN BY THE LANDING SUPERVISOR. Lane mcx `KRGcS0PG` (luna) hit the Codex usage limit mid-run
and died `failed`, code complete — so no number below is inherited from it.

## Shipped
`sup-checkpoint`/`sup-heartbeat` measure the CALLER's transcript inside the lock they already
hold and persist `context_occupancy`/`context_verdict`/`context_measured_at`; an unreadable
transcript gives null occupancy AND null verdict, never a fabricated one. `_project_claim`
publishes all three. `_sup_guard_decide`: `held` + fresh heartbeat + `over-band` → `PAGE` naming
occupancy and the ceiling. No sixth ceiling call site; the guard stays a view.

## Why (MEASURED at 87c0b6a)
`_ceiling_refuses_dispatch` is the only 400k enforcement and its census is pinned at exactly five
(`tests/test_respawn_ceiling.py:187`) — all five NATIVE dispatch verbs. Under the Claude freeze
every lane is mcx, so a supervisor calls none, and `sup-checkpoint` computed the verdict only to
discard it into a header word. tap proved it: 677k, 1.7x the ceiling, caught by a person.

## Controller corrections to the lane diff
**The lane's arm was a downtime regression; fixed.** It fired ahead of every liveness arm, but the
recorded verdict outlives the body that wrote it — so a supervisor that DIED over-band, the
likeliest end for one, kept the verdict and the guard answered `PAGE` where it had answered
`DISPATCH`, leaving the dead body waiting for a human instead of replaced. It now requires a fresh
heartbeat too; a stale over-band claim falls through unchanged. MEASURED BY MUTATION: drop that
condition and `test_a_dead_over_band_claim_still_dispatches_a_replacement` fails. Also added that
pin (the lane tested only the live case), reverted an indent slip, re-ran the self-citation tool,
and fixed the §11.3 box, which described the arm without the liveness requirement.
## Tests, MEASURED by the landing supervisor
Targeted suites, `UV_CACHE_DIR=/home/altai/.cache/uv UV_OFFLINE=1`: **775 passed, 2 skipped** on
3.10 AND 3.12. Pre-existing at base, untouched source, NOT adopted — full floor 3.10: 6 failed,
5322 passed. docs: `three-tier-command.md` §11.3, `PLAN-PROGRESS.md`.
## Where this brief was wrong, and what is NOT verified
"Existing arms keep their precedence" and "place it adjacent to the `limited` arm" conflict:
`limited` leads every liveness arm, so obeying the placement broke the precedence and the lane
shipped the regression. The brief should have named what the arm must NOT preempt; it also priced
no Codex limit six days from reset. NOT verified: no body actually crossed 400k, nor the keeper's
end-to-end behaviour on the new `PAGE` reason.
