# Lane w77 — keeper multihome

DONE means: `fleet_keeper.py` accepts repeatable `--fleet-home`; one `--once` tick observes, wakes, and pages each home independently; page/wake lines carry `[home-tag]`; the unit template lists fleet and tap; targeted one-home and two-home tests exist; keeper suites are green on Python 3.10 and 3.12.

- Implemented repeatable `--fleet-home` processing with per-home failure isolation.
- Preserved the single-home imported-home guard and no-argument snapshot test seam.
- Multi-home snapshots use read-only `status --stale-ok --json` subprocess reads; the keeper never rebinds `fleet.FLEET_HOME` or spawns supervisors.
- Every delivered/dry-run page and wake report is prefixed with `fleet.home_tag(home)`, the existing statusline tag (four hex digits of the normalized home identity).
- Dedup state remains separate at each home’s `state/keeper/last-page.json`.
- Updated `docs/operator/systemd/fleet-keeper.service` only; operator must apply it.
- Updated the fleet skill keeper line and added targeted one-home/two-home coverage.
- Checks: Python 3.10 keeper suites — 171 passed; Python 3.12 keeper suites — 171 passed.
