# Lane w74d — docs archive move
DONE means: active docs stay under 15,000 lines; archive material is under docs/archive; archive is ignored by current-tree audits; the six documentation suites pass on Python 3.10 and 3.12.

- Moved campaign history verbatim under `docs/archive/`: lanes, reviews,
  decisions, superpowers, `SPEC-v2-history.md`, and secondary receipt specs.
- Kept `docs/SPEC.md`, `docs/OPERATOR-GATES.md`, operator docs, and the active
  receipt-bearing specs: claim-nonce, native-substrate, terminal-surface, and
  three-tier-command.
- Active docs count: 14,539 lines, excluding `docs/archive/` (report included).
- `tests/test_prose_caps.py::test_docs_total_cap` is now a hard assertion.
- Current-tree claim/currency scans exclude `docs/archive/`.
- `verify_receipts.py` ignores archive paths; active receipt tests retain host
  and repository-specific receipts.
- The linked worktree index is parent-owned and read-only here, so `git mv`
  could not run. Files were moved unchanged with `mv`; parent staging will
  preserve rename detection/history.

Checks:

- Python 3.10: six-suite run passed; 186 passed, 2 skipped, 3 xfailed,
  1 xpassed.
- Python 3.12: six-suite run passed; 186 passed, 2 skipped, 3 xfailed,
  1 xpassed.
- Strict receipt self-test on active specs passed on Python 3.10 and 3.12.

PATH LIST:

- `docs/archive/`
- `docs/lanes/w74d.md`
- `tests/test_docs_currency.py`
- `tests/test_doc_claims.py`
- `tests/test_prose_caps.py`
- `tests/test_receipts.py`
- `tools/verify_receipts.py`
- `state/journals/w74d.md`
