# w98 — item29-native-codex-lanes

DONE means: `fleet spawn --model codex:<model>` dispatches a Codex lane through
mcx, every fleet verb routes on it, and the stub-mcx suite proves it.

## Result: DONE, no blockers

MEASURED — `dispatch_codex` (bin/fleet.py:1752) writes the task file and runs
`mcx spawn -m <bare> "<read the task file>"` with cwd=lane worktree and
`MCX_DIR` pinned to `<worktree>/.mcx`, so every later verb finds the job from
the registry row's `mcx_id` alone. `new_worker_record` gained `mcx_id`, and
`cmd_spawn` routes to `_cmd_spawn_codex` inside the same rollback envelope as
the native path, so a missing mcx or a non-zero spawn rolls the row back.

MEASURED — `recompute_worker` (:2647) is now a substrate router. Codex rows
probe `mcx result` (2 working, 0 idle, 1 dead) and a probe that cannot run keeps
the committed status, so G9's never-assume-dead-on-ambiguity applies to mcx too;
`status`/`wait`/`clean` skip the roster fetch when every active row is codex.
`peek` tails the job log, `result` prints `mcx result`, `send` is `mcx steer`
over stdin and refuses a live turn (RESTARTS wording) because mcx has no mailbox
to queue into, `interrupt`/`kill` are `mcx stop` (kill counts "unknown worker"
as gone), `respawn` stops the old job with `--force` and stamps a new `mcx_id`,
and wave accounting prefers the registry id over the glob.

MEASURED from mcx's launcher source (no live probe: the Codex weekly limit is
exhausted until 2026-09-20T18:16Z): mcx runs `codex exec -a never --sandbox
workspace-write`, confining writes to the worker's cwd, and a worktree's gitdir
is under the main repo's `.git/worktrees/`, outside it — so a Codex worker
cannot commit. `fleet land` already commits a dirty worktree's listed files, so
it commits on the worker's behalf; no fleet.py change was needed. 28 tests in
`tests/test_codex_substrate.py` drive a stub `mcx` on PATH with FLEET_HOME
redirected and `MCX_DIR` poisoned. Docs: `skills/fleet/SKILL.md` — the mcx
section is now "Codex lanes".

## WHERE THIS BRIEF WAS WRONG

Nothing measured contradicts it: 28 codex tests green on both interpreters, the
5-file regression at 476 passed / 3 skipped on both, the commit verdict held.
Its one load-bearing warning held too — the floor's non-zero rc is the six
documented host-venv artifacts, so it is a claim, not a `tests[]` entry.
