# Lane w83 — computed supervisor board
DONE means: `sup-checkpoint` and the boot bundle compute trusted, bounded board state.

MEASURED — `sup-checkpoint` refuses a four-line body before claim/journal work, reporting `4 lines` and maximum `3`.
MEASURED — the real bundle from `/home/altai/proga/fleet` rendered branch, HEAD, dirty/unpushed, pickup, live lanes/worktrees, standing directives, and memory/lane/occupancy gates in 12,626 characters.
MEASURED — unreadable registry, journal/task directories, git, worktrees, `free`, and transcript sources are labelled `UNREADABLE`/`UNMEASURED`, never replaced with plausible values.
MEASURED — bundle growth is refused above `SUPERVISOR_BUNDLE_MAX_CHARS = 40_000`; output is not silently truncated.
MEASURED BY THE SUPERVISOR AT LANDING — 866 passed, 1 xfailed on Python 3.10 AND 3.12 across the ten derived suites, after the corrections below. The lane could not run pytest at all, so nothing it wrote had been executed.
CONFIRMED — the lane's sandbox had no DNS, so it shipped unexecuted tests. Two of them were broken: its `run` fake did not handle `git symbolic-ref --short HEAD`, and its `free -m` fake printed seven numbers where the real command prints six.

WHERE THIS BRIEF WAS WRONG

The brief said the real bundle was roughly 3.5k tokens; after adding the board, this checkout measured 12,626 characters, still well below the 40k cap. The live registry currently contains no non-supervisor lane records, so the exercise could not measure a populated live-lane/worktree row.

docs updated: `docs/lanes/w83.md`, `docs/lanes/w83.json`; no owning SPEC section changed.

## Four defects the supervisor found by running what the lane could not

1. **Live directives were silently dropped.** The discharge test was an unanchored
   `\bDISCHARGED\b`, which matches the words "not discharged". MEASURED against
   `state/tasks/20260911-computed-checkpoint.md` — not a directive, classified as a
   discharged one. Both markers are line-anchored now; the directive marker must be
   in a HEADING.
2. **`RULED` was treated as a discharge.** An operator ruling answers a question; it
   does not finish the work. MEASURED: the batch-2 directive says `RULED:` and still
   had item 4 — this lane — in flight while the board hid it. Only `DISCHARGED`
   discharges; all three real directives now surface.
3. **The board read the process cwd, not the home.** Both helpers defaulted to
   `Path.cwd()`. An unreadable source says so, but a DIFFERENT git repository answers
   every question plausibly and wrongly — the one failure a board built on trust
   cannot have. Both default to `FLEET_HOME`.
4. **`_supervisor_live_lanes` was undeclared to the liveness census.** It consumes a
   liveness verdict rather than producing one; now in the exclusion list with that
   reason. `tools/repoint_self_citations.py 638878b` moved 10 of 53 citations the
   lane never repointed.

Exercised live against `/home/altai/proga/fleet`, not only unit-tested: three
undischarged directives, a three-item pickup list, bundle 9,377 chars under the 40k
cap, and the four-line body refused BEFORE the lock — so a refusal neither rotates
the nonce nor files a false second-body row.
