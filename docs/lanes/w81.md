# Lane w81 — context-band dispatch gate
DONE means: `_ceiling_refuses_dispatch` now reuses `supervisor_band_verdict` and refuses the supervisor soft band (350,000) as well as the hard ceiling (400,000).

DONE — `--force-band` is available on `spawn`, `send`, `respawn`, and `sup-spawn`; it overrides only a soft-band refusal. Hard-ceiling refusal, unknown occupancy, identity rules, and handoff exemptions remain fail-safe.

DONE — `sup-checkpoint` prints the caller occupancy and supervisor verdict in its header line.

MEASURED — no mcx dispatch helper exists: `bin/fleet.py` only reads `.mcx` records for token accounting; mcx lanes remain supervisor-shell dispatches and are not represented as a fifth path.

Checks: Python 3.10 code suites: 999 passed, 4 skipped; Python 3.12: 999 passed, 4 skipped. Docs suites: 185 passed, 2 xfailed on both versions.

Baseline: the untouched `10c24f3` self-citation suite reproduces cleanly (19 passed); final source-anchor checks also pass. No blockers.

PATH LIST:
- `bin/fleet.py`
- `tests/test_supervisor_ceiling.py`
- `docs/lanes/w81.md`
- `docs/lanes/w81.json`
