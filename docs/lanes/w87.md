# w87 — handoff break-even: the curve, and the two free wins

DONE means: a measured break-even curve for bands 150k/200k/250k/350k weighing the handoff ritual's per-generation cost against the per-turn cache_read growth it avoids, so the operator can see whether a lower band saves anything; plus the two no-handoff cuts landed and pinned by tests.

## The curve (full tables/scripts: `docs/lanes/w87-appendix.md`)

As the lane solved it (`C = B*`): break-even **B\* ≈ 850,000** from C = 849,667 MEASURED (one
clean 16-turn sample; per-request cache_read at context X is ≈X across 6 generations).

Lane's ratios C/B (REJECTED, see below): 150k 5.66x, 200k 4.25x, 250k 3.40x, 350k 2.43x — it read this as 350k least bad.

**THE SUPERVISOR'S GATE REJECTED THE RANKING ABOVE; C survives, the ordering does not.** It
credits a band with avoiding ONE turn and calls that the most favorable framing for a low band —
it is the LEAST: a band caps every request for the generation's whole life. Cumulatively
(per-request cost ~ context, growth k) the optimum is B\* = sqrt(2kC) = 41k-184k, and total cost
RISES with B: at k=5000, 150k costs 103,322 per unit work vs 350k 187,138 and 850k 429,998 —
150k BEST of the four, the exact inverse. Two defensible models invert each other, so no band
moves on this pass (appendix §4b). Cumulative totals per unit work at k=5000: **150k 103,322 |
200k 121,242 | 250k 141,993 | 350k 187,138 | 850k 429,998** — the operator's four bands, ordered
opposite to the lane's. Both models agree C ~ 850k/handoff is large, which is what part 2
attacks. Cut A stays STAGED, correctly.

## Part 2 — the two no-handoff cuts, landed and pinned by tests

1. **Wake resumes, never forks** (`_wake_supervisor_native`). An idle supervisor's `fleet send`
   wake dispatches fresh instead of forking the transcript; the body proves continuity via a
   minted nonce and `sup-boot` restamps the SAME incarnation_id, minting no generation. Worker
   fork-steer unchanged. MEASURED: a6c9's 4 fork-parent transcripts are 7,553,045 bytes, 76.4% of
   that generation's on-disk bytes.
2. **Successor boot was already checkpoint-only.** `cmd_sup_handoff_begin` dispatches fresh and
   the boot bundle reads GOALS + journal tail + knowledge index + status, never a transcript —
   no code change needed; added the missing pinning test.
Tests: 7 new (`test_supervisor_wake.py`), 1 new Cut-2 pin, 2 updated (`test_wake_retirement.py`
pinned the old forked behaviour), 6 census suites extended. **The lane's "0 failed" was a 2,536-
test SUBSET, not the floor**: the full floor showed 8 citation suites red because ~185 added
`bin/fleet.py` lines were never re-pointed. The supervisor ran `repoint_self_citations.py 2127983`
(40 of 53) and re-verified; my brief omitted the template's line-count row, my defect not the
lane's. NOT this lane: `20260915-guard-blocked-corpses.md` still needed — Cut 1 stops NEW corpses
but retro-cleans nothing and leaves `sup-guard`'s predicate unfixed.
