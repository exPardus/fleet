# w100 — item 31: `fleet respawn` accepts a turn that died mid-stream

DONE means: `fleet respawn` accepts a dead-suspected OpenRouter/native row (no outcome record + transcript-tail API error) without `--force`, with tests that fail against the pre-change tree on 3.10 and 3.12.

## Result: DONE, no blockers

Lane commit on `w100/respawn-dead-suspected`, amended from `041328b` to trim
this report. Not merged.

**Defect (measured 2026-09-18 ~16:15Z).** w98c (kimi-k3) ended turn 1 with
transcript tail `API Error: stream closed before completion`. The session
process outlived its stream, so no Stop hook ran and no outcome record exists.
`fleet status` recomputed the row `dead-suspected` while `fleet respawn`
refused `turn is running -- pass --force`: its guard asks Q1, which counts any
keyed roster entry as a live body. The safe verb demanded the unsafe flag.

**Shape (a).** `transcript_stream_death_scan` walks the 64 KB transcript tail
newest-first, exactly as `transcript_limit_scan` does, and answers True only
for an `isApiErrorMessage` record whose text names a stream death.
`_dead_suspected_hung_stream` accepts the row only when the SAME probe the
status table asks concludes `dead-suspected` AND that tail evidence exists; the
acceptance is announced on stderr. The old session is still stopped, tombstoned
and re-verified — the flag was never the safety property, the stop is. The
refusal survives for a running turn: a roster busy/waiting entry, a 429
`limited` tail, no transcript evidence, or chatter after an older death. The
marker set is the measured family (`stream closed before completion`,
`Connection lost mid-response`, `Server error mid-response`, `The response
stopped arriving`); 429, 402 and `model_not_found` are excluded.

**Tests.** `tests/test_respawn_dead_stream.py`, 12 tests: **12 failed** against
the pre-change tree with the final test file in place, 12 passed after it, on
3.10 and 3.12. Full floor both interpreters: `5454 passed, 6 failed, 16
skipped, 3 xfailed` — the 6 are the documented pre-existing host-venv
artifacts, not adopted. The ~89-line insertion re-pinned 44 self-citations via
`tools/repoint_self_citations.py HEAD`.

Everything else — the per-claim evidence, the SPEC edits, the scope fence —
is in `docs/lanes/w100.json`.
