# Lane w82 — measured throughput
DONE means: `wave-close` emits measured token/bin-line and external-line fields in both throughput surfaces.

MEASURED — Codex usage is read once per `result` from the landed lane worktree selected by `_wave_lane_worktree`; prose results fall back to the final `events.jsonl` usage.

MEASURED — Claude usage falls back from the roster to `state/outcomes/*.jsonl`, joined to landed lanes by worktree and current registry `session_id`; retired and unrelated sessions are excluded.

MEASURED — Claude totals include input, output, cache-creation, and cache-read token fields. Missing fields remain `UNMEASURED`, never zero.

MEASURED — `tokens_per_bin_line` divides total measured tokens by added `bin/` lines. Zero added lines remain `UNMEASURED`.

CORRECTED BY THE SUPERVISOR AT LANDING — the lane shipped `external_lines` as a hardcoded string asserting a measured zero, which its own WHERE-THIS-BRIEF-WAS-WRONG section contradicts. It is now computed by `_wave_external_lines`: a lane `_wave_lane_worktree` resolves is a worktree of THIS repo and landed here; a lane it cannot resolve is UNMEASURED and named. Neither new field had a test; three now pin it.

MEASURED — the same throughput string is prepended after the CHANGELOG title and written to the supervisor journal.

MEASURED — focused suites pass on Python 3.10 and 3.12: 143 passed each after the landing corrections (139 as the lane left it).

WHERE THIS BRIEF WAS WRONG

The brief implied a durable source for external-repository line attribution. No such source exists in this checkout; git numstat cannot observe work outside its repository. The zero above is measured only as “no externally attributable lines in this repository diff,” not evidence about untracked external work.

docs updated: this lane report and result only; no owning SPEC section or PLAN-PROGRESS row exists for this new accounting field.

Blocker: none for the observable current-repository output. External-repository attribution remains unmeasured absent a durable landing receipt carrying repository identity and line counts.

## Supervisor landing note

The supervisor's first correction was itself wrong and was caught by exercising the
verb rather than by its own passing test: a lane worktree is a SIBLING of the repo
root (`/home/altai/proga/fleet-w82-throughput` beside `/home/altai/proga/fleet`), so
a path-prefix test called this very lane external. `git worktree list` enumerates
only this repository's worktrees, so resolution is the answer and containment is not.
The test that passed encoded the same wrong model; the live call did not.

MEASURED live, before landing: `_wave_codex_tokens` returns **2,734,534** for this
lane from `/home/altai/proga/fleet-w82-throughput/.mcx/iudZ2FAd/events.jsonl`.
Four waves of `tokens: UNMEASURED` were a wrong-directory read, now proven by number.
