# w93 — item24-openrouter-substrate

DONE means: `fleet spawn --model openrouter:<slug>` really runs against
OpenRouter, records `substrate: openrouter/<slug>`, wave-close counts it as
cost 0 not UNMEASURED, and the key never appears in any file fleet writes.

## Result: DONE, no blockers

THE FORK, settled by experiment against the real resident daemon: `--bg`
hands the session to the daemon's pre-warmed spare pool, so env set on the
dispatching process (`_worker_env`'s shape) never reaches it — proven with a
probe var the session's own `env` reported absent. A `--settings` `env`
block does reach the session, but values are literal — `$VAR` never expands,
also proven directly. `apiKeyHelper` (a shell command the session runs
itself, at auth time) works: pointed at the key file, never its value, it
dispatched for real against `stealth/union-alpha`, fixed two bugs in a
scratch file, and the session's own transcript reports
`model: "stealth/union-alpha"` on every turn — the substrate's own witness.
Implementation: `_openrouter_dispatch_args` (bin/fleet.py, near
`_worker_env`) parses the `openrouter:` prefix, refuses when
`~/.config/openrouter/env` is missing/unreadable (naming the path), and
merges an in-memory `env`+`apiKeyHelper` block onto the base settings for
one dispatch — the file on disk is never touched. Wired into both native
dispatch sites: `dispatch_bg` and `cmd_sup_handoff_begin`. `new_worker_record`
gained a `substrate` field, set by `cmd_spawn`/`_cmd_respawn_native`.
`_wave_record_substrate` now recognizes `openrouter/<slug>` instead of
misclassifying it as `"claude"`, so wave-close's existing accounting already
reports a bare `"0"`, not `UNMEASURED`, for an OpenRouter-only wave.

21 new tests (`tests/test_openrouter_substrate.py`) plus 3 in
`test_wave_close.py` and a schema-enum fix in `test_core.py`. Full floor
green on 3.10/3.12 (only the 6 pre-existing host-venv failures). My
~131-line insertion shifted every self-citation below it; recomputed and
fixed (`test_self_citations.py`/`test_retired_sid_citations.py` green).

Docs: n/a — SPEC.md/SKILL.md already describe `--model` generically; SPEC.md
is a receipted snapshot a maintenance lane refreshes, not each feature lane.
Out of scope, noted: supervisor-tier spawn (`cmd_sup_spawn`) gets no
`substrate` field (not an OpenRouter target per the operator ask) — only the
dispatch wiring needed to work there too, and does.
