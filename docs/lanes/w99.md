# w99 — items 26 + 16: the merge subject that could not join, and the join key the tidy-up deleted

DONE means: a lane merged with the subject `fleet land` prints is joined by `wave-close` to its registry record, so its substrate, tokens and lines are measured rather than published as `unknown`/`0`; a lane that parses but joins to nothing is refused as `UNJOINED`; and the join survives `git worktree remove`.

## Result: DONE, no blockers

Four changes, all in `bin/fleet.py` and `bin/fleet_land.py`.

1. **The subject carries the branch (26-i).** `fleet land` printed `merge({lane})` (short lane) beside a command that merges `{lane_branch}`, and `_wave_lane_worktree` looks up `refs/heads/<lane>`. It now prints `merge(w99/lane-join): <summary>`: printed and run agree.
2. **The record keeps the branch (16).** `new_worker_record` gained `branch`, written at `spawn` and `respawn` from the worktree's own `.git`/HEAD file (stdlib read, no subprocess on the dispatch path). `_wave_lane_join` joins a merge subject to that branch first, to the worktree `cwd` second, so a lane stays attributable after the routine tidy-up prunes its worktree.
3. **A parsed lane that joins to nothing refuses (26-ii).** `wave-close` now raises `UNJOINED: N of M` beside item 14's `UNPARSED` refusal, before the claim, the reap and the floor.
4. **An unreadable substrate is not a zero (26-a).** A lane whose substrate reads `unknown` makes `tokens` and `tokens_per_bin_line` UNMEASURED, and `_wave_outcomes_claude_tokens` returns UNMEASURED rather than `0`.

Wave 86's line (`workers: 2 (w93: unknown, w92: unknown); tokens: 0; tokens_per_bin_line: 0.00; ... reaped: 0`, on a 701-line wave) is now unreachable three ways: the subject joins, the join survives the prune, and a join that still fails refuses instead of publishing a zero.

Model: `openrouter/deepseek/deepseek-v4.1-flash` (the registry row's substrate).

## Verified

Nine new or changed tests, each red against the pre-change tree (a `git archive HEAD` copy): eight fail for the missing join or the missing refusal, and the substrate test fails on a real assertion (`unknown` versus the recorded substrate). Item 19's stop-then-reap arm is exercised by the branch join in `test_mark_landed_lanes_joins_by_branch_after_the_worktree_is_removed`.

## For the supervisor

- One wave of exposure: rows written before this lane (w97b/w98b/w98c) have `branch: null`, so a lane whose worktree is pruned before the close would now REFUSE it. Keep lane worktrees until after `wave-close` this wave, as the campaign already instructs; lanes spawned after this lands carry the branch and need nothing.
- `land`'s docs-currency check fails if the shell exports `FLEET_HOME`: item 13's pre-existing defect (spawn's preamble precedes the title in `state/tasks/w99.md`, so the DONE-means lint refuses a file spawn wrote). Unset, it passes, as in the floor's fresh clone. Item 13 belongs to item 6.
