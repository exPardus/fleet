# Lane w84 — the homes inventory and the arming guard count the same homes
DONE means: `fleet homes` renders the resolution population with provenance, and no refusal states one count while embedding another.
The lane's sandbox had NO DNS, so it executed no pytest; the supervisor ran every suite at landing.

MEASURED — baseline (`c05d6c3`) `fleet homes`:
```text
fleet homes: /home/altai/.claude/fleet-homes.list
  c3e5  /home/altai/proga/fleet-dogfood  ok (1 worker)
```
MEASURED — after, against the REAL install root:
```text
fleet homes: /home/altai/.claude/fleet-homes.list
  c3e5  /home/altai/proga/fleet-dogfood  ok (1 worker)     [listed record]
  a3ad  /home/altai/proga/fleet          ok (32 workers)   [install-root legacy term]
```
MEASURED — after, the refusal embeds the same population it counts; its two counts agreed in the lane's sandbox CLI run and in the supervisor's live run against the real install root.
MEASURED — view output and refusal used no lock, probe, registry repair, or homes-list write in the exercised CLI.
MEASURED — `homes_population()` reads the resolution population once per home. Live before/after against the real install root: the view showed 1 row while the guard counted 2; it now shows 2 labelled rows and the guard still counts 2.
MEASURED — the refusal renders from the arming snapshot, so its count and its embedded view cannot drift apart.
MEASURED BY THE SUPERVISOR AT LANDING — 932 passed, 2 skipped on Python 3.10 AND 3.12 across the thirteen derived suites, after the corrections below. Two failures remain and are NOT this lane's: `test_terminal_surface.py::TestCollaboratorInstall` (two cases) fails identically at `c05d6c3` with untouched source — a host venv artifact (`Could not find platform independent libraries`).

WHERE THIS BRIEF WAS WRONG

MEASURED — the initial patch assumed resolution metadata already had `path` and `reason`; the real CLI caught both missing aliases, which were corrected before final exercise.
BELIEVED — `/home/altai/proga/tap` remains intentionally absent; this lane neither discovers nor registers it.
MEASURED — no `fleet init`, `fleet homes --add`, `fleet homes --retire`, or global homes-list write was run.
MEASURED — docs updated: `docs/specs/multi-fleet.md`, `docs/PLAN-PROGRESS.md`.

## Four corrections made at landing

1. **`homes_population()` dropped the `ok` key** — repointed at `resolution_population()`, it aliased `path`, `reason` and `members` but not `ok`; `tests/test_homes_list.py` raised `KeyError: 'ok'`. Restored.
2. **The lane's two view tests contradicted each other**, and it ran neither: one required the legacy row, the other still required "no homes listed". Resolved toward the NARROWER contract — arming begins at a population of two, so a single-fleet machine has no disagreement to fix and its output is unchanged. A new test pins the multi-fleet case instead.
3. **`test_each_listed_home_reports_its_own_reason` was never updated** for the legacy term. It now asserts the term is present AND still pins each listed home's own reason, rather than dropping the assertion.
4. **The spec paragraph was inserted between steps 2 and 3** of the numbered resolution order the section is about, terminating the list. Moved below step 5 and extended to say what the view does when a row is present but uncounted.

`tools/repoint_self_citations.py c05d6c3` moved 42 of 53 citations; the lane had not run it.
