# Lane w78 — fleet land and structured lane result
DONE means: `fleet land <lane>` stages a validated lane result, rebases mechanically, runs named checks plus docs/receipt checks, and returns a red exit code for any failure.

MEASURED — `bin/fleet_land.py` is a leaf with its own Git, shell, and result validation subprocesses; it does not import `fleet` or copy parser state.
MEASURED — `fleet land <lane>` is registered by `build_parser()` and dispatches before fleet-home resolution because it acts on the repository/worktree.
MEASURED — lane resolution uses the Git worktree table; unknown and ambiguous lanes refuse. The source branch is the invoking non-lane branch whose tip contains the result base.
MEASURED — dirty worktrees stage only `files_changed` plus `docs/lanes/<lane>.md` and `.json`; an unlisted dirty path refuses before commit.
MEASURED — rebase conflicts call `git rebase --abort` and report conflicting paths; clean lanes are idempotent.
MEASURED — recorded command logs are retained under a temporary `/tmp/fleet-land-*` directory; command output is not mixed into the capped summary.
MEASURED — `docs/lanes/<lane>.json` is the first structured result instance and documents command/count fields, claim proof commands, and blockers.
MEASURED — the report is 28 lines and names the docs/template contract.

Checks: the exact requested code-touch suite and focused land suite were run on Python 3.10 and 3.12; fixed docs-currency and strict receipt commands also returned 0; final counts are in `w78.json`.
Checks: an untouched `e87c049` clone has no pre-existing failure in the mandated suites (code: 402 passed/4 skipped; docs: 179 passed/2 expected-xfailed); no baseline defect was changed.

WHERE THIS BRIEF WAS WRONG

The original step-3 concern was not a blocker: the lane name resolves its branch from registered worktrees, the invoking branch supplies the rebase base, and the commit message is deterministic. The implementation refuses ambiguity instead of guessing.

docs updated: `skills/fleet/SKILL.md`, `docs/lanes/BRIEF-TEMPLATE.md`, `docs/lanes/w78.md`

PATH LIST:
- `bin/fleet.py`
- `bin/fleet_land.py`
- `tests/test_fleet_land.py`
- `docs/lanes/BRIEF-TEMPLATE.md`
- `docs/lanes/w78.md`
- `docs/lanes/w78.json`
