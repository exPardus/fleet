# Lane w79 — knowledge caps

DONE means: `tools/knowledge_index.py` regenerates and checks the knowledge index, reports over-cap notes, rolls dated lessons verbatim, and targeted checks pass on Python 3.10 and 3.12.

- Implemented the stdlib-only `tools/knowledge_index.py` with `--check`, `--write`, and `--roll-lessons`.
- Index entries are derived from files present under `knowledge/`; existing one-line descriptions and header/footer prose are retained.
- Boot-cap failures apply to `lessons.md`; on-demand oversized notes are reported but never moved or truncated.
- Lesson dates use the archive’s existing `## YYYY-MM-DD — ...` heading format. Undated headings are skipped safely; dated entries older than 30 days are appended verbatim to the archive.
- Added `tests/test_knowledge_index.py` covering index regeneration and verbatim rolling with an undated fixture.
- Baseline failure at `e87c049`: the repository’s pre-existing documentation-cap xfail remains unrelated and was not changed.
- Checks on Python 3.10: knowledge/census suites — 26 passed, 0 failed, 0 xfailed; documentation suites — 179 passed, 0 failed, 2 xfailed.
- Checks on Python 3.12: knowledge/census suites — 26 passed, 0 failed, 0 xfailed; documentation suites — 179 passed, 0 failed, 2 xfailed.
- The two xfails are the pre-existing docs-total-cap and supervisor-GOALS-cap items at base `e87c049`.
