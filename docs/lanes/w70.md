# Lane w70 — substrate from the lane record; board lists only unanswered rulings

DONE means: THROUGHPUT reads substrate evidence from the lane/worker record;
pending rulings exclude ruled, `lens/`, and `sup~*.md` files; targeted tests
are green on Python 3.10 and 3.12.

Implemented:

- `wave-close` joins a landed branch to its registered worktree and reads an
  explicit worker substrate or matching `.mcx` record; absent evidence is
  `unknown`, never an inferred `claude`.
- Interface pending-ruling computation ignores supervisor-rendered `sup~*.md`
  files in addition to `lens/`, and still requires no `RULED:` line.
- Tests construct an mcx record plus a misleading merge trailer, an unknown
  lane, and ruled/unruled/lens/supervisor task files.

Checks:

- Python 3.10: 22 passed (`/tmp/w70-target-py310.log`).
- Python 3.12: 22 passed (`/tmp/w70-target-py312.log`).
- `git diff --check`: passed.
- Real w69 record lookup: `w69/wave-close-accounting: codex`.

PATH LIST:

- `bin/fleet.py`
- `tests/test_wave_close.py`
- `tests/test_interface_state.py`
- `docs/lanes/w70.md`
- `state/journals/w70.md`
