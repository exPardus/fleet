# Operator gates

Decisions only Altai can make. **Step 1 of the `fleet` skill's startup ritual reads this file** (`skills/fleet/SKILL.md`) and puts the open items to the operator before any work starts.

Until 2026-07-22 a SessionStart hook injected these into every session automatically. It also injected them into every *unrelated project* on the machine, so it was removed (`docs/specs/terminal-surface.md` D7). Nothing machine-parses this file now — the reader is a human or a model following the ritual, which is why the format below still matters.

Format is load-bearing and pinned by `tests/test_supervisor.py::TestOperatorGatesFile`:

- `- [ ]` is **open** and gets surfaced. It **must be phrased as a question**, ending in `?`.
- `- [x]` is **settled** and is never re-surfaced. Tick it, append the date and the answer, and leave it in place — a settled gate is the record of a decision, not clutter.
- Keep each line to one decision, front-loaded: the hook shows the line, not this file.

Neither the manager nor any worker may tick a box. An author never promotes its own spec; a reviewer may not ratify one either. Record each answer as a dated line in `knowledge/lessons.md` too — never session memory.

## Open

- [ ] **Contract rows — `docs/specs/native-substrate.md`.** 11 `[PENDING OPERATOR RATIFICATION]` markers across 8 rows: the 2.1.212 set plus M-E's 2.1.216 `daemon.lock` PID-reuse wedge. Each carries executed receipts, and the 2.1.216 row has a shipped detector and a live green pin behind it; the caveat stated in the rows themselves is that three dead-daemon claims stay manager-report-only, because a `--bg` worker is a live daemon client and structurally cannot observe that path. Manager recommends ratifying all eleven — ratify all, ratify some, or reject?
- [ ] **fleet-index M1 — take a milestone slot, or queue it?** (`docs/specs/fleet-index.md`, `Status: ready-for-build` for M1 only.) Adversarial review returned 10 defects (2 CRITICAL, 5 HIGH); all folded, and the CRITICALs forced the restructure — a global symbol table conflicted at N=2 on disjoint files, so the index is now sharded per source file, and `fleet q` moved out of M1 because it cannot execute under the default `dontask` mode without a `worker-settings.template.json` permissions migration. M1 is deliberately the small half: indexer + `--context` digest injection, opt-in per project and per spawn, no always-on cost, no permissions change, no committed artifact by default. Two things for you specifically: **(a)** you chose per-project config for tracked-vs-gitignored index — the spec implements it as two coherent bundles (`ignored` = refresh on stale; `tracked` = refuse on stale so queries never dirty a tracked file and `git worktree remove` keeps working), confirm that split or collapse to gitignored-only; **(b)** M1 ships a transcript-parsing counter because fleet has no tool-call telemetry today (`stop_outcome.py:203-206` records tokens, not calls), and without it M1's headline claim has no acceptance test — confirm that is in scope. Take a slot now, queue behind M-F, or shelve?
- [ ] **`docs/superpowers/specs/2026-07-21-worker-providers-native-design.md` — which half of it is true?** The header says `Status: approved design — ready for implementation plan`; its own §4.2 records the gating spike as NEGATIVE and ends "Do not implement §4 as written". The document contradicts itself, it arrived on `main` with the reconcile merge, and no author or reviewer may re-status it. `docs/longcat-fleet-usage.md` is the working alternative (per-`CLAUDE_CONFIG_DIR` isolated daemon namespace, verified end-to-end 2026-07-21). Re-status it to reflect the negative spike, park the whole line, or keep it approved on a different basis?
- [ ] **Two roadmaps, no crosswalk — retire the C-campaign/soak framing, or re-anchor the M-track inside it?** `ROADMAP.md` (Phases 1→6) and `PLAN.md` (campaigns C1→C8) describe a soak-gated build order; everything actually built since 2026-07-13 ran on the M-track (M-0…M-G). `SOAK GATE 1 SIGNED` appears **nowhere** in `knowledge/lessons.md`, yet every C3+ row and the Phase-1.5 build are marked GATED on that signature while M-0→Reconcile shipped around it, and `PLAN.md` §0.1.3 still names the retired `fleet-impl` branch as the merge target. The governing process document does not describe how this project is governed. Retire the C/soak framing as superseded history, re-anchor the M-track inside it, or keep both and write the crosswalk?
- [ ] **M-F shape and budget.** The dogfood-outward run is overdue since stupidbox on 2026-07-09, and external friction is the highest-value defect report available (standing goal 5). Should it preempt the queue above, and what is the budget envelope?

## Settled

- [x] **Claim-nonce — what does the claim gate?** *(2026-07-23 by Altai, in-session docket.)* Answer: **(b) a knowingly-bypassable gate** — the only option that closes the 2026-07-16 dual-supervisor incident. Chosen with §7's accounting stated: bypass documented in the design (an actor with shell access already owns the box), the gate is armed only while the heartbeat is fresh, `autoclean` is structurally exempt, and the corrected verb taxonomy in claim-nonce §7 becomes binding for the build. Recorded in `knowledge/lessons.md#2026-07-23-operator-decisions`.
- [x] **`docs/specs/claim-nonce.md` — promote to spec-of-record?** *(2026-07-23 by Altai.)* Answer: **promoted**, with the gate question above answered as (b). The three MINOR residuals from the final break gate close in the build slice before any code ships. Status line flipped in the same commit as this tick.
- [x] **`docs/specs/three-tier-command.md` — stays queued behind the nonce?** *(2026-07-23 by Altai.)* Answer: **no — re-draft now.** The nonce question is answered and its spec promoted, so the hard prerequisite is discharged at the design level. The re-draft follows the 2026-07-17 adjudication's binding list and folds in two new operator requirements from the same session: **tier-model assignment** (highest-tier model for the human-facing interface session, second-tier for the supervisor — today Fable 5 / Opus 4.8) and a **~200k-token supervisor swap band** (down from the drafted 300–500k) to save usage. Build still waits on the re-drafted spec's own design gate.
- [x] **SDD / drift-control design — R1–R4 ratified.** *(2026-07-20 by Altai; re-confirmed 2026-07-22 during the doc reconciliation, because this gate was still listed open while the spec's own `Status:` line already claimed the ratification.)* Answer: **ratified as written** — R1 Phase-1 gate first behind `sdd.enabled` (default off), R2 both scopes (slice ∩ whole-spec), R3 both judge paths (advisory either way), R4 specs git-tracked from birth at `docs/specs/campaigns/<campaign>.md` and folded into `docs/SPEC.md` when built. Spec is `v4 — build-ready pending the M-F slot`; the manager's "narrow to Phase-1 only" recommendation is superseded by R1, which already sequences Phase 1 first. Recorded in `knowledge/lessons.md#2026-07-22-operator-decisions`.

*(Tick a gate above and move it here with its date and the answer. This file was created 2026-07-21 at the operator's request, after four campaigns of decisions queued up in handoff prose that was read too late.)*
