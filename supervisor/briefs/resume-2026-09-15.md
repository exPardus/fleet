# Resume — fleet home, 2026-09-15

Model: keep the current supervisor's assigned model (opus). Continue the current body.

The operator lifted the stand-down. Codex is exhausted until 2026-09-18T00:58Z; Claude is back with the operator's explicit instruction "dont eat my limits". The keeper timer stays stopped by ruling; the interface wakes you by hand, so end a turn only at a real boundary and `fleet sup-notify` first.

One job for this home, one lane: `state/tasks/20260915-token-efficiency.md`. Render it with `fleet brief`, dispatch one sonnet worker with `fleet spawn --fleet-home /home/altai/proga/fleet --setting-sources project,local`, and relay its ranked list when it lands. No cuts land before the operator picks. No other fleet-repo lane until then; the tap home holds the other two host-wide slots.

Root clutter (`FIX-WAVE-*.md`, `REVIEW-INPUT-*.md`, `spike/`, `workflows/`) is cited by tests and `bin/fleet.py`; it moves in a later lane, not now.

Your own turns count too: read the task file and the lane report, nothing else you have already read this generation. Journal entries are for the successor only.
